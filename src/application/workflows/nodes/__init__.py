"""The node library — registry, catalog, factories, and node implementations.

Intentionally empty (no re-exports): callers import the specific submodule they
need (``nodes.registry``, ``nodes.factories``, …) so importing one node concern
never drags in the whole library.
"""
