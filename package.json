{
	"scripts": {
		"dev": "docker compose up -d && uv run alembic upgrade head && concurrently --raw \"uv run narada-api\" \"pnpm --prefix web dev\" \"sh -c 'for i in $(seq 60); do curl -sf http://localhost:8000/api/v1/health >/dev/null 2>&1 && open http://localhost:5173 && break; sleep 1; done'\""
	},
	"devDependencies": {
		"concurrently": "^9.2.1",
		"msw": "^2.12.10",
		"orval": "^8.5.1"
	}
}
