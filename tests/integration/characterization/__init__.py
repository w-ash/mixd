"""v0.8.18 characterization net: pins CURRENT identity-resolution behavior.

Several tests here deliberately pin *bugs* (see each docstring's FM id and
the flipping epic) so that fix commits flip an assertion instead of changing
silence. Enumerated in docs/backlog/identity-resolution-design-space.md §4.
"""
