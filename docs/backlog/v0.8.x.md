# v0.8.x: Scheduling & Advanced Workflow Features

For strategic overview, see the [planning overview](README.md).

The v0.8.x cycle modernizes the workflow engine (reliability hardening → Prefect removal) and then layers automation on top (workflow + sync scheduling), finishing with authoring polish (templates, import/export, sub-flows, playlist browse). Each sub-version below is an independently shippable, manually-testable slice. The cross-cutting substrate investigation that motivates v0.8.0–v0.8.2 is captured in the [workflow-execution-substrate decision memo](../architecture/workflow-execution-substrate.md).

---

## User Workflow Context

**What the Curator does today.** Every week, the Curator opens mixd, manually triggers their Last.fm and Spotify imports, waits for them to finish, then runs each smart-playlist workflow by hand and eyeballs the result. The ritual works — but it is entirely hands-on, and it depends on the user being at the keyboard at the right moment.

**Where it breaks down.**
- **No automation.** "Refresh my dinner-party playlist every Sunday morning with songs I loved this month" is a thing the user wants the *system* to do, not a chore they want to remember to do. Today there is no way to say it.
- **The run log isn't fully trustworthy.** A worker dying (OOM/SIGTERM) and a workflow's own logic failing both record as `failed`, so the user can't tell "mixd broke" from "my pipeline is wrong." A sweeper/worker race can overwrite a run's recorded outcome. A CPU-bound node can get a healthy run falsely reaped. Until the log is trustworthy, the user can't stop babysitting.
- **Misconfigured workflows fail silently.** A few validation gaps let a pipeline produce *wrong output without erroring* — the hardest failure to trace.
- **The engine is a poor scheduling substrate.** Prefect's embedded ephemeral server is a fragile base for the concurrent runs scheduling will drive (it's documented to hit stuck-states under concurrent flows).

**The cycle's job, in order.** (1) Make runs trustworthy and reject bad configs *(v0.8.0)*; (2) swap the engine to stdlib asyncio so scheduling sits on a clean substrate *(v0.8.1)*; (3) deliver scheduling CLI-first, then web, then background sync *(v0.8.2–v0.8.4)*; (4) reduce authoring friction *(v0.8.5–v0.8.6)*.

**Headline acceptance — the compound capability this cycle exists to deliver:**
> **Given** a Curator in their own timezone who has built a "Fresh Faves" workflow and connected Last.fm,
> **when** they schedule the Last.fm sync for "every night at 2am" and the workflow for "every Sunday at 6am,"
> **then** their data refreshes overnight and the playlist rebuilds Sunday morning — in *their* local time, surviving restarts and DST — and **if** an overnight run fails, they see it the next time they open mixd rather than discovering a stale playlist days later.

**Personas across the cycle.** Curator — primary (automates the weekly ritual). Tinkerer — a simpler engine to self-host and reason about; templates and sub-flows for complex pipelines. Casual — playlists stay fresh without effort (v0.8.4) and next-run visibility builds confidence (v0.8.3).

---

### v0.8.0: Run Reliability & Validation Hardening
**Goal**: Make workflow run history trustworthy and reject misconfigured workflows before they run.

**Context**: Auditing the workflow engine (against Prefect's own source) surfaced latent run-state bugs and a class of silent-wrong-result validation gaps. These live in `workflow_runs.py` / the sweeper / `validation.py` / the executor's cleanup path — and ship independently of the engine swap, delivering user-visible trust first and de-risking the swap (v0.8.1) and scheduling (v0.8.2+) that follow.

**What this unlocks**: A run marked `failed` means "the workflow logic broke"; `crashed` means "the worker died" — distinct triage. No more last-writer-wins corruption of a run's recorded outcome, no false-positive reaps of healthy runs, no leaked HTTP pools when a deploy interrupts cleanup, and misconfigured pipelines fail loudly at validation instead of silently producing wrong output.

**Persona**: Curator (trusts the run log enough to stop babysitting), Tinkerer (validation catches taxonomy mistakes early).

- [ ] **Run-state write hardening: terminal-write guard + distinct `crashed` status**

    **Story**: The Curator needs the run log to mean what it says. When they glance at a run marked `failed`, that must reliably mean "my workflow logic broke" — something *they* can fix — not "the worker was killed mid-run," which is an operational event. Today those collapse together, and worse, two writers can race to record a single run's outcome, leaving a run with a self-contradictory `duration_ms`/`error_message`. Without this, every "failed" badge invites a babysitting session to figure out what actually happened.

    **Decisions**:
    - **First-writer-wins on terminal state.** The sweeper (`workflow_run_sweeper.py:104`) and the run's own `except`/complete path (`workflow_runs.py:367,416`) can both write a terminal status to the same row (last-writer-wins, divergent fields). Guard `update_run_status` (+ repo writer) with `WHERE status NOT IN (terminal_set)` so the first terminal write wins — mirrors Prefect's `HandleFlowTerminalStateTransitions` (`core_policy.py:1589-1607`). Implement as the established conditional-UPDATE / `rowcount == 0` idiom (`track/core.py:327-340`, `playlist/links.py:192`); the v0.8.2 scheduler claim reuses the same shape.
    - **`crashed` is a distinct status, not a flavor of `failed`.** `failed` = workflow logic raised; `crashed` = worker died (OOM/SIGTERM). This is a triage-relevant distinction the sweeper currently flattens (Prefect models it as `StateType.CRASHED`).
    - Use name-overlays on existing statuses for any *future* sub-states (retrying/cached) rather than minting more top-level statuses (Prefect `states.py:883`).

    **Spec**:
    - Add `RUN_STATUS_CRASHED = "crashed"` to `src/config/constants.py` (today only `pending/running/completed/failed/cancelled` exist — `crashed` is new); include it in the terminal set and treat it as fail-class in run-level rollups (Prefect `states.py:686-687`).
    - `update_run_status` + the repository writer add `WHERE status NOT IN (<terminal_set>)`.
    - Sweeper writes `RUN_STATUS_CRASHED` (not `failed`); reclassify the SIGTERM/server-reload path (`workflow_runs.py:393`, currently writes `failed`) to `crashed`.
    - Surface cold-start latency as a first-class field: `started_at − created_at` (Prefect's `estimated_start_time_delta`, `objects.py:660`).

    **Tests**:
    - (integration) Terminal-write race: two concurrent sessions — sweeper marks `crashed` while the completion path marks `completed` on the same run; assert first-writer-wins, the loser is a silent no-op, fields stay self-consistent. (Build a two-session helper on the existing testcontainers harness; none exists yet.)
    - (unit) `crashed` is counted as fail-class in rollups; `failed` and `crashed` are distinguishable.
    - (unit) SIGTERM/reload path records `crashed`, not `failed`.

    Effort: S | Dependencies: None | Status: Not Started

- [ ] **Crash-detection hardening: heartbeat on an OS thread**

    **Story**: A long, CPU-bound enrichment node shouldn't get a *healthy* run killed out from under the Curator. Today the heartbeat ticker shares the asyncio loop, so a node that blocks the loop stops the heartbeat — and the sweeper reaps a run that was actually fine. The user sees a spurious `crashed` and re-runs work that didn't need re-running.

    **Decisions**:
    - **Emit heartbeats from an OS daemon thread**, not an asyncio task — mixd's own sweeper docstring names "event loop blocked → heartbeat ticker can't fire" as a failure mode, and Prefect emits from a daemon thread for exactly this reason (`flow_engine.py:303-306`).
    - **Derive the stale threshold from the heartbeat interval** (a multiple), not a hardcoded 60s (mirrors Foreman, `foreman.py:88-94`).
    - **Stop heartbeating once terminal** (`flow_engine.py:330-336`), and **batch-cap each sweep** (Prefect caps at 400, `late_runs.py:68`).

    **Spec**:
    - Move heartbeat emission to a daemon thread; thread stops when the run reaches a terminal state.
    - Stale threshold = `heartbeat_interval × multiple` in the sweeper.
    - Sweeper processes at most N rows per cycle.

    **Tests**:
    - (unit) A blocked event loop still heartbeats (thread keeps ticking); the run is not reaped.
    - (unit) Heartbeat stops after terminal transition.
    - (unit) Sweep batch is capped.

    Effort: S | Dependencies: None | Status: Not Started

- [ ] **Close pre-execution validation gaps**

    **Story**: When the Tinkerer wires up a workflow wrong, they want to be told *at validation time* — not to get a quietly-wrong playlist they then have to reverse-engineer. mixd's declarative validator already exceeds Prefect's (which does no static DAG validation), but three gaps produce wrong output *without erroring*, which are the hardest failures to trace.

    **Decisions**:
    - Reject the three silent-wrong-result cases the engine audit found; add each to both the blocking `validate_workflow_def` and the editor's `validate_workflow_def_detailed`.
    - These are *correctness* checks, not style — each one today yields wrong output that looks plausible.

    **Spec** (new `_validate_*` helpers in `src/application/workflows/validation.py`, called from both validators at `:126-166` and `:297-355`):
    - **`primary_input`:** reject when `config["primary_input"]` is set but not present in `upstream` (today silently falls back to `upstream[0]`, `prefect.py:285-289`).
    - **`result_key`:** reject a `result_key` that **collides with any task id**, and reject duplicate `result_key`s across tasks. (Precise mechanism: `task_results[result_key] = result` is stored as an *alias* alongside `task_results[task_id]` at `prefect.py:404-409`; if task B's `result_key` equals task A's id, A's stored result is silently overwritten by B's alias — not a "duplicate result_key" per se. The check guards against both collisions.)
    - **Non-source node with no upstream:** reject a non-source node placed at level 0 (today passes validation, then fails/empties at runtime).

    **Tests**:
    - (unit) `primary_input` set-but-not-in-upstream is rejected by both validators.
    - (unit) `result_key` that shadows a task id is rejected; duplicate `result_key`s are rejected.
    - (unit) Non-source node at level 0 is rejected.
    - (unit) Valid workflows still pass (no false positives).

    Effort: S | Dependencies: None | Status: Not Started

- [ ] **Shield the connector-cleanup `finally` against SIGTERM**

    **Story**: When a deploy or autoscale event sends SIGTERM mid-run, the Curator shouldn't pay for it with leaked HTTP connection pools that degrade the next process. This is a *present-tense* bug in today's executor, independent of the v0.8.1 swap — it's pulled into v0.8.0 with the other reliability fixes so the leak doesn't ride through a release.

    **Decisions**:
    - **Wrap the cleanup await in `asyncio.shield()`.** A `CancelledError` landing on `await workflow_context.connectors.aclose()` (`prefect.py:471`) during SIGTERM currently aborts the close and leaks httpx pools. Prefect shields its finalizers (`flow_engine.py:1491-1497`).
    - Note for v0.8.1: this lives in the executor cleanup path, so when `prefect.py` becomes `executor.py` the shield carries over unchanged — but it ships *now*, not with the swap.

    **Spec**:
    - `await asyncio.shield(workflow_context.connectors.aclose())` in the cleanup `finally` at `prefect.py:471`.

    **Tests**:
    - (unit) A `CancelledError` raised during shutdown still completes `aclose()` (connectors closed exactly once, no leak).

    Effort: XS | Dependencies: None | Status: Not Started

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.0 accumulate here (`v0.8.0.N` tags)._

---

### v0.8.1: Workflow Engine Swap (Prefect to stdlib asyncio)
**Goal**: Replace Prefect's `@flow`/`@task` with a stdlib-`asyncio` executor — lighter runtime, no embedded server, a clean substrate for scheduling.

**Context**: v0.7.8.x burned five revisions fighting Prefect 3's import weight + ephemeral SQLite-backed server. The prework investigation (below) confirmed mixd uses none of Prefect's distinctive value and decided to remove it before scheduling layers concurrent runs on top. Manual test: workflows run identically, `prefect` is not imported at app boot, cold start is faster.

**What this unlocks**: No new end-user feature, but a real user-visible outcome the Tinkerer cares about — **faster cold start, a self-host with no embedded server process to babysit, and a simpler engine to reason about** — plus the enabler the rest of the cycle needs: it drops the ephemeral server (~80–120 MB at run time), the 222-module import, and two env-var workarounds, so scheduling's concurrent runs sit on stdlib asyncio rather than a fragile embedded server (Prefect's embedded SQLite server is documented to hit stuck-states under concurrent flows). _(Per the backlog "every slice ships user value" rule: this slice is justified by the Tinkerer-visible runtime improvement and the concurrency forward-risk it removes, with a regression-test manual scenario rather than a new UI surface.)_

**Persona**: Tinkerer (a simpler, server-free engine to self-host and reason about).

#### Prework (resolved) — substrate decision

- [x] **Investigate lighter-weight, more modern Prefect alternatives** *(prework)* — Resolved 2026-05-28

    **Why**: v0.7.8.x burned three revisions (`.3` prewarm → `.4` revert → `.5` lazy-load + ephemeral disable + uvicorn tuning) chasing the same root cause: Prefect 3's import weight + ephemeral SQLite-backed FastAPI server are an awkward fit for the deployment. Our actual usage of Prefect is shallow: `@flow` / `@task` decorators, `asyncio.gather()` over precomputed DAG levels (`validation.compute_parallel_levels`), and run-state tracking (which we own in `workflow_runs`). The Prefect import alone loads 222 modules; the runtime never uses most of them.
    **Output**: [Decision memo](../architecture/workflow-execution-substrate.md) — two decisions:
    - **Decision A — Execution substrate: swap Prefect → stdlib `asyncio`** (this version, v0.8.1). The `@flow`/`@task` decorators only feed Prefect's UI/observability surface, unreachable in prod, while `@flow` structurally forces the ephemeral server (Prefect 3 has no offline `@flow`). No job framework can replace DAG execution anyway. **Premise correction:** the "512 MB" pressure is gone — the box is `shared-cpu-2x` / 1 GB (commit `0369b414`); the swap now rests on simplicity + the concurrency forward-risk.
    - **Decision B — Scheduling substrate: RESOLVED → hand-roll** (`croniter` + asyncio poll loop). APScheduler 4 is still alpha (`4.0.0a1`, May 2026) and APScheduler 3.x brings its own job store that duplicates the `schedules` table; PgQueuer's multi-instance `SKIP LOCKED` value is latent on a single-instance deployment and costs a v1.0 dependency + a dedicated autocommit connection + non-Alembic tables. mixd already runs a long-lived poll loop (`run_sweeper_loop()`), so the hand-roll has a living precedent and is tested with the existing testcontainers harness. See v0.8.2 Key Tech Choices.
    **Status**: ✅ Resolved (2026-05-28; Decision B closed 2026-05-30).

- [ ] **Replace Prefect engine with stdlib asyncio executor**

    **Story**: The Tinkerer self-hosting mixd wants the app to boot fast and run without an embedded orchestration server they never asked for. This story rewrites the executor internals so Prefect contributes nothing at runtime and isn't imported at boot — preserving identical workflow behavior while shedding the server, the 222-module import, and two env-var workarounds.

    **Decisions**:
    - Drop `@flow`/`@task`/`tags`/`cache_policies`/`get_run_logger`; keep `compute_parallel_levels` + the level loop. Signature + `OperationResult` return unchanged (the seam `run_workflow(...) -> OperationResult` leaks no Prefect types — verified by the memo's implicit-behavior audit).
    - **Logging:** replace the ~4 `flow_logger.*` lifecycle breadcrumbs with the module's existing `logger = get_logger(__name__)`, re-emitting them explicitly. Per-run JSONL correlation (`logging_context` contextvars + `add_workflow_run_logger`, `logging.py:236`) is mixd's own — unaffected. Drop the dead `getLogger("prefect").setLevel(...)` and `LoggingConfig.prefect_log_level`.
    - **Config-test replacement (don't just delete):** removing `LoggingConfig.prefect_log_level` deletes its assertions in `tests/unit/config/{test_logging,test_settings_validation}.py`. Replace them with an assertion that the logging config is clean post-swap (no prefect-named handlers/levels) so the deletion can't silently leave dead config handling.
    - Remove `prefect>=3.6.19` (pyproject.toml) + all `PREFECT_*` env vars (fly.toml + Dockerfile); run `uv sync`.

    **Spec**:
    - Rename `src/application/workflows/prefect.py` → `executor.py`; update the 3 import sites (`use_cases/workflow_runs.py`, `use_cases/workflow_preview.py`, `cli/workflow_commands.py`).
    - **Explicit test rename:** `tests/unit/application/workflows/test_prefect.py` → `test_executor.py`, and rewrite every `from src.application.workflows.prefect import ...` within it (these imports break on the rename — not "passes unchanged").
    - **Architectural guard test:** assert `"prefect" not in sys.modules` after importing the API app (locks in the v0.7.8.5 zero-modules win).
    - **Golden `OperationResult` regression:** capture a serialized-to-dict snapshot of `OperationResult` for a representative workflow **against the current Prefect engine, before the swap**, committed as a fixture; assert byte-identical after the swap. (No `syrupy` dependency — a serialize-and-compare fixture; the snapshot must exist *before* the rewrite or it isn't a regression net.)

    **Tests**:
    - (unit) Existing engine tests are the regression net and pass after the rename: `test_validation`, `test_fault_tolerance` (enricher-degrade, fatal source/transform), `test_transform_execution`, `test_executor` (renamed), `test_workflow_runs`; integration `test_workflow_run_repository`, `test_workflow_runs`.
    - (unit) Golden `OperationResult` snapshot identical pre/post swap.
    - (unit) Per-run JSONL still keyed on `workflow_run_id` after the rename.
    - (unit/architectural) `"prefect" not in sys.modules` after app import.

    Effort: M | Dependencies: Prework decision (resolved) | Status: Not Started

- [ ] **Level executor on `asyncio.TaskGroup` with explicit failure semantics**

    **Story**: The house convention mandates `TaskGroup` over `asyncio.gather`, but because `_run_node_lifecycle` never raises (it returns tuples), a mechanical swap would change *nothing* about failure handling — and quietly pick a sibling-cancellation behavior by accident. This story makes that behavior a *conscious* choice so a fatal node does the right thing to its level-siblings.

    **Decisions**:
    - **Deliberate semantics, not a mechanical swap.** With the tuple-return contract a `gather`→`TaskGroup` swap delivers zero sibling cancellation. Choose explicitly: **(a)** keep tuple-return — siblings run to completion, matching today and Prefect's own choice (`futures.py:557-576`); or **(b)** let fatal (non-enricher) nodes raise inside the group and handle the `ExceptionGroup` with `except*`. **Default: (a)** preserve current behavior unless fast-fail sibling cancellation is explicitly wanted. **This decision must be closed before the story is implemented** (its test can't be written until it is).
    - Distinct from the scheduler's isolation model (v0.8.2): there, each dispatch is wrapped *inside its own coroutine's* try/except so one schedule's failure never reaches the group — opposite intent, same primitive.
    - **Keep `CancelledError` out of the degrade/failure path.** Verify (don't assume) that `except (TimeoutError, Exception)` (`prefect.py:313`) can't catch cancellation — in Python 3.8+ `CancelledError` is a `BaseException`, so `except Exception` already won't catch it; confirm this holds and migrate to PEP 758 no-parens `except` syntax per `python-conventions.md`. Ensure `WorkflowCancelledError` can't enter `_is_failure_recoverable` (Prefect's `Exception` vs `BaseException` fault line, `task_engine.py:1499-1513`).
    - Preserve: per-node `asyncio.timeout`, enricher-degrade-vs-fatal, SIGTERM `_shutdown_requested` checks, observer callbacks. _(The cleanup-shield fix shipped in v0.8.0 and carries into `executor.py` unchanged.)_

    **Spec**:
    - Replace `asyncio.gather()` with `async with asyncio.TaskGroup()` implementing the chosen semantics.

    **Tests**:
    - (unit) Independent nodes in a level run concurrently (gate via `asyncio.Event`).
    - (unit) A fatal node cancels/finishes siblings per the chosen semantics, with no orphaned tasks.
    - (unit) `WorkflowCancelledError` is not treated as recoverable/degradable.

    Effort: S | Dependencies: Replace Prefect engine | Status: Not Started

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.1 accumulate here (`v0.8.1.N` tags)._

---

### v0.8.2: Workflow Scheduling - Engine & CLI
**Goal**: Schedule a workflow (or background sync) to run automatically, drivable end-to-end from the CLI.

**Context**: The scheduling engine — DB-stored schedules + an in-process polling loop in the FastAPI lifespan — plus CLI commands as the first user surface (CLI-before-Web, mixd's established pattern). The web UI follows in v0.8.3. Depends on the engine swap (v0.8.1) so scheduling's concurrent runs sit on the stdlib executor.

**What this unlocks**: `mixd workflow schedule <id> --cron "0 6 * * 1" --tz America/Los_Angeles` and `mixd sync schedule lastfm:plays --every 1h` — schedules persist across restarts, fire automatically, and respect the user's local time across DST.

**Persona**: Curator/Tinkerer (terminal-first power use); the foundation every other scheduling slice builds on.

**Key Tech Choices**:
- **Substrate = hand-roll (resolved).** In-process async scheduler — database-stored schedules + an async poll loop in the FastAPI lifespan — not Prefect server / APScheduler / PgQueuer. Rationale: single-VM single-instance today (so PgQueuer's multi-instance `SKIP LOCKED` is latent), APScheduler 4 is alpha and 3.x duplicates our schedule store, and mixd already runs `run_sweeper_loop()` as a precedent. ~100 LOC fully owned and tested with the existing testcontainers harness. The hardening below (forward-from-now next-run, stable interval anchor, cadence correction, stuck-start reaper, optimistic lock) is therefore mandatory. Full comparison: [decision memo](../architecture/workflow-execution-substrate.md) (Decision B).
- **`croniter>=6.0` for cron parsing** — lightweight, `croniter.is_valid(expr, strict=True)` and `croniter(expr, base).get_next(datetime)`; passing a timezone-aware base makes croniter handle DST automatically.
- **Single `schedules` table** with a `target_type` discriminator (`"workflow"` | `"sync"`) — one schema/migration/repository/CRUD surface for both kinds.
- **Per-schedule IANA timezone** — store the user's zone; compute `next_run_at` by evaluating the cron in that zone, then persist `next_run_at` in UTC. The polling query stays UTC-simple; correctness lives at compute time; croniter owns DST.
- **Pre-computed `next_run_at`** — indexed column makes polling a simple `WHERE status='enabled' AND next_run_at <= NOW()` index scan, not O(all-schedules) croniter evaluation.
- **60-second poll interval** — balances responsiveness with Neon keep-alive and resource efficiency on the 1 GB / shared-cpu-2x Fly.io VM.

- [ ] **Add `croniter` Dependency**

    **Story**: Computing next fire times and validating cron expressions without a Prefect server requires a small, dependency-light cron library — the foundation the scheduler and its validators stand on.

    **Decisions**:
    - `croniter>=6.0` (matches the API surface researched: `is_valid(strict=...)`, tz-aware `get_next`); ~400 KB, no transitive deps.

    **Spec**:
    - Add `croniter>=6.0` to `pyproject.toml`; run `uv sync`.

    **Tests**:
    - (unit) `croniter.is_valid("0 6 * * 1", strict=True)` is True; an impossible date (`0 0 31 2 *`, strict) is False.

    Effort: XS | Dependencies: None | Status: Not Started

- [ ] **Schedule Domain Entity**

    **Story**: Both workflow and sync schedules share one shape — a target, a cadence, a timezone, and execution-tracking state. A single immutable domain entity captures that so the rest of the system (repository, use cases, API) speaks one language for "a thing that runs on a schedule."

    **Decisions**:
    - **Exactly-one target, modeled honestly.** Instead of a polymorphic `target_id`, the entity carries `workflow_id: str | None` and `sync_target: str | None`; an `__attrs_post_init__` invariant enforces exactly one is set, and `target_type` is derived. This keeps the domain connector-agnostic (no sync identifiers baked in) and mirrors the DB's exclusive-arc shape (see migration story).
    - **Timezone is first-class.** `timezone: str` (IANA name) so a cron schedule means the user's local time, surviving DST.
    - **Domain purity:** `compute_next_run()` lives in the **application** layer (it imports `croniter`); the entity stores `next_run_at` as a plain field the application layer sets.
    - Interval schedules fire on a stable grid anchored at `anchor_at`, not "now on enable" (so "every 6h" keeps stable fire times across restarts — lesson from Prefect `schedules.py:303-306`).

    **Spec**:
    - `src/domain/entities/schedule.py` — `@define(frozen=True, slots=True)`.
    - `workflow_id: str | None`, `sync_target: str | None`, derived `target_type` property; invariant: exactly one of the two is non-None.
    - `timezone: str` (IANA, e.g. `"America/Los_Angeles"`).
    - `schedule_type: Literal["cron", "interval"]` + `cron_expression | interval_seconds`.
    - `status: Literal["enabled", "disabled"]`.
    - `next_run_at` (pre-computed, UTC), `last_run_at`, `last_run_status`, `last_error`.
    - `run_count`, `consecutive_failures` for observability.
    - `anchor_at` (defaults to creation time) for the interval grid `anchor + ceil((now − anchor)/interval) × interval`.
    - `config: Mapping[str, JsonValue]` for target-specific overrides (e.g. `{"import_mode": "incremental"}`).
    - _(`SyncTarget` is derived in the application layer from the canonical sync identities, not redefined here — see CRUD use cases.)_

    **Tests**:
    - (unit) Constructing with both `workflow_id` and `sync_target` raises; with neither raises; with exactly one succeeds and `target_type` derives correctly.
    - (unit) Entity is frozen/immutable.

    Effort: S | Dependencies: None | Status: Not Started

- [ ] **Database Model + Migration**

    **Story**: Schedules must survive restarts and stay isolated per user, so the engine can reload them after a deploy and never cross users' wires. This story is the persistent store — and it's where the original spec's single-column polymorphic FK is replaced with a design PostgreSQL can actually enforce.

    **Decisions**:
    - **Exclusive-arc, not a polymorphic column.** A single column can't carry a conditional FK *and* hold sync-enum strings (a column-level FK applies to every non-null value; a `text` value can't reference `workflows.id`). Use two nullable columns with a CHECK — the May-2026 best practice ([Cybertec](https://www.cybertec-postgresql.com/en/conditional-foreign-keys-polymorphism-in-sql/)), reusing mixd's existing CHECK-constraint and partial-index patterns.
    - **`sync_target` is plain `TEXT`, not a DB enum** — validated in the application layer (see CRUD), so adding a connector (Apple Music, v1.0.1) is a one-line Literal edit, no migration.
    - **Uniqueness via partial indexes** (NULLs are distinct in Postgres, so a plain composite unique wouldn't enforce one-per-target): one partial unique index per arm.

    **Spec** (`DBSchedule` + Alembic migration):
    - `workflow_id UUID NULL REFERENCES workflows(id) ON DELETE CASCADE` — deleting a workflow removes its schedule at the DB level.
    - `sync_target TEXT NULL`.
    - `target_type TEXT NOT NULL` (discriminator for readable polling/queries).
    - `CHECK (num_nonnulls(workflow_id, sync_target) = 1)` — exactly one target (pattern from `alembic/versions/004_check_constraints.py`).
    - `CHECK` tying `target_type` to the populated arm (`target_type='workflow'` ⇔ `workflow_id IS NOT NULL`).
    - `timezone TEXT NOT NULL DEFAULT 'UTC'`.
    - `next_run_at` stored UTC, truncated to whole seconds (see repository — keeps the optimistic-lock equality robust).
    - `config` via `PgJsonb`; serialized size validated in the use case (max 4 KB); `last_error` capped at 2000 chars via the existing `truncate_error_message` (matches `workflow_runs.error_message`).
    - Partial unique indexes: `(user_id, workflow_id) WHERE workflow_id IS NOT NULL` and `(user_id, sync_target) WHERE sync_target IS NOT NULL` (pattern from `db_models.py` `uq_primary_mapping`, `postgresql_where=text(...)`).
    - Polling index `ix_schedules_next_run` on `(status, next_run_at)`.
    - Working `downgrade()` (drop table + indexes + constraints).

    **Tests**:
    - (integration) Insert a workflow schedule (`workflow_id` set, `sync_target` NULL) succeeds; deleting the workflow cascades and removes the row.
    - (integration) Insert a sync schedule (`sync_target="lastfm:plays"`, `workflow_id` NULL) succeeds — the old polymorphic-FK design would have failed here.
    - (integration) A row with both arms set, or neither, is rejected by the CHECK.
    - (integration) Partial unique index rejects a second schedule for the same `(user_id, workflow_id)` / `(user_id, sync_target)`.

    Effort: S | Dependencies: Schedule Domain Entity | Status: Not Started

- [ ] **Schedule Repository**

    **Story**: Clean data access for schedule CRUD plus the scheduler's hot path — finding due schedules and claiming them without two poll cycles double-firing the same schedule.

    **Decisions**:
    - **Optimistic claim via timestamp equality, made robust.** `mark_schedule_started` uses `WHERE status='enabled' AND next_run_at = :expected` (the status check prevents starting a just-disabled schedule in the race window). This is safe because croniter emits second-boundary datetimes and we store `next_run_at` truncated to whole seconds — so the read-then-write round-trip compares equal. A lost race (0 rows) is a silent no-op, never an error. **Use the established conditional-UPDATE / `rowcount == 0` idiom** (`track/core.py:327-340` optimistic lock, `playlist/links.py:192`) — the same shape as v0.8.0's terminal-write guard, so the codebase has one claim/guard idiom, not two new divergent ones.
    - **`started_at` marker for the reaper.** `mark_schedule_started` records a running marker so the stuck-start reaper (Scheduler Service) can reclaim a schedule whose dispatch died between start and completion.
    - `find_due_schedules(now)` is a **cross-user system query** used only by the internal scheduler — never by API endpoints.

    **Spec**:
    - CRUD: `create_schedule`, `update_schedule`, `delete_schedule`, `get_schedule_by_id`, `get_schedule_for_target`, `list_schedules`.
    - Scheduler: `find_due_schedules(now)`; `mark_schedule_started(id, now, expected_next_run)`; `mark_schedule_completed(id, next_run_at)` (**resets `consecutive_failures = 0`** — see Scheduler Service); `mark_schedule_failed(id, error, next_run_at)`.
    - `ScheduleRepositoryProtocol` in domain; SQLAlchemy impl in infrastructure; `get_schedule_repository()` on `UnitOfWorkProtocol`.

    **Tests**:
    - (integration) `mark_schedule_started` optimistic lock: two concurrent callers with identical `expected_next_run` — exactly one updates, the other is a silent no-op (two-session testcontainers helper).
    - (integration) A disabled schedule can't be started in the race window (status guard).
    - (integration) `find_due_schedules` returns only due, enabled rows, across users.

    Effort: S | Dependencies: Database Model + Migration | Status: Not Started

- [ ] **Scheduler Config**

    **Story**: A self-hoster needs to turn the scheduler off in local dev and tune it in prod — without code changes.

    **Decisions**:
    - Nested Pydantic sub-model following the existing `ConnectorAPIConfig` + `_FLAT_ENV_ROUTES` pattern in `src/config/settings.py` (accessed as `settings.scheduler.*`, env via `SCHEDULER__*`).

    **Spec**:
    - `SchedulerConfig`: `enabled: bool = True`, `poll_interval_seconds: PositiveInt = 60`, `max_concurrent_scheduled_runs: PositiveInt = 3`, `catchup: bool = False`, `min_interval_seconds: PositiveInt = 300`, `stuck_start_timeout_seconds: PositiveInt`.
    - Register routes in `_FLAT_ENV_ROUTES`.

    **Tests**:
    - (unit) `SCHEDULER__ENABLED=false` disables; defaults load; `poll_interval_seconds` rejects non-positive.

    Effort: XS | Dependencies: None | Status: Not Started

- [ ] **Schedule CRUD Use Cases**

    **Story**: The Curator must be stopped from footgunning their own automation — an invalid cron, a too-frequent schedule that exhausts API rate limits, a non-existent target, two schedules fighting over one target. This story is the application-layer rulebook, and it puts validation where **both** the CLI and the API hit it (the CLI bypasses the API entirely).

    **Decisions**:
    - **Validate at the use-case layer, reused at the edges** — following the project's `normalize_tag` pattern (pure validators reused by API Pydantic `AfterValidator` + CLI `typer.BadParameter`, with the use case as the non-bypassable backstop). API/CLI checks are fast-fail UX; the use case is the guarantee.
    - **`compute_next_run()` lives here**, always computed **forward from `now`**, never from a stale stored `next_run_at` (a long outage on an every-5-min schedule would otherwise iterate croniter through thousands of skipped intervals — Prefect `scheduler.py:177`). It builds a tz-aware base via `zoneinfo.ZoneInfo(schedule.timezone)` and stores the result in UTC.
    - **`SyncTarget` derives from the canonical sync identities — not a hand-listed third enumeration.** The valid `(service, entity)` pairs already exist as `ServiceType` (`import_play_history.py:28`) + `CHECKPOINT_COMBINATIONS` (`sync_likes.py:145`). Define the schedulable targets (application layer, beside `ServiceType`) as the curated *subset* of those identities formatted `f"{service}:{entity}"` — `lastfm:plays`, `spotify:likes`, `lastfm:likes` (note `spotify:plays` is excluded — file-import only, not a background sync). Adding a connector then touches one source, not three. Validation is membership in that derived set.
    - **Min-interval is a simulation, not static analysis.** croniter has no min-gap helper; sample `get_next()` across a 24h window and reject if any consecutive gap `< min_interval_seconds`. Enforced for both cron (simulation) and interval (direct compare), in **both** Create and Update.

    **Spec**:
    - `CreateScheduleUseCase`: validate cron (`is_valid(strict=True)`), validate target exists (workflow via `workflow_repo`; sync via `SyncTarget`), validate timezone (IANA), validate `config` ≤4 KB serialized, enforce min-interval, enforce one-schedule-per-target, compute initial `next_run_at`.
    - `UpdateScheduleUseCase`: same shared validators (cron, min-interval, tz, config) — **must recompute `next_run_at`** from the new expression, never carry the old value.
    - `ToggleScheduleUseCase`: on enable, recompute `next_run_at` — cron from `now` in the schedule's tz; **interval anchored to `anchor_at`'s stable grid** (not "now"), so re-enabling doesn't shift phase.
    - Shared `_validate_schedule_timing(...)`, `_validate_cron(...)`, `_validate_timezone(...)`, `_validate_config_size(...)` reused by API + CLI.

    **Tests**:
    - (unit) Cron firing more often than `min_interval_seconds` is rejected — including a compound expression (`0,4,8,...`) the simulation catches but a naive `*/N` check would miss.
    - (unit) The same rejection happens via `UpdateScheduleUseCase` (no create-then-update bypass).
    - (unit) Invalid IANA tz, oversized `config`, unknown `sync_target`, missing workflow target each rejected.
    - (unit) `compute_next_run` for `"0 6 * * 0"` in `America/Los_Angeles` lands on 6am local across a DST boundary (tz-aware base), stored as the correct UTC instant.
    - (unit) Update recomputes `next_run_at`; toggle-enable anchors interval phase to `anchor_at`.

    Effort: M | Dependencies: Schedule Repository, Add `croniter` Dependency | Status: Not Started

- [ ] **Scheduler Service (Polling Loop)**

    **Story**: This is the engine that makes "every Sunday at 6am" actually happen — the background loop that wakes up, finds what's due, runs it on the same code path as a manual trigger, and does so resiliently: one schedule's failure can't take down the others, a dead dispatch can't wedge a schedule forever, and a token expiry surfaces as a failure the Curator will see rather than silent staleness.

    **Decisions**:
    - `src/application/services/scheduler.py` — an **application-layer background service** (peer of `run_sweeper_loop()`), not an infrastructure bridge.
    - **Per-schedule failure isolation.** Use `async with asyncio.TaskGroup()` but wrap each dispatch **inside its own coroutine's** try/except so a failure never propagates to the group's `ExceptionGroup` (a TaskGroup is a cancel-scope; an unhandled child exception would cancel sibling dispatches — [Python docs](https://docs.python.org/3/library/asyncio-task.html)). This is the inverse of v0.8.1's level-executor intent.
    - **Cadence correction:** `asyncio.sleep(max(0, poll_interval − cycle_duration))` to prevent drift toward ever-longer real intervals (Prefect docket `_perpetual.py:130`).
    - **Concurrency gate:** `asyncio.Semaphore(settings.scheduler.max_concurrent_scheduled_runs)`.
    - **Missed-run policy:** on startup, schedules with `next_run_at` in the past advance to the next future fire (not once per missed interval). `catchup=False` (default) skips all missed fires; `catchup=True` runs one catchup then advances.
    - **Per-user token isolation:** dispatch binds `user_id` from the schedule row to the use case (assertion / typed user-scoped command) — a scheduled job for user A can never run with user B's OAuth tokens.
    - **Stuck-start reaper — reuse the sweeper's mechanism.** A schedule whose `started_at` marker exceeds `stuck_start_timeout_seconds` without completing is `mark_schedule_failed` + advanced (Prefect's `MarkLateRuns`, `late_runs.py:69-85`). This is the same operation as `sweep_stalled_runs`/`list_stalled_runs` (`workflow_run_sweeper.py`) on a different table — follow that pattern, including the v0.8.0 *threshold-as-multiple-of-interval* + *batch-cap* hardening, rather than writing a parallel reaper. _(A generic two-table staleness-reaper is available but over-engineered for two callers — skip it.)_
    - **`consecutive_failures` resets to 0 on success** (in `mark_schedule_completed`) and increments on failure — without the reset, the v0.8.3 dashboard banner would latch on forever after a single failure.

    **Spec**:
    - Main loop: `find_due_schedules(now)` → dispatch (under semaphore + per-task try/except) → cadence-corrected sleep.
    - Workflow dispatch: check `is_workflow_running()` (in-process guard) then `RunWorkflowUseCase` → `ExecuteWorkflowRunUseCase` (same path as `POST /workflows/{id}/run`). `WorkflowAlreadyRunningError` → advance to next fire, no retry storm, **no `consecutive_failures` increment**.
    - Sync dispatch: map `sync_target` → import use case (`lastfm:plays`→`ImportTracksUseCase`, `spotify:likes`→`ImportSpotifyLikesUseCase`, `lastfm:likes`→`ExportLastFmLikesUseCase`).
    - Optimistic `mark_schedule_started` prevents double-execution across poll cycles.
    - OAuth: import use cases refresh internally; full expiry → `mark_schedule_failed` (schedule stays enabled, `consecutive_failures` increments). Store the connector classifier's safe message (not raw `str(exc)`), capped via `truncate_error_message`.

    **Tests** (unit, time-injected `now` + mocked repo — no `freezegun`):
    - (unit) Two due schedules; one's dispatch raises — the other still runs (isolation holds).
    - (unit) `catchup=False`: 3 missed fires → advance to next future, zero catchup runs; `catchup=True` → exactly one catchup then advance.
    - (unit) Cadence correction keeps loop wall-time bounded when dispatch takes nonzero time.
    - (unit) Stuck-start reaper reclaims a schedule wedged past the timeout.
    - (unit) `WorkflowAlreadyRunningError` advances without incrementing `consecutive_failures`; a real failure increments; a subsequent success resets to 0.
    - (unit) Sync dispatch for a schedule owned by user A never reads user B's tokens.

    Effort: M | Dependencies: Schedule CRUD Use Cases, Scheduler Config | Status: Not Started

- [ ] **Wire Scheduler into FastAPI Lifespan**

    **Story**: The scheduler should "just be on" with the server — no separate worker process for the Tinkerer to run — and it must not crash the server during the deploy window before its table exists.

    **Decisions**:
    - Reuse the exact lifespan pattern of `run_sweeper_loop()` / `_seed_templates()` (`src/interface/api/app.py`): `asyncio.create_task(...)` on startup, `task.cancel()` + `contextlib.suppress(CancelledError)` on shutdown. Conditional on `settings.scheduler.enabled`.
    - **[DRY — deferred]** The loop skeleton (`while True / try / except CancelledError: raise / except: log / sleep`) + this lifespan wiring is identical to `run_sweeper_loop()`'s, and `sub_operation_progress.py:116` is a third poll-loop. When the scheduler lands, extract a `run_periodic_background_loop(tick, *, interval, name)` helper and retrofit the sweeper onto it — deferred until the second concrete caller exists, not built speculatively now.
    - **Pre-migration safety:** if the `schedules` table is absent (rolling deploy: server up before migrations run), the loop logs a warning and defers, never crashes.

    **Spec**:
    - Add `_start_scheduler()` to the lifespan startup tasks, guarded by `settings.scheduler.enabled`; cancel on shutdown.
    - Catch the "missing table" SQLAlchemy error and back off rather than propagate.

    **Tests**:
    - (unit) Scheduler disabled via config → not started.
    - (unit) Missing `schedules` table → logs warning and continues looping (no crash).
    - (unit) Cancelled cleanly on shutdown.

    Effort: XS | Dependencies: Scheduler Service | Status: Not Started

- [ ] **CLI Schedule Commands**

    **Story**: The Curator lives in the terminal and wants to set up and inspect automation there first — the first end-to-end scheduling surface, before any web UI exists.

    **Decisions**:
    - Same use cases as the API via the `run_async()` bridge; the shared validators give identical errors as `typer.BadParameter`.
    - Timezone is a first-class flag (defaults to the system tz, falls back to UTC).

    **Spec**:
    - `mixd workflow schedule <id> --cron "0 6 * * 1" [--tz America/Los_Angeles]` / `--every 24h` / `--disable` / `--enable` / `--remove`.
    - `mixd sync schedule lastfm:plays --every 1h [--tz ...]` / `mixd sync schedule --list`.

    **Tests**:
    - (unit) Invalid cron / sub-min-interval / bad tz → `typer.BadParameter` (exit 2), no row written.
    - (integration) `schedule … --cron …` then `--list` shows the schedule with next-run in the chosen tz.

    Effort: S | Dependencies: Schedule CRUD Use Cases | Status: Not Started

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.2 accumulate here (`v0.8.2.N` tags)._

---

### v0.8.3: Workflow Scheduling - Web UI & Failure Alerts
**Goal**: Manage workflow schedules from the web UI, see next-run state at a glance, and get alerted when scheduled runs fail.

**Context**: The Curator's primary surface for automation, built on the v0.8.2 engine. The HTTP API (consumed by web + external clients) lands here alongside the `SchedulePicker` component and proactive failure notifications.

**What this unlocks**: Set "Weekly on Sunday at 6am" in a picker (in your own timezone), see the "Next run" column on the workflow list, and get a dashboard banner when an overnight run fails — instead of discovering stale data on Sunday evening.

**Persona**: Curator (primary), Casual (next-run visibility builds confidence).

- [ ] **Schedule Management API**

    **Story**: The web UI and any external client need HTTP access to schedule CRUD — and that access must be airtight per-user, since one user must never see or mutate another's schedules.

    **Decisions**:
    - **PUT for idempotent create-or-replace** — a cron expression has no meaningful partial update.
    - **User scoping everywhere.** All endpoints pass `user_id` from the JWT to use cases; `GET /api/v1/schedules` is user-scoped (NOT the cross-user `find_due_schedules`). Sync mutations filter by `(user_id, target_id)`.
    - **Validation enforced in the use case, not just here.** The path-param regex is defense-in-depth; the real guarantee is `SyncTarget` enum + cron + tz + config validation at the use-case layer (so the CLI can't bypass it). `croniter.is_valid(strict=True)` at the boundary returns 422 early for UX.

    **Spec**:
    - `PUT/GET/DELETE /api/v1/workflows/{workflow_id}/schedule`; `PUT/GET/DELETE /api/v1/sync/schedules/{target_id}`; `GET /api/v1/schedules` (all of caller's schedules).
    - `target_id` path param constrained to `[a-z0-9_:.-]+` (defense-in-depth; enum is the real check). `config` serialized-size ≤4 KB.
    - Thin handlers: parse → Command → `execute_use_case(lambda uow: ..., user_id=user_id)` → Response.

    **Tests**:
    - (integration) User A cannot read/modify/delete user B's workflow or sync schedule.
    - (integration) Invalid cron → 422 before DB; oversized config → 422.
    - (integration) PUT create returns 201, PUT replace returns 200.

    Effort: M | Dependencies: Schedule CRUD Use Cases (v0.8.2) | Status: Not Started

- [ ] **Workflow List/Detail Schedule Data**

    **Story**: The Curator wants to see "when does this run next?" on the workflow list without extra clicks, and the full schedule state on the detail page.

    **Decisions**:
    - Embed a schedule summary in existing workflow responses; batch-fetch alongside latest runs to avoid N+1.

    **Spec**:
    - Add `WorkflowScheduleSchema` (cron/interval, status, `next_run_at`, timezone) to `WorkflowSummarySchema` and `WorkflowDetailSchema`.
    - Batch-fetch schedules in `list_workflows`.

    **Tests**:
    - (integration) List response includes schedule summary; `next_run_at` rendered in the schedule's tz.
    - (integration) Workflows without a schedule return null summary (no error).

    Effort: S | Dependencies: Schedule Management API | Status: Not Started

- [ ] **Web UI: Schedule Picker**

    **Story**: Setting up automation is the Curator's primary reason to be here, so the picker must make the common rituals one click and always show — in the user's own timezone — exactly when the next runs will happen, so there's no UTC guessing game.

    **Decisions**:
    - Reusable component (the v0.8.4 sync settings reuse it). Cron presets ordered by Curator usefulness.
    - **Timezone-aware UX:** a timezone field defaulting to the browser's IANA zone; the next-3-fire-times preview renders in that zone, so "Sunday 6am" visibly means 6am *their* time.
    - Client-side cron validation before submit; the 422 is the backstop, not the primary UX.

    **Spec**:
    - `web/src/components/shared/SchedulePicker.tsx` — type selector, cron presets + freeform, interval input, timezone selector (default browser tz), human-readable preview, next-3 fire times in tz.
    - Presets: "Weekly on Sunday at 6am" (primary), "Every Monday at 6am", "Daily at midnight", "Every 6 hours", "Every hour".
    - Workflow detail: "Schedule" card below the pipeline strip (current schedule, toggle, edit). Workflow list: "Next run" column (relative time).

    **Tests**:
    - (component) Selecting a preset shows correct next-3 fire times in the chosen tz; changing tz updates them.
    - (component) Client-side invalid cron blocks submit.
    - (component) Toggle enable/disable round-trips.

    Effort: M | Dependencies: Schedule Management API, Workflow List/Detail Schedule Data | Status: Not Started

- [ ] **Schedule Failure Notifications**

    **Story**: The whole point of "sync overnight, rebuild in the morning" is that the Curator *doesn't* watch it — so when an overnight run fails, mixd must surface it proactively the next time they open the app, not make them check each schedule. Passive `last_error` fields aren't enough.

    **Decisions**:
    - In-app surfacing keyed on `consecutive_failures >= 1` (which resets to 0 on the next success, per v0.8.2 — so the banner clears itself when things recover).
    - **Email/push is explicitly deferred**, not "not needed" — the discovery-without-checking problem isn't fully closed until a channel reaches the user when the app is closed. Tracked as a future revision.

    **Spec**:
    - Dashboard banner: "2 scheduled runs failed — Last.fm sync (3h ago)" when any schedule has `consecutive_failures >= 1`.
    - Schedule detail: warning badge with last error + timestamp.
    - Workflow list: indicator on workflows with failing schedules.
    - In-app only for v0.8.x; email/push deferred to a later revision.

    **Tests**:
    - (component) Banner appears when a schedule has `consecutive_failures >= 1`; disappears after a success resets the counter.
    - (integration) `last_error` shown is the sanitized/truncated message, never raw token-bearing text.

    Effort: S | Dependencies: Workflow List/Detail Schedule Data | Status: Not Started

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.3 accumulate here (`v0.8.3.N` tags)._

---

### v0.8.4: Background Sync Scheduling
**Goal**: Schedule per-connector background data syncs from the Settings/Connectors page.

**Context**: Completes the compound capability — "sync source data overnight → run workflows in the morning." Reuses the v0.8.3 `SchedulePicker` and the sync schedule API endpoints; users configure sync intervals where they manage connector credentials.

**What this unlocks**: Last.fm play history, Spotify likes, and Last.fm likes export each refresh on their own schedule without manual triggers.

**Persona**: Curator (overnight freshness), Casual ("playlists stay fresh without effort").

- [ ] **Web UI: Sync Schedule Settings**

    **Story**: The Casual user wants their likes and play history to stay current without ever thinking about it, and the Curator wants source data fresh *before* the morning workflow run — both configured in the same place they already manage connector credentials.

    **Decisions**:
    - Reuse `SchedulePicker` (and its timezone awareness) inside each connector section; surface current defaults so users see what they get before customizing.

    **Spec**:
    - Per-connector section: last sync time (from `SyncCheckpoint`), schedule toggle + picker.
    - Surfaced defaults: Last.fm play history ("every 1 hour"), Spotify likes ("every 24 hours"), Last.fm likes export ("every 24 hours").

    **Tests**:
    - (component) Setting a sync schedule persists and shows next-run in the user's tz.
    - (component) Toggling off removes the schedule.

    Effort: S | Dependencies: Schedule Management API (v0.8.3), Web UI: Schedule Picker (v0.8.3) | Status: Not Started

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.4 accumulate here (`v0.8.4.N` tags)._

---

### v0.8.5: Workflow Templates & Import/Export
**Goal**: Give workflow creation curated starting points and portability.

**Context**: v0.4.3 (✅ Completed) delivered the visual editor. The template *infrastructure* — a file-backed template **gallery** (`GET /workflows/templates`, served from `definitions/*.json`), clone-on-use instantiation (`InstantiateWorkflowUseCase`), **Duplicate** on any workflow (`DuplicateWorkflowUseCase`, fetch+clone+save in one transaction), and the elimination of the read-only `is_template` kind so the `workflows` table holds only user-owned, editable workflows — was **built as a pre-cycle DRY consolidation** (migration `023`, code-complete 2026-05-30, pending ship). Cloning mints a fresh unique `definition.id` slug, so duplicates/template-instantiations never collide on the slug the CLI and personal seeder key on. What remains in this slice is the curated template *content* and the import/export path.

**What this unlocks**: Pick "Hidden Gems" and customize instead of building from scratch; export a workflow as JSON to back up or share; import one from a file or URL.

**Persona**: Tinkerer (templates as onboarding, sharing between instances), Curator (faster ritual setup).

- [ ] **Workflow Templates (curate the gallery content)**

    **Story**: A new Tinkerer staring at a blank editor doesn't yet know the node catalog — they want to start from a working example that already expresses a good idea ("Hidden Gems"), see how it's built, and tweak it. The gallery + clone-on-use plumbing already exists (pre-cycle refactor); this story curates the *content* that fills it.

    **Decisions**:
    - Templates are **user-facing starting points** — curated empty-state inspiration and copy-and-modify seeds. They stand on their own user value. _(They are explicitly NOT the LLM's training data; v0.9.0 generates workflows via schema-constrained tool use, where the WorkflowDef JSON Schema is the prior. v0.8.5 and v0.9.0 are independent — see v0.9.x Key Design Decisions.)_
    - Templates are **file-backed `WorkflowDef` JSON** under `definitions/*.json`, served by the gallery and instantiated on use into a new user-owned, editable workflow — **not** seeded `workflows` rows, and **not** a read-only kind (that plumbing shipped in the pre-cycle refactor). Each carries a description explaining its curation logic. Some use scheduled refresh — pairs with v0.8.2+ scheduling.

    **Spec**:
    - 5–8 curated definitions: "Current Obsessions" (liked + 8+ plays last 30d → top 20), "Hidden Gems" (liked + 3+ plays, untouched 6mo), "Forgotten Favorites" (liked + 10+ plays all-time, 0 plays last year), "Recent Discovery Mix" (first played last 30d, 3+ plays), "Decade Deep Cuts" (liked, released 2010–2019, <5 plays), etc.
    - The "New Workflow" flow surfaces the gallery (✅ built) — this story populates it. Blank / from-template / from-import paths.

    **Tests**:
    - (unit) Every bundled template passes `validate_workflow_def`.
    - (component) Selecting a gallery template instantiates and opens an editable workflow.

    Effort: S | Dependencies: v0.4.3 (✅ Visual Editor) | Status: Not Started

- [ ] **Workflow Import/Export**

    **Story**: The Tinkerer wants to back up a workflow they're proud of, move it to another mixd instance, or share it — and to import someone else's safely.

    **Decisions**:
    - Export downloads the `WorkflowDef` as `.json`. Import validates via the existing `validate_workflow_def` before creating.
    - **URL sharing is a client-side decode of an in-URL definition** (the WorkflowDef JSON is encoded *in* the query param for small workflows), not a server-side fetch of a user-supplied URL. _(If a server-side fetch is ever added, it must use an allowlist and block private CIDRs — SSRF guard. Not in scope here.)_

    **Spec**:
    - Export: download `WorkflowDef` as `.json`.
    - Import: upload `.json`, validate (reuse `validate_workflow_def`), create new workflow.
    - Share via URL: encoded definition in query param (small workflows), decoded client-side.

    **Tests**:
    - (integration) Importing an invalid `WorkflowDef` is rejected with a clear error; a valid one creates a workflow.
    - (component) Export → re-import round-trips to an equivalent workflow.

    Effort: S | Dependencies: v0.4.3 (✅ Visual Editor) | Status: Not Started

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.5 accumulate here (`v0.8.5.N` tags)._

---

### v0.8.6: Editor Polish - Sub-Flows & Playlist Browse
**Goal**: Reduce friction in building large workflows and in linking playlists.

**Context**: The two remaining editor-friction reducers. Sub-flows tame 10+ node workflows; browse-to-link removes the paste-an-ID power-user pattern. Depends on the v0.4.3 editor (✅) and v0.4.4 connector linking (✅).

**What this unlocks**: Collapse related nodes into named, reusable groups; pick a Spotify playlist to link from a searchable list instead of pasting its ID.

**Persona**: Tinkerer (sub-flows for complex pipelines), Casual (browse-to-link removes the only ID-knowledge requirement).

- [ ] **Sub-Flows & Node Grouping**

    **Story**: As a Tinkerer's workflow grows past ~10 nodes it becomes hard to navigate; they want to collapse related nodes into a named group they can fold away — and reuse as a snippet.

    **Decisions**:
    - Use React Flow's `parentId` + `extent: 'parent'`; groups are collapsible, nameable, reusable as "snippets".

    **Spec**:
    - Labeled group nodes as collapsible containers; expand/collapse toggle.

    **Tests**:
    - (component) Grouping nodes, collapsing/expanding, and naming a group all work; a named group can be reused.

    Effort: M | Dependencies: v0.4.3 (✅ Visual Editor) | Status: Not Started

- [ ] **Browse/Search Playlists from Connector**

    **Story**: Most users don't know their playlist IDs — paste-ID-to-link is a power-user pattern that creates friction for everyone else. They want to browse and search their *own* playlists and click to link. This is the most broadly user-facing friction fix in the cycle: it touches everyone who links a playlist.

    **Decisions**:
    - New paginated `list_user_playlists()` connector method (name + ID + track count); browse UI replaces/supplements paste-ID.

    **Spec**:
    - Spotify: `GET /me/playlists` (OAuth scope already held). Apple Music: `GET /me/library/playlists` (when that connector ships in v1.0.1).
    - Browse UI: searchable/filterable list, click to select.

    **Tests**:
    - (integration) `list_user_playlists()` paginates correctly.
    - (component) Browse → search → select links the chosen playlist.

    Effort: M | Dependencies: v0.4.4 (✅ Connector Playlist Linking) | Status: Not Started

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.6 accumulate here (`v0.8.6.N` tags)._

---

### v0.8.7: Reserved Capacity (Iteration & Polish)
**Goal**: _Reserved slot_ — capacity held for iteration and polish surfaced while prod-testing v0.8.0–v0.8.6.

**Context**: This is a deliberate capacity reservation, not a planned story. Likely candidates: scheduling UX refinements, additional templates, editor ergonomics, or a small feature that doesn't warrant its own minor series (e.g. email/push for failure alerts, deferred from v0.8.3). Scope is defined from prod feedback before this version is locked; until then it carries no stories.

_(No stories — capacity reservation. Populate from v0.8.x prod feedback before locking.)_

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.7 accumulate here (`v0.8.7.N` tags)._
