# v0.5.x: CI/CD, PostgreSQL, PostgreSQL Polish, Deployment & OAuth

For strategic overview, see the [planning overview](README.md).

---

### v0.5.0: CI/CD & Environment Hardening
**Goal**: Automated safety net for testing, linting, and type checking — plus env config audit to prepare for containerization.

**Context**: Every subsequent milestone in v0.5.x involves irreversible infrastructure changes (database migration, deployment, OAuth). CI/CD must land first so regressions are caught before merge. Environment hardening has no dependencies and naturally pairs here.

**What this unlocks**:
- **Regression protection**: Automated test + quality gates before the PostgreSQL migration
- **Contributor confidence**: PR checks catch issues before review
- **Container-ready config**: All settings driven by env vars with fail-fast validation

#### CI/CD Pipeline Epic

- [x] **Unified CI Workflow**
    - Effort: S
    - What: Single `.github/workflows/ci.yml` with two parallel jobs (python, web)
    - Why: Catch regressions before merge — critical before the irreversible PostgreSQL migration and production deployment
    - Dependencies: None
    - Status: Completed
    - Notes:
        - **Single workflow, two parallel jobs** — right-sized for a hobby project; separate workflows add checkout+install overhead that exceeds the parallelism gain for a ~32s test suite
        - **`python` job** (timeout 10min):
            - `astral-sh/setup-uv@v7` (caching ON by default, handles Python install from `.python-version` — no `actions/setup-python` needed)
            - `uv sync --frozen` (fails if lockfile stale)
            - `uv run ruff check .` + `uv run ruff format --check .` (lint before test — fast checks fail first)
            - `uv run basedpyright src/`
            - `uv run pytest -m "" --cov --cov-report=term-missing --cov-fail-under=80`
        - **`web` job** (timeout 10min):
            - `pnpm/action-setup@v4` + `actions/setup-node@v4` (cache: pnpm)
            - `pnpm install --frozen-lockfile`
            - `pnpm check` (Biome lint + tsc) + `pnpm test` (Vitest) + `pnpm build`
        - Trigger: push to main, PR to main
        - Concurrency: cancel-in-progress on PRs
        - **No Codecov** — `--cov-fail-under=80` is sufficient for hobby project
        - **No bandit** — ruff's `S` rules already cover flake8-bandit equivalents
        - **No Playwright E2E** — defer to v0.5.3 (needs API server + Vite — complex without Docker)
        - **Coverage at 80%** — no baseline exists yet; start here, tighten after measuring

- [x] **Automated Changelog Generation**
    - Effort: XS
    - What: git-cliff config + release workflow for automated GitHub Release notes on tag push
    - Why: Clear release notes, semantic versioning
    - Dependencies: None
    - Status: Completed
    - Notes:
        - **git-cliff v2.12** — single Rust binary, no Node.js toolchain; lighter than semantic-release (npm publishing), release-please (self-approve PRs), or standard-version (deprecated)
        - `cliff.toml` config: group by type (feat→Features, fix→Bug Fixes, etc.), skip submodule noise
        - `.github/workflows/release.yml`: trigger on `v*` tags, `orhun/git-cliff-action@v4` → `softprops/action-gh-release@v2`
        - Generate initial `CHANGELOG.md` from full git history
        - **No commitlint in CI** — solo dev already uses conventional commits; a pre-commit hook is more effective if enforcement is ever needed

#### Environment Hardening Epic

- [x] **Environment Configuration Hardening**
    - Effort: S
    - What: Consolidate env var reads, make server config flexible, add startup validation
    - Why: Scattered hardcoded paths break in containers; inconsistent config access patterns
    - Dependencies: None
    - Status: Completed
    - Notes:
        - **`get_database_url()` helper** in `settings.py` — consolidate 3 places that read `DATABASE_URL` (`db_connection.py`, `completions.py`, settings) into one function. Reads `os.environ` first (test fixture compat), falls back to `settings.database.url`
        - **Server host/port** — extend `ServerConfig` with `host` + `port` fields, replace hardcoded values in `run_server()`, add flat env routes
        - **Startup credential warnings** — `log_startup_warnings()` method on `Settings`, called from API lifespan and CLI entry point. Warns about unconfigured Spotify/Last.fm credentials
        - **Update `.env.example`** — add server section (`SERVER__HOST`, `SERVER__PORT`)

---

### v0.5.1: PostgreSQL Migration
**Goal**: Swap SQLite for PostgreSQL — driver, schema, and query compatibility.

**Context**: The Repository + UoW pattern already fully abstracts database access — only connection config, driver, and a handful of SQLite-specific SQL constructs change. This was deferred from v0.3.0 because the web UI works identically on SQLite for local development.

**What this unlocks**:
- **Remote hosting**: PostgreSQL connection pool handles concurrent web requests
- **MVCC concurrency**: Foundation for Prefect parallel execution (v0.5.5)
- **JSONB columns**: `@>` containment queries and GIN indexes for workflow definitions

#### Database Migration Epic

- [x] **Evaluate and Select Database**
    - Effort: S
    - What: Document ADR for PostgreSQL + psycopg3 selection
    - Why: Commit to one technology before migration
    - Dependencies: None
    - Status: Completed
    - Notes:
        - **Selected**: PostgreSQL via `psycopg3` (SQLAlchemy 2.1 default, Jan 2026)
        - psycopg3 handles both sync (Alembic) and async (app) from single `psycopg[binary]` package
        - SQLite removed entirely — PostgreSQL-only, no dual-dialect branching
        - **Managed hosting**: Neon free tier (100 CU-hrs/month, 0.5 GB, scale-to-zero)

- [x] **Driver and Connection Config**
    - Effort: S
    - What: Swap `aiosqlite` → `psycopg[binary]`, update engine/session config, remove SQLite PRAGMAs
    - Why: Core connectivity change
    - Dependencies: Database Selection
    - Status: Completed
    - Notes:
        - `db_connection.py`: removed all SQLite code (PRAGMAs, NullPool, connect_args, event listener)
        - URL scheme: `postgresql+psycopg_async://`
        - `alembic/env.py`: removed `render_as_batch=True`, added DATABASE_URL env var override
        - Docker Compose for local PG dev (`docker-compose.yml`)

- [x] **Schema and Query Compatibility**
    - Effort: M
    - What: Migrate SQLite-specific SQL constructs to PostgreSQL
    - Why: Bulk upsert dialect, JSON → JSONB, partial indexes
    - Dependencies: Driver and Connection Config
    - Status: Completed
    - Notes:
        - `sqlite_insert()` → `pg_insert()` in `base_repo.py` and `metrics.py`
        - `json_extract()` → JSONB subscript, `group_concat()` → `string_agg()` in `track/core.py`
        - `sqlite_where` → `postgresql_where` on partial index in `db_models.py`
        - Fresh `initial_schema` migration; SQLite chain archived to `versions_sqlite_archive/`
        - Test fixtures: testcontainers-python with nested transaction (savepoint rollback) per test

#### PostgreSQL Optimization Epic (post-migration cleanup)

- [x] **PostgreSQL Optimization Audit**
    - Effort: M
    - What: Replace remaining SQLite-tuned patterns with PostgreSQL-native equivalents
    - Why: The initial migration swapped the driver and fixed SQL dialect issues, but left SQLite-era query patterns: in-memory aggregations, expression tree batching, generic JSON, ILIKE without index support, N+1 query loops
    - Dependencies: Schema and Query Compatibility
    - Status: Completed
    - Notes:
        - **Phase 1 — Query fixes** (`plays.py`):
            - `get_play_aggregations()`: DB-side `GROUP BY` + `COUNT/MIN/MAX` replaces loading all plays into Python defaultdict (50-100x faster, 99% less memory)
            - `total_plays_desc` sort: CTE + window function replaces N+1 loop (51 queries → 1)
            - `_find_existing_plays()`: `tuple_.in_()` replaces batched OR chains with SQLite expression tree limit comments (chunks of 200 → single query)
            - `find_plays_in_time_range()`: Removed chunked batching
            - Same patterns applied to `play/connector.py`
        - **Phase 2 — JSON → JSONB** (`db_models.py`, migration):
            - All 15 JSON columns → JSONB (25% smaller, GIN-indexable)
            - `source_services` JSON → `ARRAY(String())` for native array semantics
            - GIN index on `tracks.artists` with `jsonb_path_ops`
        - **Phase 3 — Search** (`core.py`, `db_models.py`, migration):
            - `pg_trgm` extension + GIN trigram indexes on `title`, `album`, `artists_text`
            - New `artists_text` denormalized column replaces `cast(artists, String).ilike()` — populated in `save_track()` and via migration
        - **Phase 4 — Indexing** (migration):
            - BRIN index on `track_plays.played_at` (99% smaller than B-tree for time-series)
            - Status indexes on `workflow_runs.status`, `playlist_mappings.sync_status`
            - Removed unnecessary `VARCHAR(N)` constraints on high-traffic columns
        - **Phase 5 — Docs**: `database.md` rewritten — removed all SQLite sections (WAL, NullPool, pragmas), added PostgreSQL patterns (JSONB, pg_trgm, BRIN, connection pooling)
        - Migration: `002_pg_opt` with full downgrade support

- [x] **Keyset Pagination**
    - Effort: M
    - What: Replace offset-based pagination with cursor-based keyset pagination on `list_tracks`
    - Why: Offset pagination degrades at scale (`OFFSET 10000` scans and discards 10k rows). Keyset pagination uses a `WHERE (sort_col, id) > (:value, :last_id)` pattern that's O(1) regardless of page depth — critical for the 15k+ track library
    - Dependencies: PostgreSQL Optimization Audit
    - Status: Completed
    - Notes:
        - **Hybrid cursor+offset**: API accepts both `cursor` and `offset` params — cursor takes precedence when present, offset fallback for page jumps and deep links
        - **Scoped to `list_tracks`** only — playlists, workflows, match reviews use in-memory or low-volume pagination where offset is fine
        - **Page-based UI preserved**: kept `TablePagination` page numbers (better for Weekly Curator browsing). Frontend caches cursors in `useRef<Map>` for sequential next-page navigation, falls back to offset for random page jumps
        - **Cursor encoding**: opaque base64 JSON of `(sort_column, sort_value, id)` — column name embedded to reject stale cursors on sort change
        - **Architecture**: cursor encode/decode in `src/application/pagination.py` (application layer); repository accepts raw `after_value`/`after_id` primitives (respects dependency rule — infrastructure doesn't import application)
        - **`PaginatedResponse`**: added `next_cursor: str | None = None` (backward compatible default)
        - **Row-value comparison**: `tuple_(col, DBTrack.id) > tuple_(literal(value), literal(id))` — PostgreSQL natively supports `(a, b) > (x, y)`
        - **Secondary ID tiebreaker**: ORDER BY now includes `DBTrack.id` as second sort key for stable ordering across pages
        - **Composite indexes**: migration `003_keyset_idx` adds `(title, id)`, `(created_at, id)`, `(artists_text, id)`
        - **Orval types regenerated**: `cursor` query param and `next_cursor` response field auto-generated

- [x] **Track Merge CTE Refactor**
    - Effort: S
    - What: Consolidate the multi-statement track merge logic (`move_references_to_track`, `merge_mappings_to_track`, `merge_metrics_to_track`) into PostgreSQL CTEs
    - Why: Current approach used multiple sequential raw SQL statements with Python loops for conflict resolution. CTE chains provide single atomic operations with clearer declarative logic
    - Dependencies: PostgreSQL Optimization Audit
    - Status: Completed
    - Notes:
        - **`move_references_to_track`**: 3 sequential statements + `_merge_track_likes` (6 potential SQL ops in Python loop) → 1 CTE chain with 6 operations. `_merge_track_likes` private method deleted.
        - **`merge_mappings_to_track`**: raw SQL + Python loop with if/else branching → 1 CTE with 2 SQL-level branches (`WHERE loser_ct_id = winner_ct_id` vs `!=`). Same-ext-ID: keep higher confidence, delete loser. Different-ext-ID: keep both, winner=primary, loser=secondary.
        - **`merge_metrics_to_track`**: raw SQL + Python loop + `isinstance()` defensive type checks → 1 CTE. Eliminates string-to-datetime coercion hacks (PostgreSQL handles timestamps natively).
        - **CTE snapshot semantics**: explicit `AND id NOT IN (SELECT loser_id FROM [read-only conflict CTE])` prevents double-operations from PostgreSQL's same-snapshot data-modifying CTE behavior
        - **Net result**: ~170 lines of Python loop logic → ~100 lines of SQL CTEs. Method signatures unchanged. Service orchestrator unchanged. All existing integration tests pass without modification.

- [x] **SQLAlchemy & PostgreSQL Best Practices Audit (March 2026)**
    - Effort: S
    - What: Research current SQLAlchemy 2.x and PostgreSQL best practices (as of March 2026) and apply any relevant patterns to the narada persistence layer
    - Why: The codebase was built over many months — newer SQLAlchemy features, PostgreSQL patterns, or psycopg3 capabilities may simplify code, improve performance, or increase robustness. Final sweep before calling v0.5.1 complete.
    - Status: Completed
    - Dependencies: Keyset Pagination, Track Merge CTE Refactor
    - Status: Not Started
    - Notes:
        - Web research: SQLAlchemy 2.1 changelog, psycopg3 best practices, PostgreSQL 17 features
        - Areas to audit: connection pool tuning, session lifecycle patterns, bulk operation APIs, type annotation improvements, async best practices
        - DRY focus: identify any remaining boilerplate in repository layer that newer APIs could eliminate
        - Performance: review `EXPLAIN ANALYZE` on critical queries, check for missing index opportunities
        - Robustness: connection retry, health checks, graceful degradation patterns
        - Goal is to close gaps, not introduce new features — keep scope tight
    - **Known issues from keyset pagination + CTE work** (investigate during audit):
        - **Duplicate sort-column mapping**: `_SORT_MAP` in `track/core.py` and `_SORT_BY_TO_COLUMN` in `list_tracks.py` duplicate the same sort-key→column knowledge. Consolidate into a single source of truth. Related: `sort_by` flows as a raw `str` through four layers (API route → use case → interface → repository). A `Literal` union (like `PlaySortBy` already does for plays) would catch invalid sort keys at type-check time instead of silently falling through to the default.
        - **`list_tracks` parameter sprawl**: Signature grew to 8 parameters + 4-element return tuple `(list[Track], int, set[int], tuple[Any, int] | None)` from keyset pagination additions (`after_value`, `after_id`). The opaque return tuple requires `_, _, _, _` destructuring at every call site. Consider a typed `@define` pagination params object and a `ListTracksResult` at the repository boundary (one already exists in the use case layer).
        - **Count query on keyset pages**: `list_tracks` always runs `SELECT count(*)` before the data query. For cursor-paginated requests (page 2+), the frontend already has the total from page 1 — the count query is redundant. With a 15k+ track library and trigram ILIKE filters, the count can be the most expensive part of the request. Consider an `include_total` parameter the use case sets to `False` when a cursor is present.
        - **`text()` result typing**: CTE `fetchone()` results require `# type: ignore[index]` (6 occurrences in `track/core.py`); `fetchall()` with `row[0]` indexing appears 10+ times across `connector.py`, `plays.py`, `likes.py`. Check if SQLAlchemy 2.1 `Row` typing or named tuples eliminate these suppressions.
        - **ORDER BY missing ID tiebreaker**: `list_tracks` was fixed, but `match_review.py:74-82` (confidence/created_at sort) and `workflow/runs.py:161` (created_at sort) still lack a secondary ID sort key — unstable ordering for rows with equal values.
        - **EXPLAIN ANALYZE on keyset queries**: Verify the new composite indexes `(title, id)`, `(created_at, id)`, `(artists_text, id)` from migration `003_keyset_idx` are actually used by the keyset WHERE clause query plans.
        - **Document CTE snapshot pattern**: Add the `AND id NOT IN (SELECT ... FROM read_only_cte)` pattern for data-modifying CTEs to `database.md` — prevents the same-snapshot double-operation pitfall in future CTEs.
    - **DRY issues from code review** (investigate during audit):
        - ~~**Duplicate batch-tuple-IN pattern**~~: Resolved in v0.5.2 — replaced entire SELECT-filter-INSERT pattern with `ON CONFLICT DO NOTHING`, eliminating both callers and the `batch_lookup.py` helper.
        - **`artists_display` property on Track**: The pattern `", ".join(a.name for a in track.artists)` appears inline in 7+ locations across domain, application, infrastructure, and interface layers (`diff_engine.py`, `track_commands.py`, `workflow_runs.py`, `playlist_results.py`, `track/core.py`). Add a `@property` on the `Track` entity to provide a single source of truth.

---

### v0.5.2: PostgreSQL-Native Feature Adoption
**Goal**: Stop "thinking in SQLite" — use PostgreSQL-native features to simplify code, add operational safety, and consolidate queries.

**Context**: The v0.5.1 migration and optimization audit are complete. But a few places still do work in Python that PostgreSQL can do natively, or miss defensive configuration that production PostgreSQL needs. Focused polish pass, not a new feature set.

**What this unlocks**:
- **Simpler code**: Play dedup replaced SELECT-filter-INSERT with a single `ON CONFLICT DO NOTHING`
- **Operational safety**: `statement_timeout` and `lock_timeout` prevent runaway queries and deadlocks; CHECK constraints guard data integrity at the database level
- **Faster dashboard**: 5 queries instead of 8 for stats aggregation

#### PostgreSQL-Native Polish Epic

- [x] **Engine Timeouts: statement_timeout + lock_timeout**
    - Effort: XS
    - What: Add `connect_args` with `statement_timeout=30000` (30s) and `lock_timeout=10000` (10s)
    - Why: No query timeout configured — runaway queries block the pool indefinitely; lock contention waits forever
    - Status: Completed
    - Notes:
        - Per-connection defaults via `-c` options in `connect_args`
        - Individual statements can override via `SET LOCAL` (e.g., migrations)

- [x] **Play Dedup: ON CONFLICT DO NOTHING**
    - Effort: S
    - What: Replace 3-step SELECT-filter-INSERT with single `INSERT ... ON CONFLICT DO NOTHING`
    - Why: The unique constraints `uq_track_plays_deduplication` and `uq_connector_plays_deduplication` already prevent duplicates — the pre-query was redundant
    - Status: Completed
    - Notes:
        - Added `bulk_insert_ignore_conflicts()` to `BaseRepository` — single INSERT with `ON CONFLICT DO NOTHING`, returns `rowcount`
        - Rewrote `bulk_insert_plays()` and `bulk_insert_connector_plays()` — deleted `_find_existing_plays()`, `_filter_duplicates()`, and equivalents
        - Deleted `_shared/batch_lookup.py` (no remaining callers)
        - Resolves v0.5.1 audit item "Duplicate batch-tuple-IN pattern"

- [x] **Dashboard Stats: Single-Query Aggregation**
    - Effort: S
    - What: Replace 8 sequential COUNT queries across 6 repos with 5 efficient queries in a dedicated `StatsRepository`
    - Why: 8 round trips per dashboard load; PostgreSQL sub-selects compute all scalar counts in one statement
    - Status: Completed
    - Notes:
        - New `StatsRepositoryProtocol` + `StatsRepository` — cross-table read-only aggregation
        - Query 1: scalar sub-selects for totals. Queries 2–5: GROUP BY for service/connector breakdowns (not parallelizable — AsyncSession is single-query)
        - `GetDashboardStatsUseCase` simplified to single `get_dashboard_aggregates()` call
        - Not a materialized view — refresh complexity not worth it at hobby scale

- [x] **CHECK Constraints: Database-Level Business Rules**
    - Effort: XS
    - What: Add CHECK constraints for `confidence` (0–100) and `match_weight` (0.0–1.0)
    - Why: Python validates first, but database is the last line of defense against silent corruption
    - Status: Completed
    - Notes:
        - Migration `004_check_constraints`: `chk_mapping_confidence_range`, `chk_review_confidence_range`, `chk_review_match_weight_range`
        - Applied to both `track_mappings` and `match_reviews` tables

#### Evaluated and Rejected

| Feature | Why Not |
|---------|---------|
| Full-text search (tsvector) | pg_trgm ILIKE sufficient for 15k library substring matching |
| Materialized views for stats | Refresh complexity > benefit at hobby scale |
| Generated columns | Python's NFKD normalization not replicable in PL/pgSQL |
| LISTEN/NOTIFY | Already using asyncio.Queue push model for SSE |
| Advisory locks | Prefect handles workflow concurrency; advisory locks for v1.0+ multi-instance |
| COPY protocol | Only beneficial above ~100k rows/batch; typical imports 1k–10k |

---

### v0.5.3: Containerization & Deployment
**Goal**: Containerize narada, deploy to Fly.io, and get running with a fresh database.

**Context**: With PostgreSQL in place (v0.5.1–v0.5.2) and CI/CD catching regressions (v0.5.0), narada is ready to go live. Docker provides reproducible builds; Fly.io provides managed hosting. Still in dev mode — likes, history, and play data can be rebuilt from Spotify/Last.fm imports. Only workflows need migrating, and existing CLI export/import tooling handles that.

**What this unlocks**:
- **Narada goes live**: The hosted instance is accessible from any device via the web UI
- **Data durability**: Managed PostgreSQL with backups
- **Deployment path**: Reproducible Docker builds

#### Containerization Epic

- [x] **Dockerfile and Docker Compose**
    - Effort: S
    - What: Multi-stage Dockerfile (Python builder + Vite builder + runtime) + docker-compose.yml
    - Why: Reproducible, portable build
    - Dependencies: v0.5.1 (PostgreSQL migration)
    - Status: Completed
    - Notes:
        - Stage 1 (`python-builder`): `ghcr.io/astral-sh/uv:0.10-python3.14-trixie-slim` + `UV_COMPILE_BYTECODE=1` + `UV_LINK_MODE=copy`; deps-only install (`--no-install-project`) — source copied in runtime stage
        - Stage 2 (`node-builder`): Node 22 + `npm install -g pnpm@10`
        - Stage 3 (`runtime`): `python:3.14-slim-trixie`; non-root `narada` user; `COPY --chown`; OCI labels; `HEALTHCHECK` via Python stdlib urllib; log file redirected to `/app/data/`
        - BuildKit cache mounts for uv and pnpm stores
        - docker-compose.yml: `app` service (profile `full`) + `postgres` service
        - Default `docker compose up` still starts only PostgreSQL for `pnpm dev` workflow
        - Entrypoint: `alembic upgrade head && exec uvicorn ...`
        - Image size: **395MB** (42% reduction from initial 681MB — achieved by deps-only venv, eliminating hatchling build toolchain from image)
    - **Additional changes**:
        - Standardized DATABASE_URL on `postgresql+psycopg://` (works for both sync and async with psycopg3)
        - `_normalize_database_url()` in `settings.py` and `alembic/env.py` handles Fly.io's `postgres://` and legacy `+psycopg_async://` URLs
        - Committed alembic files to git (were previously gitignored)
        - Fixed pre-existing TypeScript errors in `MergeTrackDialog.test.tsx` and `workflow-layout.ts`
        - `src/__init__.py`: reads version directly from `pyproject.toml` via `tomllib` (no package metadata dependency)

- [x] **Fly.io Deployment**
    - Effort: M
    - What: Deploy to Fly.io with Neon PostgreSQL
    - Why: Primary hosting target; scale-to-zero for near-$0 idle costs
    - Dependencies: Dockerfile and Docker Compose
    - Status: Completed
    - Notes:
        - `fly.toml`: port 8000, health check on `/api/v1/health`, scale-to-zero
        - Neon PostgreSQL (not Fly.io managed — Fly's MPG is $38/month, unmanaged is DIY)
        - `fly secrets set` for DATABASE_URL (from Neon) + API credentials
        - `alembic upgrade head` as release command (runs in temporary VM before traffic swap)
        - HTTPS automatic via Fly.io
        - VM: shared-cpu-1x, 512MB RAM
    - **Deployment findings**:
        - **Neon pooler rejects startup parameters**: `connect_args` with `-c statement_timeout=...` causes `OperationalError` because Neon's PgBouncer doesn't support startup parameters. Fixed by using SQLAlchemy `pool_events` to execute `SET` commands post-connect instead.
        - **Pydantic Settings v2 flat env var routing**: OS env vars (Fly.io secrets) bypass the `model_validator(mode="before")` — they're matched directly by Pydantic's field-matching logic. Fixed by reading `os.environ` in the model_validator for flat keys not in the data dict. JSON strings (e.g., `CORS_ORIGINS='["url"]'`) parsed with `json.loads`.
        - **Cold start ~7.5s**: Python imports (~5s) + lifespan (engine + template seed ~2s). Fly proxy connects successfully within this window. `pool_pre_ping=True` handles Neon scale-to-zero reconnection.
        - **Neon project**: `delicate-hill-47205215`, us-west-2, PG 17, pooler endpoint (built-in PgBouncer)
        - **Image size**: 109 MB compressed (Depot builder cache hit on deps layers)
        - **Template seeding**: works on first deploy — templates auto-seeded during lifespan
        - **Version**: ~~showed `0.0.0-dev`~~ — fixed by switching to `tomllib` (see below)

- [x] **Single-Source Version via tomllib**
    - Effort: XS
    - What: Replace `importlib.metadata.version()` with direct `tomllib` read of `pyproject.toml`
    - Why: `importlib.metadata` requires installed package metadata — the container's deps-only venv (`--no-install-project`) doesn't have it, so version showed `0.0.0-dev`. Also eliminates the `uv sync` prerequisite before `pnpm --prefix web sync-api`.
    - Dependencies: Fly.io Deployment (discovered during deploy)
    - Status: Completed
    - Notes:
        - `src/__init__._read_version()` uses `tomllib` (stdlib 3.11+) to read `pyproject.toml` directly
        - `pyproject.toml` COPY'd to Dockerfile runtime stage (~12KB)
        - CLI version consumers (`cli/app.py`, `cli/workflow_commands.py`) centralized through `src.__version__`
        - Version bump is now one step: edit `pyproject.toml`, run `pnpm --prefix web sync-api`
        - No fallback — if `pyproject.toml` is missing, `FileNotFoundError` surfaces immediately

- [x] **Actionable Infrastructure Error Reporting**
    - Effort: S
    - What: Classify database errors (connection refused, DNS failure, auth, timeout, cold start) into actionable user-facing messages
    - Why: With Neon, Docker, and Fly.io as new dependencies, generic "Database connection unavailable" messages don't help users diagnose failures. CLI showed raw 50-line SQLAlchemy tracebacks.
    - Dependencies: Fly.io Deployment (discovered during deploy)
    - Status: Completed
    - Notes:
        - New `error_classification.py` module: pure function `classify_database_error()` walks exception `__cause__` chain, returns `DatabaseErrorInfo(category, user_message, detail)`
        - Categories: `connection_refused`, `dns_failure`, `auth_failure`, `ssl_error`, `timeout`, `pool_exhaustion`, `cold_start`, `unknown`
        - Three consumers: API middleware (JSON envelope), health endpoint (logs + `database_error` field), CLI (`handle_cli_error()` + `main()` catch-all)
        - Health endpoint now logs classified errors (was silently swallowing `except Exception`)
        - Serverless cold-start detection is vendor-neutral (matches error text patterns, not hostnames)

- [x] **Workflow Migration from SQLite**
    - Effort: XS
    - What: One-time script to extract workflow definitions from the old SQLite database into JSON files compatible with `narada workflow create --file`
    - Why: Workflows are the only data worth preserving — likes, history, and plays can be rebuilt from Spotify/Last.fm imports
    - Dependencies: None (can run before deployment)
    - Status: Completed
    - Notes:
        - `scripts/export_sqlite_workflows.py` reads `workflows.definition` from old SQLite, writes `{slug}.json` per workflow
        - Import into new instance: `narada workflow create --file <path>` (existing CLI tooling)
        - Export CLI (`narada workflow export --all`) also works if old instance is still runnable
        - Templates auto-seed on startup — only user-created workflows need migration

- [x] **Deployment Documentation**
    - Effort: XS
    - What: Single `DEPLOYMENT.md` covering local Docker dev, Fly.io deploy, updates, backups
    - Why: Reproducible deploys; reference for months-later return
    - Dependencies: Fly.io Deployment
    - Status: Completed
    - Notes:
        - Covers: prerequisites, local Docker dev, Neon setup, Fly.io setup, deploying updates, workflow migration, backups, monitoring, known limitations, cost breakdown

---

### v0.5.4: OAuth & Credentials
**Goal**: Persist OAuth tokens in the database, implement web OAuth flows for Spotify and Last.fm, and build an interactive Settings/Integrations page where users connect and disconnect services entirely through the browser.

**Context**: The CLI uses `webbrowser.open()` for Spotify OAuth and stores tokens in a `.spotify_cache` file. Neither works in a headless container. The Settings page was read-only with CLI auth commands. This milestone adds database-backed token storage, web OAuth callback routes for both services, and a full interactive auth UX.

**What this unlocks**:
- **Headless auth**: Spotify + Last.fm OAuth work without a local browser
- **Token durability**: Tokens survive container restarts
- **Browser-first onboarding**: Users connect services via the web UI — no CLI required
- **Disconnect flow**: Users can revoke service connections from the browser

#### OAuth Epic

- [x] **Token Persistence**
    - Effort: S
    - What: Move tokens from cache files to `oauth_tokens` database table
    - Why: Containers have no persistent filesystem; tokens must survive restarts
    - Dependencies: v0.5.1 (PostgreSQL migration)
    - Status: Completed (2026-03-18)
    - Notes:
        - `oauth_tokens` table: `service` (PK), `token_type`, `access_token`, `refresh_token`, `session_key`, `expires_at`, `scope`, `account_name`, `extra_data` (JSONB), timestamps
        - `TokenStorage` protocol in `_shared/token_storage.py`: `load_token`, `save_token`, `delete_token`
        - `DatabaseTokenStorage` in `persistence/repositories/token_storage.py`: standalone sessions (not UoW), upsert via `pg_insert().on_conflict_do_update()`
        - `FileTokenStorage` retained for CLI development
        - `get_token_storage()` factory always returns `DatabaseTokenStorage` (PostgreSQL-only since v0.5.1)
        - Migration `005_oauth_tokens`

- [x] **Spotify OAuth Web Flow**
    - Effort: M
    - What: Web OAuth flow for Spotify — auth URL generation, callback handling, token exchange
    - Why: `SpotifyTokenManager._run_browser_auth()` uses `webbrowser.open()` — non-functional headless
    - Dependencies: Token Persistence, v0.5.3 (deployed callback URL)
    - Status: Completed (2026-03-18)
    - Notes:
        - `GET /api/v1/connectors/spotify/auth-url`: generates OAuth URL with CSRF state token
        - `GET /auth/spotify/callback`: exchanges code for tokens, fetches display name from `/v1/me`, stores via `DatabaseTokenStorage`, redirects to `/settings/integrations?auth=spotify&status=success|error`
        - In-memory CSRF state store with 5-minute TTL (single-user, single-worker)
        - Config: `SPOTIFY_REDIRECT_URI` env var (localhost for dev, Fly.io hostname for production)

- [x] **Last.fm OAuth Web Flow**
    - Effort: S
    - What: Web auth flow for Last.fm — auth URL generation, callback handling, session key exchange
    - Why: Last.fm CLI auth uses `auth.getMobileSession` (requires password). Web flow uses `auth.getSession` (token-based, no password needed)
    - Dependencies: Token Persistence
    - Status: Completed (2026-03-18)
    - Notes:
        - `GET /api/v1/connectors/lastfm/auth-url`: generates Last.fm auth URL with callback derived from request headers
        - `GET /auth/lastfm/callback`: exchanges token for permanent session key via `auth.getSession`, stores username + session key
        - Last.fm session keys are permanent (no refresh needed)

- [x] **Disconnect Endpoint**
    - Effort: XS
    - What: `DELETE /api/v1/connectors/{service}/token` — removes stored OAuth token
    - Why: Users need to disconnect services from the browser without CLI access
    - Dependencies: Token Persistence
    - Status: Completed (2026-03-18)
    - Notes:
        - Service whitelist: `spotify`, `lastfm` (rejects `musicbrainz`, `apple`)
        - Delegates to `DatabaseTokenStorage.delete_token()`

#### Integrations Page Auth UX Epic

- [x] **Interactive ConnectorCard**
    - Effort: M
    - What: Rewrite ConnectorCard from static display to interactive auth card with 6 states
    - Why: Static "run `narada auth spotify`" text is useless for web-only users; connect/disconnect must happen in the browser
    - Dependencies: OAuth Web Flows, Disconnect Endpoint
    - Status: Completed (2026-03-18)
    - Notes:
        - **Card states**: `disconnected` (connect CTA), `connected` (status + disconnect), `expired` (reconnect), `error` (try again), `coming_soon` (Apple ghost card), `public_api` (MusicBrainz always-on)
        - **Connector-colored buttons**: Spotify green, Last.fm red — `bg-{service}/15 text-{service} border-{service}/30`
        - **Permissions transparency**: below connect button — "We'll access your playlists..." (addresses trust concern)
        - **Connected state**: left border accent in connector color, green status dot, account name, "Token refreshes automatically" / "Permanent session"
        - **Disconnect flow**: subtle text button → `ConfirmationDialog` → DELETE mutation → toast
        - **`useConnectorAuth` hook**: imperative auth-url fetch + `window.location.href` redirect for connect; `useMutation` for disconnect

- [x] **Auth Callback Handling**
    - Effort: S
    - What: Read `?auth=&status=&reason=` query params on page load, show toast, invalidate connectors query, clean URL
    - Why: OAuth redirects return to `/settings/integrations` with query params indicating success/failure
    - Dependencies: Interactive ConnectorCard
    - Status: Completed (2026-03-18)
    - Notes:
        - `useEffect` reads query params on mount
        - Success: `toast.success("Spotify connected")` + query invalidation
        - Error: `toast.error` with human-readable reason mapping (access_denied, exchange_failed, invalid_state, etc.)
        - URL cleaned via `navigate("/settings/integrations", { replace: true })`
        - Auth error persisted in component state → passed to card for error-state rendering
        - **Backend fix**: redirects changed from `/settings?auth=...` to `/settings/integrations?auth=...` (React Router's `<Navigate>` was stripping query params)

- [x] **Onboarding Hero**
    - Effort: S
    - What: Warm first-run empty state when no connectable services are connected
    - Why: "No connectors configured" is a sad empty state — this is THE first impression for new users
    - Dependencies: Interactive ConnectorCard
    - Status: Completed (2026-03-18)
    - Notes:
        - Vinyl disc icon, "Bring your music home" heading, "Your data stays yours — always" messaging
        - Connect Spotify + Connect Last.fm buttons with connector colors
        - Shown when both Spotify and Last.fm are disconnected (MusicBrainz/Apple don't count)
        - Cards still render below for full status visibility

- [x] **Responsive Grid & Polish**
    - Effort: XS
    - What: 2-column grid (was 3), staggered fade-up animations, 44px touch targets
    - Dependencies: None
    - Status: Completed (2026-03-18)
    - Notes:
        - Grid: 1-column mobile, 2-column sm+ (breathing room for auth flow content)
        - Staggered `animate-fade-up` with 75ms delay per card
        - Connect buttons: `min-h-[44px]`, `w-full sm:w-auto`
        - Updated page description: "Your music services. Connect streaming services and data sources..."

- [x] **OpenAPI + Orval Regeneration**
    - Effort: XS
    - What: Regenerate OpenAPI spec and Orval client after new auth endpoints
    - Dependencies: OAuth Web Flows, Disconnect Endpoint
    - Status: Completed (2026-03-18)
    - Notes:
        - New generated: auth query hooks (spotify/lastfm auth-url), delete mutation hook, MSW mocks
        - Auth MSW mocks added to test `setup.ts`
        - Tests updated: `ConnectorCard.test.tsx` (all 6 states, connect flow, disconnect dialog), `Integrations.test.tsx` (onboarding hero, connect buttons, sections)

#### Accessibility & Theming Epic

- [x] **WCAG AA Accessibility Pass**
    - Effort: S
    - What: Fix contrast, focus indicators, aria attributes, and motion preferences on the Integrations page
    - Why: Several WCAG AA violations: `text-faint` contrast at 3.9:1 (below 4.5:1 minimum), no focus ring on Disconnect button, status dots not marked decorative, no `prefers-reduced-motion` support
    - Dependencies: Interactive ConnectorCard
    - Status: Completed (2026-03-19)
    - Notes:
        - `text-faint` lightness bumped 0.55 → 0.60 (dark) / 0.45 (light) for 4.5:1+ contrast
        - Disconnect button: `focus-visible:ring-2` + `aria-label="Disconnect {service}"`
        - Status dots: `aria-hidden="true"` (adjacent text already conveys status)
        - `ConnectorIcon`: `aria-hidden` when `labelHidden` to prevent duplicate screen reader announcements
        - `prefers-reduced-motion` media query: `0.01ms` durations (not `0s`) so `animationend` events still fire for `useAnimatedPresence.ts`

- [x] **Light/Dark Mode Toggle**
    - Effort: M
    - What: Full light palette, dark/light/system toggle in sidebar, flash-free theme persistence
    - Why: App was dark-only. Light mode is a basic accessibility expectation; system preference support respects user OS settings
    - Dependencies: WCAG AA Accessibility Pass (contrast must work in both modes)
    - Status: Completed (2026-03-19)
    - Notes:
        - **Design philosophy**: warm editorial in both modes — light mode is "sunlit parchment in a record shop" (oklch hue 60 warm off-whites), not sterile #FFFFFF. Low chroma surfaces (0.003–0.008), warm borders, grain texture preserved in both modes.
        - **CSS restructuring**: mode-independent tokens (connector colors, status, typography, animations) stay in `@theme`. Mode-dependent tokens (surfaces, text, borders, shadows) moved to `@theme inline` with `var()` references to CSS custom properties defined in `:root` (light) and `.dark` (dark). Tailwind v4 canonical pattern.
        - **ThemeContext**: `ThemeProvider` + `useTheme()` hook. Modes: dark/light/system. Persists to `localStorage`. Listens to `matchMedia("prefers-color-scheme: dark")` for system changes. Toggles `.dark` class + `colorScheme` style on `<html>`.
        - **Flash prevention**: inline `<script>` in `<head>` before CSS loads. `class="dark"` default on `<html>` for no-JS fallback.
        - **ThemeToggle**: cycling icon button (Moon → Sun → Monitor) in sidebar footer. `Button variant="ghost" size="icon-xs"`.
        - **Light palette**: primary gold L=0.50 (vs dark L=0.75) with slightly higher chroma (0.16) — richer amber for contrast on light backgrounds. Surfaces: page L=0.97, cards L=0.99, sunken L=0.93. Borders: warm gray L=0.88. Shadows: warmer, lower opacity.
        - **React Flow**: light mode defaults + `.dark .react-flow` overrides
        - **Sonner toasts**: dynamic `theme={resolvedTheme}` from `useTheme()`
        - **Grain texture**: light mode `opacity: 0.02; multiply`, dark mode `opacity: 0.035; overlay`
        - **Test infrastructure**: `matchMedia` polyfill in test setup, `ThemeProvider` added to `renderWithProviders` wrapper
        - Files: `theme.css` (major rewrite), `ThemeContext.tsx` (new), `ThemeToggle.tsx` (new), `index.html`, `Sidebar.tsx`, `main.tsx`, `sonner.tsx`, `test-utils.tsx`, `setup.ts`
        - Tests: 390 passing (5 new: ThemeToggle cycle, accessible label, ConnectorCard aria-label, status dots aria-hidden, Sidebar toggle presence)

- [x] **User Settings Persistence**
    - Effort: S
    - What: Generic `user_settings` table with JSONB blob, API endpoints, and ThemeContext sync
    - Why: Theme preference must survive device/browser changes; JSONB pattern extends to future settings without migrations
    - Dependencies: Light/Dark Mode Toggle
    - Status: Completed (2026-03-19)
    - Notes:
        - **`user_settings` table**: single-row JSONB store keyed by `key='default'` (upgradeable to per-user in v1.0.0). New settings = new JSONB keys, no migrations needed.
        - **API**: `GET /api/v1/settings` → full settings, `PATCH /api/v1/settings` → partial merge. Follows `DatabaseTokenStorage` pattern (standalone session, pg_insert upsert).
        - **Frontend dual-write**: localStorage for instant flash-free reads, API for durable persistence. On mount: localStorage is authoritative (prevents flash), then API response overwrites if different. On change: write both simultaneously.
        - **Default: dark** for new users (no stored value → dark mode). System preference available via toggle.
        - Migration: `f80e19f95cdd_add_user_settings_table`

---

### v0.5.5: Parallel Execution & Performance
**Goal**: Unlock Prefect parallel task execution (now that PostgreSQL provides MVCC) and add simple caching for the web UI.

**Context**: `SharedSessionProvider` was a SQLite workaround — all Prefect tasks shared one session because SQLite can't handle concurrent writers. With PostgreSQL's connection pool, each task gets its own session and `.submit()` parallelism becomes safe. Caching is lightweight optimization work that polishes the deployed experience.

**What this unlocks**:
- **Faster workflows**: Source and enricher nodes run concurrently where the DAG allows
- **Simpler internals**: Remove `SharedSessionProvider` and manual topological sort
- **Snappier UI**: Reduced database queries via caching, measurable performance targets

#### Parallel Execution Epic

- [ ] **Prefect Parallel Execution**
    - Effort: M
    - What: Remove `SharedSessionProvider`, enable per-task sessions, `.submit()` parallelism
    - Why: `SharedSessionProvider` was a SQLite workaround; PostgreSQL handles concurrent sessions
    - Dependencies: v0.5.1 (PostgreSQL MVCC)
    - Status: Not Started
    - Notes:
        - Each Prefect task creates its own session from the pool
        - Replace manual topological sort with `.submit()` + Prefect-native future resolution
        - Source and enricher nodes become concurrently executable where DAG allows

#### Performance Epic

- [ ] **Caching & Performance Strategy**
    - Effort: S
    - What: Simple caching for hobbyist web UI
    - Why: Reduce database queries without external dependencies
    - Dependencies: None
    - Status: Not Started
    - Notes:
        - **Backend**: `lru_cache` for pure functions; ETags + Last-Modified; selectinload() (already implemented)
        - **Frontend**: Tanstack Query stale-while-revalidate + optimistic updates
        - **Targets**: TTI <5s, FCP <2s, API p95 <500ms, Lighthouse >70
