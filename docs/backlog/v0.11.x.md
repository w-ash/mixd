# v0.11.x: Connector Foundations

For strategic overview, see the [planning overview](README.md).

## User Workflow Context

The user's musical life spans more services than mixd can see: Apple Music and Tidal are invisible to the play history and library v0.10.x made honest, and the physical collection catalogued on Discogs has no presence. The existing Spotify connector also has to track Spotify's 2026 program changes to stay trustworthy. This cycle lands the *bones* of three connectors — auth, API clients, conservative track resolution, the Apple play channel — plus a Spotify currency pass. It deliberately precedes first-class artists and albums (v0.12.x) for two reasons: entity modeling should be designed against real catalog data from six services (the v0.12.0 spike consumes these clients), and Apple plays should start accruing into the observation ledger now rather than after the entity work. Full feature surfaces — Apple library/likes, Discogs collection import, Tidal likes/playlists — intentionally wait for v0.13.x, once the entities they attach to exist.

Persona view (from the [alignment table](README.md#persona-alignment)): the **Weekly Curator** gets Apple listening flowing into their canonical history and a Spotify connection that asks to reconnect instead of failing silently; the **Tinkerer** gets three new connectors' raw surfaces to poke at; the **Casual** listener gets the groundwork for "connect Apple Music" and "connect Tidal" being first-class buttons.

Cross-cutting contract (stated once — per-epic Specs reference it): every new use case this cycle must be classified in the chat-tool parity registry (`src/application/tools/registry.py`) — CI (`tests/unit/application/tools/test_registry_parity.py`) fails on any unclassified use case. Reads get a `ToolSpec` or an explicit classification; mutations additionally get two-phase confirmation wiring like the existing `*_write` dispatchers.

---

### v0.11.0: Apple Music Foundation

**Goal**: mixd can authenticate to Apple Music, resolve its tracks conservatively, and capture Apple plays as a live observation channel — the bones that let the v0.12.0 spike see Apple's real catalog shapes and let play history start accruing immediately.

**Context**: An existing stub at `src/infrastructure/connectors/apple_music/` (`connector.py`, `error_classifier.py`) provides the starting point; shared infrastructure (`_shared/http_client.py`, retry policies, error classification) means the client is mostly wiring. Auth differs from Spotify in kind, not degree. The developer token is a self-signed ES256 JWT (Team ID, `kid`, `exp` ≤6 months). The per-user Music User Token (MUT) exists **only** via MusicKit JS `authorize()` in a browser — Apple offers no server-side user auth and no CLI path. The MUT lives 6 months, fixed, with no refresh ([Apple staff](https://developer.apple.com/forums/thread/654814)). **Doctrine: each deployment brings its own Apple Developer membership (US$99/yr)** and pastes its `team_id`/`key_id`/`.p8` key into instance config; this deployment's membership is in place. Apple has no "love" API (Add to Library is the like analogue — deferred with the rest of the library surface to v0.13.0). Play data accrues only once the user actually listens on Apple Music; the channel ships regardless so nothing is lost when they do. Endpoint reference: [provider-integration-research-2026-08.md](provider-integration-research-2026-08.md).

**What this unlocks**: Apple catalog access for the v0.12.0 entity-representation spike; Apple plays flowing through the v0.10.0 projection; the auth substrate v0.13.0's library/likes integration reuses.

**Persona**: Curator-primary (Apple listening joins the canonical history); Tinkerer secondary (a fourth connector's auth model to inspect).

**Key Design Decisions**:
- **Ingest early, resolve conservatively** — Apple observations land in `connector_plays` immediately, but resolution is **ISRC-only** (excellent coverage in Apple's catalog); fallback candidates are recorded *unresolved* rather than matched, because the pre-alias comparator (alias-aware artist comparison lands in v0.12.1) would mint permanent weak mappings. v0.13.0's re-resolution pass + `mixd plays rebuild` promotes the deferred observations once the full matcher exists. No permanent weak mappings, no lost plays.
- **Supersession-native from birth** — landing after v0.10.2 means Apple's platform-asserted succession (`catalogId` vs `playParams.catalogId` equivalence, parallel to Spotify relinking) is representable in the mapping schema from the first row; the migration the old sequencing risked never happens.
- **The play channel is presence-weak by design** — Apple's recently-played API returns **no timestamps** (≤30 items, offset paging), so `played_at` is approximated from the poll window with a channel-specific wide tolerance, and the `apple_api` channel ranks below every timestamped channel in survivorship: it may create a play, but never wins timestamp fields against an observer that knows when.

#### Substrate Epics (cycle-wide)

- [x] **Connector Auth-State Fidelity**

    **Story**: The user whose connector credential ages out wants the app to say "reconnect", not fail silently or show a generic error. Two of this cycle's credentials expire by design — the Spotify refresh grant (v0.11.2 Context) and the Apple MUT (v0.11.0 Context) — so "credential expired, reconnect" must be a first-class state every connector can raise.

    **Decisions**:
    - **Expected expiry is a state, not an error** — the user can fix it in one click, and an error-styled surface trains them to ignore real failures.
    - **No `StoredToken` schema change** — `total=False` plus `extra_data` (JSONB) already absorb every credential shape this cycle adds; a documented field contract prevents per-connector drift.

    **Spec**:
    - Add `"reauth_required"` to `ConnectorAuthError` (`src/domain/entities/connector.py:34`); map it in `derive_status_state()` (:76) to `"needs_reauth"` (the `scope_missing` branch is the pattern)
    - Widen `ConnectorAuthMethod` (:20) once for the auth shapes this cycle adds (browser-bridge, personal access token, device-code); pick the exact literals at implementation
    - Document the per-connector `StoredToken` field contract in `_shared/token_storage.py`'s docstring: Apple = opaque `access_token` + explicit `expires_at`, no `refresh_token`; Tidal = access + refresh pair; Discogs = personal access token (OAuth 1.0a token secret in `extra_data` if ever needed); `authorized_at` (Unix ts) in `extra_data` wherever grant age matters
    - Web (`GET /connectors`) and CLI status render `needs_reauth` with a reconnect affordance (surfaces exist — `_shared/connector_status.py`, `interface/cli/connector_commands.py`)

    **Tests**:
    - (unit) `reauth_required` → `derive_status_state()` returns `needs_reauth`
    - (integration) the web connector list and CLI status render the reconnect state, not an error

    Effort: S | Dependencies: None | Status: Completed (2026-08-22)

    **Completion note**: literals chosen: `browser_bridge`, `token`, `device_code`. CLI keeps its direct `auth_error` ladder (no `derive_status_state` refactor). Web/CLI reconnect rendering test-pinned; `reauth_required` wins over `expired`.

#### Apple Foundation Epics

- [x] **Apple Music Auth & API Client**

    **Story**: Every Apple Music operation — catalog lookups for the spike, play polling, the eventual library sync — needs an authenticated client; this is the connector's foundation and the external-prerequisite resolver for everything Apple.

    **Decisions**:
    - **Developer token minted server-side; user token only via the MusicKit JS browser bridge** — Apple offers no server-side user auth, so the connector is web-only by construction. The CLI reads Apple data through the server but can never mint a MUT.
    - **Persist the MUT server-side** (encrypted, RLS-scoped, explicit `expires_at`) — MusicKit's own per-origin browser storage silently de-authorizes when the instance is reached at a second hostname ([music-assistant #3897](https://github.com/orgs/music-assistant/discussions/3897)).

    **Spec**:
    - `AppleMusicAPIClient(BaseAPIClient)` with httpx2; Pydantic models for JSON:API response shapes; rebuild the pre-modern `apple_music/` stub to the current spotify/lastfm package layout
    - Developer token: ES256 mint from instance config (`team_id`, `key_id`, `.p8` key), `exp` ≤6 months, rotate ~90 days (starting point, revisit), cached until expiry; optional `origin` claim as an admin hardening field — the claim is developer-authored, no Apple-side registration exists
    - Bridge route: loads MusicKit JS v3 from Apple's CDN (it cannot be vendored), runs `authorize()`, POSTs the resulting MUT to the backend; the route sets `Referrer-Policy: strict-origin-when-cross-origin` — a stricter policy (e.g. `no-referrer`) is the documented cause of `authorize()` 403s *(superseded in v0.11.3.2 by the in-app MusicKit flow — the SPA runs `authorize()` itself; no bridge page remains)*
    - MUT storage via the `TokenStorage` pattern (encrypted at rest), `expires_at = granted_at + 6 months`, no refresh; a 403 `TOKEN_EXPIRED` raises `reauth_required` and the connector card offers re-authorize
    - Refactor existing `AppleMusicErrorClassifier` to the modern hook pattern
    - Reuse: `_shared/http_client.py`, `_shared/retry_policies.py`, `_shared/error_classifier.py`

    **Tests**:
    - (unit) developer-token mint/expiry/cache — correct ES256 claims; the key never serializes into a response
    - (integration, mocked transport) 401 → actionable reconnect error; retry policy on 5xx
    - (integration) expired MUT (403 `TOKEN_EXPIRED`) yields `needs_reauth`, not a generic error
    - (integration) the bridge route response carries the `Referrer-Policy` header

    Effort: L | Dependencies: Connector Auth-State Fidelity (Apple Developer membership in place) | Status: Completed (2026-08-22)

    **Completion note**: live probe (2026-08-22) corrected auth semantics — 401 = developer-token problem, 403 = MUT problem (code `40300`, no `TOKEN_EXPIRED` string exists); classifier uses the any-403 rule. Bridge needs Vite `/auth` proxy in dev. Shapes documented in `apple_music/API_NOTES.md`; fixtures are redacted live captures.

- [x] **Conservative Track Resolution**

    **Story**: Apple tracks must connect to canonical tracks for plays and the spike to mean anything — but mappings are permanent until v0.10.2's supersession machinery is exercised, so the resolver must only assert what it can anchor.

    **Decisions**:
    - **ISRC-first, ISRC-only** — no artist+title fallback in this milestone: the alias-aware comparator doesn't exist until v0.12.1, and a weak mapping minted now persists. Fallback candidates are recorded as unresolved observations and queued via the v0.10.2 event log's no-match backoff — v0.13.0's "Deferred Play Re-Resolution" epic is their designed exit.
    - **Content equivalence via dual mapping** — `catalogId` vs `playParams.catalogId` reuses the Spotify-relinking dual-mapping pattern, recorded as platform-asserted succession (v0.10.2 typed reason).

    **Spec**:
    - `AppleMusicInwardResolver` + `AppleMusicMatchingProvider` with only the ISRC path active
    - `filter[isrc]` batches ≤25 identifiers per request; every catalog path is storefront-scoped — read the user's storefront once via `GET /v1/me/storefront` (needs the MUT) and hold it as connection state
    - `filter[equivalents]` is the cross-storefront successor surface behind the dual-mapping decision
    - Reuse: `_shared/inward_track_resolver.py`, `_shared/matching_provider.py`, `_shared/isrc.py` (including the ISRC-suspect duration cross-check)
    - Unresolved-candidate recording: ledger rows keep `resolved_track_id = NULL`; no-match rows enter the v0.10.2 backoff queue

    **Tests**:
    - (unit) ISRC hit → mapping with `match_method="isrc"`; no ISRC → **no** `artist_title` mapping minted, observation stays unresolved
    - (integration) unresolved Apple ledger rows excluded from the projection and queryable for the v0.13.0 re-resolution pass

    Effort: M | Dependencies: Apple Music Auth & API Client, v0.10.2 (supersession schema) | Status: Completed (2026-08-22)

    **Completion note**: mapping method is existing `isrc_match` (raw-match grade string stays `"isrc"` — the two-level split Spotify uses). `playParams.catalogId` is absent on catalog/recent data, so the dual-mapping path is dormant until v0.13.0 library surfaces. ISRC 1:N observed at 43 songs. ISRC-only enforced via shared `supports_artist_title_matching=False` flag.

- [x] **Apple Play Channel**

    **Story**: The Apple Music listener's plays should land in the same canonical history as everything else — this activates the channel the v0.10.0 projection was designed to accept without new dedup code (that claim is this epic's acceptance test).

    **Decisions**:
    - **Poll-window-midpoint timestamps, presence-only precedence** — Apple returns no `played_at`, so the channel contributes *that* a play happened, never *when* precisely; the projection's channel-keyed survivorship handles the rest.
    - **Rides the v0.10.1 polling policy wholesale** — demand triggers, adaptive heartbeat, redundancy-aware caps, and the shared `run_sync_target` runner all apply; Apple is one more `SYNC_TARGETS` entry and one more checkpoint row, not new infrastructure.

    **Spec**:
    - Client: `GET /v1/me/recent/played/tracks` (`types=songs`, window ≈30 items — community-measured 2021–23, measure at implementation; offset paging)
    - Channel `("apple", "apple_api")`: identifier = Apple catalog song id; `played_at` = poll-window midpoint; channel-specific wide tolerance constant in `play_projection.py`; lowest timestamp-precedence rank
    - `SYNC_TARGETS["apple:plays"]` (`SyncTargetSpec`, `src/application/use_cases/_shared/sync_targets.py`), executed by `run_sync_target` (`src/application/services/sync_target_runner.py`); add `("apple", "plays")` to `CHECKPOINT_COMBINATIONS` (`sync_likes.py`) so the sync-status table shows the checkpoint with effective-interval-aware staleness
    - Rate posture: Apple publishes no limits and returns no rate headers; 429s self-resolve; the quota attaches to the developer token, so every user of the instance shares one budget
    - Import stats + `OperationRun` audit come free from the shared runner

    **Tests**:
    - (unit) wide-tolerance grouping + presence-only survivorship for `apple_api` observations
    - (integration) simulated overlapping Apple + Last.fm observations of one listen → one canonical play, Last.fm's timestamp wins
    - (manual — milestone acceptance) connect Apple → listen → play appears in history and the sync table shows the checkpoint row

    Effort: M | Dependencies: Conservative Track Resolution, v0.10.1 (polling infrastructure) | Status: Completed (2026-08-22)

    **Completion note**: ships without adaptive-poll hooks — the v0.10.1 policy hardcodes spotify/lastfm; generalization filed in `unscheduled.md`. Probe facts: limit caps at 30 (error `40005`), window ≥60 via offset paging, window holds unique songs (repeats move, not duplicate) — prefix-diff dedupe conflict-skips on turnover. Tolerance 1800 s; acceptance verified live (2 plays, midpoint timestamps).

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.11.1: Discogs Foundation

**Goal**: An authenticated Discogs client and a first look at the user's collection — the bones the v0.12.0 spike queries and v0.13.1 physical media builds on.

**Context**: Discogs has 15M+ releases with the richest physical-media metadata anywhere (formats, labels, catalog numbers, pressing notes) and a distinctive artist model (name variations, ANVs, join phrases) that the v0.12.0 spike must see before artist entities are designed. The user's own collection may still be small or empty — the snapshot degrades gracefully, and the *catalog* endpoints (release/master/artist) are what the spike consumes regardless.

**What this unlocks**: Discogs catalog access for the spike; the client and token plumbing v0.13.1's collection import reuses; the connector-card presence that invites the user to start cataloguing.

**Persona**: Curator-primary (the record collector's on-ramp); Tinkerer secondary.

**Key Design Decisions**:
- **Foundations only** — no canonical writes this milestone: release matching and collection import need albums (v0.12.2) and land in v0.13.1; shipping the client early is what makes the spike's Discogs leg real instead of speculative.

#### Discogs Foundation Epics

- [x] **Discogs API Client & Auth**

    **Story**: Every Discogs operation — collection import, release detail — needs an authenticated client; this is the connector foundation.

    **Decisions**:
    - **Per-user personal access token (BYO-token), not OAuth 1.0a** *(2026-07 check: Discogs API healthy, no deprecation; tokens officially recommended for single-user apps — same 60 req/min, never expire)* — collection reads don't need the OAuth dance, and BYO-token matches the existing BYO-Anthropic-key pattern (v0.9.0.1): validated live, stored encrypted, write-only in the UI. The client takes an injected auth strategy so OAuth 1.0a can slot in later if write/marketplace scopes are ever needed — mixd has zero RFC 5849 code today.

    **Spec**:
    - `DiscogsAPIClient(BaseAPIClient)` with httpx2, Pydantic models; auth-strategy seam (token header now, OAuth 1.0a slot open)
    - Token storage + settings surface follow the Anthropic-key pattern: encrypted at rest via `oauth_tokens`, connect-gated UI
    - Rate model: Discogs throttles **by source IP** — every user of the instance (and anything sharing its egress) shares one 60/min counter (docs; [staff-confirmed](https://www.discogs.com/forum/thread/867991); probed 2026-08-20). Auth choice does not change throughput — OAuth 1.0a and personal tokens share the bucket
    - One instance-wide serialized fetch queue for Discogs API calls — a 5,000-item collection at `per_page=100` consumes ≈50 s of the whole instance's budget, so concurrent per-user fetches would starve each other
    - Header-authoritative throttling: pace ~40–45 req/min (starting point, revisit) and self-correct from `X-Discogs-Ratelimit-Remaining` — the counter can be non-zero on the first call
    - Unique `User-Agent` (`Mixd/<version> +<repo-url>`) — generic agents get silently lower limits
    - `i.discogs.com` image fetches bypass the API limiter (separate, undocumented bucket); ignore rate headers on image responses
    - Pagination stops at the 10,000-item ceiling; add a `discogs` `ConnectorAPIConfig` field to `APIConfig` (`src/config/settings.py`)
    - Key endpoints: `/users/{username}/collection/folders/0/releases` (full collection), `/releases/{id}` (release detail), `/masters/{id}` (master release)
    - Pydantic models for release, master, artist, label, format responses
    - Reuse: `_shared/http_client.py`, `_shared/retry_policies.py`

    **Tests**:
    - (unit) auth header injection + 401 → actionable reconnect error; (integration, mocked transport) pagination + rate-limit retry
    - (integration) two users' concurrent fetches serialize through one queue; a low `X-Discogs-Ratelimit-Remaining` slows the pacer; an image fetch consumes no API budget

    Effort: M | Dependencies: None | Status: Completed (2026-08-22)

    **Completion note**: `token_type="personal_token"` (column VARCHAR(20)). Serialization = per-loop asyncio.Lock registry + `ConnectorAPIConfig(concurrency=1, rate_limit=0.7)`; header feedback via proportional `pause_for`. Live probe (2026-08-22): headers lowercase, first counter 60/60/0, tracklist uses `type_` key. Invalid token → 400 `DISCOGS_INVALID_TOKEN` envelope.

- [x] **Collection Snapshot**

    **Story**: Connecting a service should show something real immediately — the collector connects Discogs and sees their collection acknowledged, before any of the matching machinery exists.

    **Decisions**:
    - **Raw display, zero canonical writes** — the snapshot lists what Discogs reports, unmatched; import-with-matching is v0.13.1's job, and pretending otherwise here would mint the exact premature mappings v0.11.0 is designed to avoid.

    **Spec**:
    - Fetch collection count + recent items via the collection endpoint (paginated); all fetches run through the instance-wide Discogs queue (see Discogs API Client & Auth)
    - CLI: `mixd discogs snapshot` — count + table of recent items; connector card shows "connected · N releases"
    - Empty collection renders an inviting zero-state, not an error
    - Parity (file-header contract): read → `ToolSpec`

    **Tests**:
    - (unit) empty-collection zero-state; (integration, mocked transport) pagination aggregation to a correct count

    Effort: S | Dependencies: Discogs API Client & Auth | Status: Completed (2026-08-22)

    **Completion note**: status probe is network-free — count stored at connect, refreshed by snapshot (`ConnectorStatus.detail`). Zero-state verified live against the user's empty collection (web card + CLI). Items missing `basic_information` skip with a warning (shape unverifiable live).

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.11.2: Spotify API Currency

**Goal**: The existing Spotify connection stays honest under Spotify's 2026 program changes — an expired grant says "reconnect", a quota exhaustion is not mistaken for a rate limit, and disconnect states exactly what it removes.

**Context**: The program facts this milestone responds to (effective dates, not predictions): refresh tokens expire 6 months after the original authorization, and a refresh does not extend the window ([policy, 2026-06-18](https://developer.spotify.com/blog/2026-06-18-refresh-token-expiration); enforced for existing apps 2026-07-20; expiry surfaces as HTTP 400 `invalid_grant`). API quota pools per developer account across all of the account's client IDs (2026-07-23); quota exhaustion returns a 429 whose body carries `reason: "QUOTA_EXCEEDED"` and does not clear on `Retry-After` timescales. Development-mode `GET /me` no longer returns `email`, `country`, or `product`; the `account_id` field (added 2026-05) is Spotify's designated key for external linkage. Endpoint reference: [provider-integration-research-2026-08.md](provider-integration-research-2026-08.md).

**What this unlocks**: a Spotify connector whose failure states are all actionable — and the shared re-auth substrate exercised on a live connector before v0.11.3 Tidal reuses it.

**Persona**: Curator-primary (their primary connector keeps working and says what it needs); Tinkerer secondary (CLI reconnect flow).

**Key Design Decisions**:
- **Expiry is handled where it surfaces** — the refresh POST is the one place `invalid_grant` appears, so detection, token deletion, and the `reauth_required` signal live in the auth layer; UI and CLI only render the state.

#### Spotify Currency Epics

- [x] **Spotify Re-Auth & Reconnect**

    **Story**: The user whose grant aged out sees a "reconnect Spotify" prompt and is back in one click — instead of silent sync failures behind a swallowed warning.

    **Decisions**:
    - **Delete the dead token on `invalid_grant`** — a revoked refresh token can never succeed again; keeping it makes every later operation re-fail slowly.

    **Spec**:
    - Detect HTTP 400 `invalid_grant` on the refresh POST (`spotify/auth.py:196` — today `raise_for_status()` discards the body; `try_silent_refresh` (:361) swallows failures to a warning; `force_refresh` (:388) raises uncaught)
    - On detection: delete the stored token, raise `reauth_required` (v0.11.0 substrate); connector card + CLI status render the reconnect affordance
    - Persist `authorized_at` (Unix ts, `StoredToken.extra_data`) at grant time so grant age is knowable
    - Add the reconnect flow to `docs/web-ui/01-user-flows.md`; update `.claude/rules/spotify-api.md`
    - Dev-env fix (found 2026-08-22, during v0.11.0 live probe): `SPOTIFY_REDIRECT_URI` in `.env.local` points at the CLI flow's one-shot `127.0.0.1:8888` listener, so the *web* Connect button dead-ends after approval in local dev — register `http://localhost:5173/auth/spotify/callback` in the Spotify app dashboard and repoint the env var (the Vite `/auth` proxy added in v0.11.0 forwards it). Verify as part of this epic's reconnect-affordance testing — the reconnect button is only honest if a fresh web connect actually completes.

    **Tests**:
    - (unit) mocked 400 `invalid_grant` → token deleted, `reauth_required` raised; mocked 500 → retry path unchanged
    - (integration) the silent-refresh path surfaces `needs_reauth` instead of a swallowed warning

    Effort: M | Dependencies: Connector Auth-State Fidelity (v0.11.0) | Status: Completed (2026-08-22)

    **Completion note**: `SpotifyReauthRequiredError` subclasses `SpotifyAuthRequiredError` (existing catch sites work unchanged). Delete is compare-and-swap on the failed refresh token (stale-manager race). After deletion the card shows Connect, not a persistent Reconnect (same one-click remedy). Dev redirect-URI fix still open (this epic's Spec).

- [x] **Quota-429 Discrimination**

    **Story**: When the pooled account quota runs out, the user sees "Spotify quota exhausted" — not an endless rate-limit retry that cannot succeed.

    **Spec**:
    - `spotify/error_classifier.py`: split 429 handling on body `reason == "QUOTA_EXCEEDED"` vs plain rate-limit
    - Quota exhaustion skips the `Retry-After` backoff loop and emits a distinct log signal naming PDR-003 (pattern: the existing `_get_tracks_batch_impl` 403 instrumentation)

    **Tests**:
    - (unit) both 429 shapes classify differently; the quota body does not enter the rate-limit retry path

    Effort: S | Dependencies: None | Status: Completed (2026-08-22)

    **Completion note**: quota 429 → `SpotifyQuotaExhaustedError` via a `BaseAPIClient._surface_suppressed_error` hook; batches abort on first hit; HTTP 503 `SPOTIFY_QUOTA_EXHAUSTED`. Plain 429s keep Retry-After backoff.

- [x] **Account Identity & Explicit Disconnect Semantics**

    **Story**: The user's Spotify link survives a display-name change, and disconnecting states exactly what it removes — credentials go, imported library and history stay theirs.

    **Decisions**:
    - **Disconnect removes credentials, not data** — imported likes, plays, and mappings are the user's records; account deletion (v0.6.4 purge) stays the full-removal path.

    **Spec**:
    - Adopt `account_id` from `GET /me` for external linkage; verify the live field name first (single-source claim)
    - Remove any read of `email`/`country`/`product` (absent in development mode)
    - Make disconnect explicit: `DELETE /api/v1/connectors/{service}/token` (`routes/connectors.py:129-147`) removes the token row and stops Spotify play polling; state the retained data on the disconnect confirm and in `docs/web-ui/01-user-flows.md`. (`spotify/personal_data.py` is the GDPR-export parser — unrelated to disconnect)

    **Tests**:
    - (unit) profile parse without `email`/`country`/`product` succeeds
    - (integration) disconnect deletes the token row and nothing else; status renders disconnected

    Effort: S | Dependencies: None | Status: Completed (2026-08-22)

    **Completion note**: live-verified (2026-08-22): `account_id` top-level on /me; email/country/product absent. Stored in `extra_data` with an `account_id_unavailable` marker to stop probe re-fetch loops. `authorized_at`/`account_id` have no reader yet — grant-age surfacing is future work.

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.11.3: Tidal Foundation

**Goal**: An authenticated Tidal client, conservative track resolution, and a favorites snapshot — the bones the v0.12.0 spike queries and v0.13.4 completes.

**Context**: Tidal's current API ([developer.tidal.com](https://developer.tidal.com/)) is JSON:API-shaped with first-class ISRC and barcode lookup, cursor pagination, and full playlist CRUD — the strongest catalog surface of the new connectors. It has no listening-history endpoint, so Tidal contributes library and playlist data, never plays. The legacy v1 API and the `tidalapi` PyPI library that drives it are deprecated; this connector builds only on the documented v2 API. Endpoint reference: [provider-integration-research-2026-08.md](provider-integration-research-2026-08.md).

**What this unlocks**: Tidal catalog access for the v0.12.0 spike (the sixth service); the client, auth, and resolution substrate v0.13.4 builds likes sync and playlist CRUD on.

**Persona**: Curator-primary (the Tidal library's on-ramp); Tinkerer secondary (an OAuth 2.1 + device-code auth model to inspect).

**Key Design Decisions**:
- **BYO client ID per deployment** — each deployment registers its own free app at developer.tidal.com (≤10 apps per account, ≤3 redirect URIs per app, app name ≤30 characters), so each instance holds its own throughput bucket and credentials. Matches the Spotify BYO-credentials posture.
- **OAuth 2.1 with PKCE S256, mandatory for every client** — `client_credentials` serves catalog-only calls.
- **Design rate handling to `Retry-After` only** — Tidal publishes no rate numbers; trust `expires_in` from the live token response rather than documented TTLs.
- **Vendor the OpenAPI spec, diff it in CI** — Tidal revises the API in place (the search contract changed 2026-08-12 and was fixed same-day, [#366](https://github.com/orgs/tidal-music/discussions/366)); a vendored `tidal-api-oas.json` plus a CI diff turns silent contract changes into visible failures. Generated client code stays in an isolated module wrapped by the adapter (import-linter enforced).

#### Tidal Foundation Epics

- [x] **Tidal Auth & API Client**

    **Story**: Every Tidal operation — catalog lookups for the spike, the favorites snapshot, v0.13.4's library surface — needs an authenticated client; this is the connector foundation.

    **Decisions**:
    - **Refresh is single-flight per (user, service)** — Tidal rotates refresh tokens, and rotation treats a concurrent refresh as replay. This is the second refresh-token connector, so the guard (Postgres advisory lock at the token seam) is built shared, not Tidal-private.
    - **CLI authenticates via the device-code flow** — `POST /v1/oauth2/device_authorization` works but is undocumented; verify at implementation, with a localhost redirect URI on the same app as the fallback.

    **Spec**:
    - `TidalAPIClient(BaseAPIClient)` with httpx2; Pydantic models for JSON:API shapes; cursor pagination (page size is server-controlled)
    - Auth-code + PKCE (S256) with a server-held verifier per attempt; authorize `login.tidal.com/authorize`, token `auth.tidal.com/v1/oauth2/token`; token storage via the `TokenStorage` pattern (access + refresh pair)
    - `TidalErrorClassifier`; 429 honors `Retry-After`; add a `tidal` `ConnectorAPIConfig` field to `APIConfig` (`src/config/settings.py`)
    - Vendored `tidal-api-oas.json` + CI diff + codegen, per the churn-defense decision
    - At app registration, record the dashboard's allowed-scope list in this epic's completion note
    - Reuse: `_shared/http_client.py`, `_shared/retry_policies.py`, `_shared/error_classifier.py`

    **Tests**:
    - (unit) PKCE verifier/challenge round-trip; two concurrent refreshes produce one token POST
    - (integration, mocked transport) 429 + `Retry-After` honored; JSON:API cursor pagination; device-code polling loop

    Effort: L | Dependencies: Connector Auth-State Fidelity (v0.11.0) | Status: Implemented (2026-08-22) — live probe pending

    **Completion note**: PKCE + rotation-safe refresh via the shared `single_flight_token_refresh` (advisory xact lock, two-signal dedup: rotated token OR `refreshed_at`). Web + CLI redirect URIs split (`TIDAL_CLI_REDIRECT_URI`, register both). Pending T7 (needs app registration): device-code endpoint existence, live `expires_in`, allowed-scope list.

- [x] **Tidal Conservative Track Resolution**

    **Story**: Tidal tracks must connect to canonical tracks for the spike and later sync to mean anything — with the same discipline as Apple's v0.11.0 resolver: assert only what an identifier anchors.

    **Decisions**:
    - **ISRC-first, ISRC-only** — no artist+title fallback until the v0.12.1 alias-aware comparator exists (the v0.11.0 Apple epic states the rationale).
    - **`replacement` is platform-asserted succession** — Tidal's official successor pointer maps onto the v0.10.2 typed succession reason, the same shape as Spotify relinking and Apple `filter[equivalents]`. [Design-space §5.3](identity-resolution-design-space.md#5-connector-contract-scalability-omni-integration-assessment) defers seam extraction to the second in-code implementor; Tidal is the third (after Spotify relinking and the v0.11.0 Apple equivalents), so extract the per-connector resolve-successor seam here.

    **Spec**:
    - `TidalInwardResolver` + `TidalMatchingProvider`, ISRC path only: `GET /tracks?filter[isrc]=` (1:N, paginated; multi-ISRC batching returns one match per code — send one code per request)
    - Successor seam: a per-connector resolve-successor hook consulted for dead IDs; Tidal implements it via the `replacement` relationship; Spotify relinking and Apple equivalents migrate onto the seam
    - Reuse: `_shared/inward_track_resolver.py`, `_shared/matching_provider.py`, `_shared/isrc.py`

    **Tests**:
    - (unit) ISRC hit → `match_method="isrc"` mapping; no ISRC → observation stays unresolved, no `artist_title` mapping minted
    - (unit) a dead ID with a `replacement` pointer → typed succession recorded through the shared seam

    Effort: M | Dependencies: Tidal Auth & API Client, v0.10.2 (supersession schema) | Status: Implemented (2026-08-22) — live probe pending

    **Completion note**: successor seam extracted to `_shared/successor_resolution.py` (recording only; Spotify/Apple migrated byte-identical; Tidal is the first `SuccessorHook` implementor, one hop, fan-in deduped). New `isrc_match_stale_id` method. `replacement` fixture-tested; live verification pending T7.

- [x] **Favorites Snapshot**

    **Story**: Connecting Tidal shows something real immediately — the user sees their favorites acknowledged before any matching machinery exists (the Discogs snapshot's first-contact pattern).

    **Spec**:
    - `GET /userCollectionTracks/me/relationships/items` (`sort=-addedAt` default): count + recent items
    - CLI: `mixd tidal snapshot`; connector card shows "connected · N favorites"; zero canonical writes
    - Parity (file-header contract): read → `ToolSpec`

    **Tests**:
    - (unit) empty-collection zero-state; (integration, mocked transport) pagination aggregation to a correct count

    Effort: S | Dependencies: Tidal Auth & API Client | Status: Implemented (2026-08-22) — live probe pending

    **Completion note**: count = `meta.total` when served, else capped cursor walk (floor + warning); recent titles via ≤25 per-track lookups; `detail="N favorites"` storage-only. Live snapshot pending T7 connect.

**T7 live probe: deferred (2026-08-22)** — no active Tidal account. Trigger: when a Tidal subscription exists, register the app (developer.tidal.com, redirect URIs for web `5173/auth/tidal/callback` + CLI), then probe: device-code endpoint existence, live `expires_in`, refresh-rotation error shape, `filter[isrc]`, collection items + cursor, `replacement` on a dead id, allowed-scope list. The three epics above flip to Completed after the probe reconciles models/fixtures.

#### Post-Deploy Revisions

- [x] **v0.11.3.2** (2026-08-23) — fix/refactor: connector staleness root cause (HTTP cache), in-app Apple Music connect, cycle craft pass

    **Why:** Prod testing showed cards stale after connect despite the query-layer fixes — `/api/v1/connectors` shipped `Cache-Control: max-age=300`, so browsers answered refetches from disk cache. The Apple bridge page also opened mixd in a chromeless window; the popup users should see is Apple's sheet only.
    **What:** Connector status + settings are `no-cache` (musickit-config is `private, no-store`); Apple connect rebuilt in-app (MusicKit from the Connect click, prewarmed; bridge page deleted; token POST authenticated + stateless). Two Opus reviews: security clean; robustness fixes (dismissal heuristic, loader timeout, prewarm) and a cycle craft pass (shared OAuth primitives, 409 handler table, `CREDENTIAL_AUTH_METHODS`, `tidal/device_auth.py` split, ~450 lines of test dedup; net −550 lines). Four deferred findings filed in `unscheduled.md`.
- [x] **v0.11.3.1** (2026-08-23) — fix: every connector's web auth flow 500ed in prod (`DefaultUserOnRemoteDatabaseError`)

    **Why:** First prod connect attempt since the v0.10.4.1 remote-DB guard shipped: `_create_state`/`validate_state` (and all 15 remote-MCP OAuth AS helpers) opened bare sessions, so the RLS contextvar sat at `default` and the guard refused the transaction. Dev is guard-exempt (localhost), so no test ever armed it.
    **What:** Sessions now declare `user_context(user_id)` (state creation) or `system_context()` (state validation, OAuth AS). New remote-mode regression fixture arms the guard in tests — 7 red-first failures reproduced the prod error exactly.

---

### v0.11.4: Web Cache Consistency

**Goal**: After any action — connect a service, finish an import, save a key — every surface that shows the result updates itself, because invalidation is one declared mechanism instead of ~40 hand-picked `invalidateQueries` calls, and backend truth is never hand-mirrored in the frontend.

**Context**: Three drift classes surfaced this cycle. (1) A TanStack Query first-fetch race — `invalidateQueries` during a query's *initial* in-flight fetch joins it instead of restarting it (cancel-and-refetch requires `state.data !== undefined`), upstream-confirmed and [closed as not planned](https://github.com/TanStack/query/issues/8530) ([also #6536](https://github.com/TanStack/query/issues/6536)) — left the auth-callback redirect on a stale connectors list until manual reload; the fix (`fetchQuery`-then-invalidate in `Integrations.tsx`) is a local patch, not a pattern. A second live staleness hit during the v0.11.1 Discogs connect (success toast, stale card) forced a second local patch (await-refetch-before-success in `useDiscogsToken`) — each new mutation surface re-derives ordering by hand until this milestone lands. (2) Invalidation is hand-rolled per callsite (~40 calls across ~20 files: `useConnectorAuth`, `useAssistantKey`, `useScheduleController`, `Sync.tsx`, `workflow-queries.ts`, the playlist dialogs, `TrackDetail.tsx`, …) — nothing guarantees a mutation invalidates everything it staled. (3) Backend truth duplicated by hand: `web/src/lib/schedule.ts` mirrors `USER_SCHEDULABLE_TARGETS`, `web/src/test/factories.ts` mirrors `derive_status_state()` — each mirror is a silent drift trap that every v0.11.x connector widens. React TanStack Query remains v5 as of 2026-08 (v6 exists only as Svelte/Solid adapters — [releases](https://github.com/TanStack/query/releases)), so the design builds on v5's stable primitives.

**What this unlocks**: v0.11.0–3 each add a connector whose auth, checkpoint, schedule, and snapshot surfaces multiply invalidation edges — new connectors land as one tag-map entry instead of new callsites; the v0.13.x feature surfaces (library sync, collection import) inherit correct-by-default cache behavior.

**Persona**: every persona, as trust infrastructure — Curator-primary (what the app shows after an action is true without a reload); Tinkerer secondary (one legible invalidation map instead of scattered callsites).

**Key Design Decisions**:
- **One vocabulary: resource tags** — a single module maps each tag (`connectors`, `checkpoints`, `schedules`, `playlists`, …) to its generated query-key prefixes, because staleness bugs come from callsites each re-deciding which keys an action touched. Query keys are never hand-written; the map composes Orval's generated `get*QueryKey()` factories.
- **One mechanism, two triggers** — a global `MutationCache.onSuccess` reads mutation `meta.invalidates` tags (the community-standard pattern — [TkDodo, *Automatic Query Invalidation after Mutations*](https://tkdodo.eu/blog/automatic-query-invalidation-after-mutations)), and SSE operation-terminal events carry server-named tags into the same helper — so a user action and a long-running operation stale the cache through identical code. Orval's own [`mutationInvalidates`/`useInvalidate`](https://orval.dev/docs/reference/configuration/output) options were evaluated and rejected: they generate per-endpoint `onSuccess` code the SSE path can't share, splitting the mechanism in two and moving the rules into `orval.config.ts` away from the tag vocabulary.
- **Race-safe by construction** — the shared `invalidateTags()` helper owns the first-fetch-race workaround (settle the in-flight initial fetch, then invalidate) in one place, because upstream declines to change the behavior and a workaround that lives in one page recurs as a bug on every other page.
- **Coverage enforced, not hoped** — a parity test requires every generated mutation hook to have a tag entry (the frontend analogue of the chat-tool parity registry), because an unmapped mutation is exactly the silent staleness this milestone exists to end.
- **The backend owns every enum it already defines** — schedulable sync targets become a typed endpoint consumed via generated types, because a frontend mirror of a backend list turns every new connector into a two-repo edit with a silent failure mode.
- **Deferred, not adopted (as of 2026-08): TanStack DB** — its collection/live-query model would absorb the invalidation problem wholesale and its adoption curve is steep ([releases](https://github.com/TanStack/db/releases), `react-db@0.3.2` 2026-08-21), but at 0.x with frequent breaking releases and no Orval codegen path, adopting now means hand-rewiring every generated hook. The tag vocabulary this milestone builds maps directly onto DB collections, so today's work is a stepping stone, not a competing bet. **Re-evaluate when: 1.0 ships, or a codegen path (Orval or first-party) appears** — whichever comes first.
- **Deferred, not adopted (as of 2026-08): sync engines (Rocicorp Zero 1.0, ElectricSQL)** — they solve staleness by syncing Postgres to the client beneath the application layer ([Zero 1.0](https://www.infoq.com/news/2026/06/zero-version-1/)); today that bypasses the use cases and RLS that carry mixd's data-ownership guarantees. **Re-evaluate if** one grows a mode that composes with an application layer (per-user shape auth mapping onto RLS) or if mixd's realtime needs outgrow SSE. Same 2026-08 pass verified the current ground: React TanStack Query on v5 (`streamedQuery` still experimental, orthogonal to `touched`), Orval active (v8.25.0), Hey API's tags-in-query-keys is endpoint keying, not a mutation→resource map — worth re-checking each planning cycle, as all three move quickly.

#### Cache Consistency Epics

- [x] **Tag Vocabulary & Race-Safe Invalidation Core**

    **Story**: The user who completes any action wants every surface showing its result to update without a reload — today whether that happens depends on which callsite fired and which keys its author remembered, and the auth-callback redirect proved a forgotten case ships as a stale page.

    **Decisions**:
    - **Tags name resource collections, not endpoints** — because one action stales several endpoints (disconnect stales connectors *and* checkpoints *and* schedules), and the mutation author should declare what changed in the world, not enumerate URLs.
    - **Per-endpoint default tags live in one map, callsite `meta` only extends** — because DRY fails if every `useMutation` callsite still hand-declares its tags; the common case is zero callsite code.

    **Spec**:
    - `web/src/api/cache-tags.ts`: `CacheTag` union + tag → query-key-prefix map built from generated `get*QueryKey()` factories; parameterized tags (e.g. `playlist-detail:{id}`) as a typed helper
    - `invalidateTags(queryClient, tags)`: for each mapped key, if the query is on its initial in-flight fetch (`state.data === undefined` + `fetchStatus === "fetching"`) settle it first, then invalidate — the `Integrations.tsx` workaround generalized
    - `web/src/api/query-client.ts`: extend the existing typed `mutationMeta` Register (`errorLabel`/`suppressErrorToast` precedent) with `invalidates?: CacheTag[]`; add `MutationCache.onSuccess` = default-map lookup (keyed by generated mutation key, or injected via Orval's `mutationOptions` override with operation context — pick at implementation) merged with `meta.invalidates`
    - Parity test enumerating generated mutation hooks against the default map; unmapped → test failure (explicit `none` entry for mutations that genuinely stale nothing)

    **Tests**:
    - (unit) `invalidateTags` during a query's initial in-flight fetch produces a fresh network request (regression for the auth-callback race)
    - (unit) a mutation with default-map tags invalidates the mapped queries with zero callsite code; callsite `meta.invalidates` extends the defaults
    - (unit) parity test fails on a generated mutation hook with no tag entry

    Effort: M | Dependencies: None | Status: Completed (2026-08-27)

    **Completion note**: tags are *derived from the request path*, not registered per endpoint. `cache-tags.ts` holds an ordered rule table (path regex → owning family + `alsoReads` dependencies + `scoped`), and an Orval mutator stamps each generated mutation with its route template, so adding an endpoint under an existing family needs no edit here and a new path family fails the parity test until a rule exists. Declaring dependencies on the *read* side removed the need for a separate write-fanout map. `awaitInvalidation` joined `invalidates` on the meta Register: the global `MutationCache.onSuccess` runs before the mutation's own, so awaiting unconditionally would put a round trip in front of every optimistic dialog close — opt-in only where the screen must be true before the toast says so.

- [x] **Ad-Hoc Callsite Migration**

    **Story**: The user gets the same freshness guarantee from every dialog and hook — today each of ~20 files carries its own invalidation idiom, so consistency depends on which door they walked through.

    **Decisions**:
    - **Delete, don't wrap** — migrated callsites lose their `useQueryClient` + key plumbing entirely (tags ride the default map or `meta`), because a thin wrapper over the old idiom preserves the drift surface this epic removes.

    **Spec**:
    - Migrate all non-generated `invalidateQueries` callsites to tags: `pages/settings/Integrations.tsx` (delete the local `fetchQuery`-then-invalidate), `hooks/useConnectorAuth.ts`, `hooks/useAssistantKey.ts`, `hooks/useScheduleController.ts`, `pages/settings/Sync.tsx` (`CHECKPOINT_KEYS`, queue keys), `pages/settings/Tags.tsx`, `pages/TrackDetail.tsx`, `lib/workflow-queries.ts`, `components/playlist/link-queries.ts` + the playlist/track dialogs (grep `invalidateQueries` for the full census, ~40 calls at authoring time)
    - `lib/workflow-queries.ts` and `link-queries.ts` collapse into tag entries where they were only key bundles; keep any residual optimistic-update logic
    - No behavior changes beyond freshness — this is a mechanical retrofit; existing component tests are the harness

    **Tests**:
    - (component) auth-callback redirect regression: fresh mount with the connectors query mid-initial-fetch → the list re-renders from a second network response
    - (component) existing dialog/hook tests pass unchanged; spot-check one migrated flow per file cluster (connector disconnect, playlist edit, schedule save)

    Effort: M | Dependencies: Tag Vocabulary & Race-Safe Invalidation Core | Status: Completed (2026-08-27)

    **Completion note**: no non-generated `invalidateQueries` callsite remains. Two deliberate exceptions kept their own code, both documented at the callsite: `Integrations.tsx` keeps a `fetchQuery` fallback for the case where *no query object exists yet* (`invalidateQueries` would match nothing and resolve in a microtask, firing the success toast over a card that never refetched) — the existence test is the query object, not its data, since a data check sends it onto a `fetchQuery` that dedupes back onto the pre-connect request; and `writeWorkflowDetail` cancels the detail query before seeding it, because the save's own family tag necessarily matches the mounted detail and its refetch would otherwise land on top of the seeded response.

- [x] **Server-Named Invalidation for Long Operations**

    **Story**: When an import or sync finishes, the user sees checkpoints, plays, and likes surfaces refresh because the *server* names what the operation touched — today each trigger callsite guesses a key list to pass into `useOperationProgress`, and a wrong guess is invisible until someone notices stale data.

    **Decisions**:
    - **The producer declares, the subscriber applies** — the use case that did the work is the only party that knows what it staled, so terminal events carry tags and the client applies them mechanically (the server-driven cache-tag pattern CDNs converged on, e.g. [CloudFront tag-based invalidation](https://aws.amazon.com/blogs/networking-and-content-delivery/tag-based-invalidation-in-amazon-cloudfront/), applied to the client cache).
    - **Tags ride the durable run row too** — the SSE stream can drop; the poll-recovery path (`useOperationProgress` falling back to `operation_runs`) must yield the same invalidation as a live terminal frame.
    - **Unknown tags are ignored with a dev-only warning** — because a backend deploy may name a tag an older client bundle doesn't know; staleness there is the pre-existing behavior, not a crash.

    **Spec**:
    - Backend: terminal event payload gains `touched: list[str]` (`build_terminal_event` / `_push_terminal_event`, `src/interface/api/services/sse_operations.py`); tags declared per operation — `SyncTargetSpec` gains a `touches` field for sync targets, other `launch_sse_operation` callers pass theirs; persisted on the `OperationRun` terminal write; emitted on both `complete` and `error` (a failed run may have partially written)
    - Tag strings are the shared vocabulary — document the list in one place server-side; the frontend `CacheTag` union is its consumer
    - Frontend: `useOperationProgress` terminal handling calls `invalidateTags` with the frame's (or recovered row's) `touched`; the caller-supplied `invalidateKeys` option and its per-page key lists (`Sync.tsx` `CHECKPOINT_KEYS`, workflow-run callers) are removed
    - Parity (file-header contract): no new use case — envelope change only

    **Tests**:
    - (unit, backend) terminal events carry `touched` on clean completion, soft failure, and uncaught exception; the run row persists them
    - (unit, frontend) a completion frame with tags invalidates the mapped queries; the recovery-poll path invalidates identically; an unknown tag is a no-op

    Effort: M | Dependencies: Tag Vocabulary & Race-Safe Invalidation Core (frontend half; backend envelope work is independent) | Status: Completed (2026-08-27)

    **Completion note**: two design changes from the spec, both simplifications. (1) `touched` is **derived, not persisted** — `OperationRunSummarySchema.touched` is a computed property over `OPERATION_TOUCHES[operation_type]`, so there is no column, no migration, and no way for a stored list to drift from the table that defines it. (2) `SyncTargetSpec` gained `operation_type` rather than a `touches` field: a scheduled fire runs the work its target names, so its tags are that operation's plus `schedules`, derived rather than spelled. The tag table (`interface/api/schemas/cache_tags.py`) is keyed by `operation_type` because every `launch_sse_operation` caller already passes one and every `OperationRun` row already stores one — nothing needs declaring at a callsite. `test_cache_tags.py` AST-scans the API package for tag-bearing calls, so an untagged operation fails CI. Sub-operation terminals carry a deliberately narrow `PER_ITEM_TOUCHES` subset (`import-queue`), so a thirteen-file export advances the queue manifest per file without thirteen full invalidation rounds.

- [x] **Backend-Owned Enums & Labels**

    **Story**: Adding a connector (three arrive this cycle) should light up the Sync page from the backend's own registry — today `SYNC_TARGETS` in `web/src/lib/schedule.ts` must be hand-edited to match `USER_SCHEDULABLE_TARGETS`, and the `factories.ts` mirror of `derive_status_state()` can drift green while the real UI drifts wrong.

    **Decisions**:
    - **List and labels are server truth; card copy stays frontend** — the endpoint returns what is schedulable and what it's called (`sync_target_label` already exists server-side), because that's the drift-prone part; per-target descriptions and trigger wiring are UX copy the frontend legitimately owns (the `categoryDisplay` precedent in `Integrations.tsx`).
    - **Mirrors that must exist get a conformance check** — the `factories.ts` status-state mirror serves tests that construct payloads the backend would never emit piecemeal; it stays, pinned by a generated fixture so drift is a CI failure instead of a silent wrong-default.

    **Spec**:
    - `GET /api/v1/sync/targets`: `{id, label, self_managed}` rows from `USER_SCHEDULABLE_TARGETS` + the `spotify:plays` self-managed entry; `id` typed as an OpenAPI enum (Literal) so Orval emits the union type; parity (file-header contract): read → `ToolSpec`
    - Delete `SYNC_TARGETS`/`SyncTarget` from `web/src/lib/schedule.ts`; `Sync.tsx` keys its scheduler cards off the endpoint (generated hook, `STALE.STATIC`), hand-written trigger wiring per card stays
    - Status-state conformance fixture: a backend test exports the `auth_method × auth_error → status` matrix as JSON (sibling of `export-openapi`, starting point — revisit the transport); `factories.ts` reads it, a vitest contract test compares mirror output against every row
    - Sweep for further mirrors while here (bounded: report-only list in the completion note, ≤5 candidates, no extra migrations this epic)

    **Tests**:
    - (integration, backend) `/sync/targets` returns every user-schedulable target with its registry label; adding a `SyncTargetSpec` requires no frontend edit to appear
    - (component) Sync page renders a scheduler card per endpoint row; unknown-to-copy targets render with the server label rather than vanishing
    - (unit, frontend) conformance test fails when the factories mirror diverges from the exported matrix

    Effort: S | Dependencies: None | Status: Completed (2026-08-27)

    **Completion note**: `/sync/targets` types `id` off the application's own `SyncTarget` Literal rather than a wire copy, and `test_cache_tags.py` pins the literal and the registry to the same set in both directions. `Sync.tsx` consumes the endpoint for the *control* decision too, not just the list: `SyncScheduleField` branches on the server's `self_managed` to render the read-only cadence or the daily/weekly picker, which retired the parallel `pollingService` prop the page carried alongside `syncTarget`. Status-state conformance took the fixture route: `uv run export-status-matrix` (run by `pnpm sync-api`, sibling of `export-openapi`) writes the full `auth_method × auth_error × connected × expired` cross product to `web/src/test/fixtures/`, `factories.ts` *reads* it rather than re-implementing the rule, and drift is red on both sides — `test_status_matrix_export.py` if the fixture falls behind `derive_status_state()`, `status-matrix.test.ts` if the factory disagrees with a row. The expiry axis was a real gap, not just a pin: the old hand-written mirror had no `expired` branch at all, so a connected-but-aged-out connector read `connected`.

    **Mirror sweep** (report-only, no migrations this epic): `factories.ts`'s `connectorDefaults` mirrors the backend connector registry (category / auth_method / capabilities) — the next-widest trap, and the one every new connector grows; `lib/sse-types.ts` mirrors `WorkflowConstants`' event names; `lib/connectors.ts`'s `isConnectable()` re-decides which auth methods are connectable (`CREDENTIAL_AUTH_METHODS` server-side); `lib/schedule.ts`'s `describeMinutes` mirrors the CLI's `describe_minutes` (presentation, low risk). `connectorBrand` is deliberately frontend-owned UX copy, not a mirror.

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.11.5: Connector Quality Sweep

**Goal**: The connectors tree the cycle just grew to six services stops carrying six private copies of the same machinery — the next connector is adapters over shared mechanism, and imports stop paying for duplicated pacing and sequential I/O.

**Context**: A four-angle quality review (reuse, simplification, efficiency, altitude) of the whole `src/infrastructure/connectors/` tree (~22k lines) after the v0.11.0–v0.11.3 foundation ships; all findings applied in one behavior-preserving pass. Deferred findings live in [unscheduled.md § Connector Architecture Follow-Ups](unscheduled.md#connector-architecture-follow-ups-2026-08-simplify-sweep).

**What this unlocks**: v0.13.x parity work (Apple/Tidal feature surfaces) lands as thin adapters on the shared write pipeline, match strategies, and status probes instead of a seventh copy of each.

**Persona**: The **Weekly Curator** gets faster imports (concurrent Apple catalog fetches, batched Last.fm cross-discovery, no dead sleep time in playlist sync); the **Tinkerer** gets a connectors tree where each package holds only what is genuinely service-specific.

**Key Design Decisions**:
- **One request pacer**: `ConnectorRateLimiter` alone paces requests; concurrency bounds (`bounded_fan_out`) compose with it but never pace. The second pacer (batch processor) and per-call sleeps were deleted, not reconciled.
- **Shared mechanism over special cases**: divergence that is genuinely per-service (reauth rules, payload extraction, mapping primacy) became named hooks/strategy objects; everything else moved into `_shared/` once.

#### Sweep Epics

- [x] **Shared write pipeline & strategy split**

    **Story**: The user's imports behave identically across Apple, Tidal, and Spotify because identical decisions run through one code path — the ~350-line inward-resolver write pipeline that existed three times (with drift: Apple lacked Tidal's dedupe-by-current-id; Apple/Tidal mis-filed rolled-back writes as `failed`) now exists once.

    **Spec**: `_shared/inward_track_resolver.py` — `PlannedWrite[TPayload]`, `plan_isrc_write`, `WritePlanningResolver` (hooks: `_canonical_payload`, `_mapping_metadata`, `_successor_assertion`, mapping-policy pair, `_on_writes_persisted`); `write_failed` tallied by the base for every resolver. `_shared/matching_provider.py` — one workflow shell + `MatchStrategy` (`IsrcThenArtistTitle`, `IsrcOnly`, `SingleBatch`); escape hatches (`supports_artist_title_matching`, Last.fm's hand-copied shell) deleted. Per-connector `status.py` probes over one `stored_token_status(…, reauth_rule=…)` primitive; `_shared/connector_status.py` back-imports gone. Shared `oauth.py` grant/carry-forward/expiry helpers, `json_api.py` error parsing, `token_storage.load_required_access_token`.

    **Tests**: shared-pipeline ordering/dedupe/write_failed units; strategy units per shape; `stored_token_status` state matrix; per-service status/auth suites unchanged in meaning.

    Effort: L | Dependencies: — | Status: Completed (2026-08-27)

    **Completion note**: Apple resolver 491→279 lines, Tidal 589→398, Spotify 1310→1085. Two intended convergences: Apple gains Tidal's dedupe-by-current-id; collision reviews key on `current_id`. `ReauthRule` preserves the three genuinely different expiry semantics the collapsed status probes carried.

- [x] **One pacer, batch-first hot paths**

    **Story**: The user's large imports stop paying for redundant throttling and serialized I/O: Apple catalog chunks fetch concurrently under the shared limiter, Last.fm cross-discovery batches its Spotify probes and ISRC lookups, and playlist sync drops ~0.1 s of pure sleep per operation stacked on an already-paced client.

    **Spec**: `_shared/fan_out.bounded_fan_out` (BoundedSemaphore + TaskGroup, typed `except*` unwrap) adopted at seven formerly hand-rolled sites; `rate_limited_batch_processor.py` (429 lines, one caller, 10 ms poll loop) deleted; `request_delay` setting retired; MusicBrainz on the shared limiter (`rate_limit=1.0`, process-wide); `CrossDiscoveryProvider.discover_batch` replaces per-identifier `discover`; storefront memoized on the Apple client; Spotify ids parsed once per play; `_fold_onto_leader` bucketed by `identity_key`.

    **Tests**: fan-out ordering/cap/unwrap units; one-batched-call assertions for cross-discovery and ISRC prefetch; per-request failure-isolation parity.

    Effort: M | Dependencies: — | Status: Completed (2026-08-27)

- [x] **Dead surface removal**

    **Story**: The Tinkerer reading a connector package sees only load-bearing code: the metric-resolver protocol nobody called, `ConnectorConfig`'s unread fields, Spotify's 13 wrapper/`_impl` pairs and no-op log-and-reraise blocks, and the unreachable validation guards are gone; metrics register at discovery instead of import-time side effects.

    **Spec**: `register_metrics(connector: str, …)` fed by `discover_connectors()` from `ConnectorConfig["metrics"]`/`metric_freshness_hours`; `dependencies` dropped, `factory` zero-arg; `spotify/client.py` collapsed onto `_json`/`_request_json` through `_api_call`; `SpotifyOperations` double-hop removed (connector holds sync ops); `_validate_operations` returns typed `_ValidOp`s; latent `capture_logs`/`cache_logger_on_first_use` flake pinned in the PDR-003 test.

    **Tests**: discovery→registry registration path; `_ValidOp` validation; retry/quota suites retargeted to the `_request_json` seam.

    Effort: M | Dependencies: — | Status: Completed (2026-08-27)

- [x] **Review hardening & follow-ups**

    **Story**: The sweep's `/code-review` findings are fixed and its four deferred architecture items are implemented, so a new connector wires itself entirely from its own package and every service's rate budget is honestly enforced.

    **Decisions**:
    - Review fixes (6): matching-progress double-count (was failing web matching outright on ISRC-miss fallback), ISRC phase extracted to one `_run_isrc_phase`, MusicBrainz outages now classify `API_ERROR` not `NO_RESULTS` (errored codes omitted from batch results), cross-discovery's collision arm delegates to `plan_isrc_write`, savepoints narrowed to the review write (~51 → 1 per clean chunk), loop-aware profile client.
    - Capability wiring: `ConnectorConfig` gains `play_importer_factories`/`play_resolver_factory`/`play_service_name`/`cross_discovery_factory`/`on_disconnect`/`supports_play_polling`; registry tuple table and route name-checks deleted. Polling rides the declarative flag because the teardown hook is application-layer — Apple enables by flipping it once the poll policy generalizes.
    - One request pacer, config-driven: `max_concurrent`, `rate_remaining_header`+`rate_low_water` (Discogs-only brake — MusicBrainz's headers reflect a shared global bucket, documented do-not-wire), loop-keyed limiter cache; `discogs/pacer.py` deleted. Rate-less services get a pause-only limiter, closing the Tidal 429 hole.
    - ListenBrainz: full client shape in its own package (own rate budget — folding under `spotify/` would spend Spotify's); the Labs lookup contract was provably broken (missing required `release_name`, wrong response key) and is fixed with a Pydantic boundary model, batch echo-keying, and first-time live wiring, album-gated.
    - `progress_callback` dropped from `PlayResolverProtocol` — the orchestrator owns real progress; the only implementation using it emitted fake percentages.
    - Second review round (4): the newly wired Last.fm resolver chain leaked three httpx2 pools per import — `LastfmConnectorPlayResolver.aclose()` now closes the chain with owned-vs-injected semantics and the cross-discovery provider closes a factory-built connector under `owns_connector`; all 12 Spotify response-validation sites route through the shared `boundary.validated()` (`ConnectorSyncError`, matching Discogs/Tidal); duplicate `_SupportsAclose` replaced by the sanctioned `Closeable`; ListenBrainz lookup triples deduplicated.

    Effort: M | Dependencies: the three sweep epics | Status: Completed (2026-08-28)

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.11.6: Application-Layer Quality Sweep

**Goal**: The application layer stops reaching sideways for what it needs. The same four-angle review that made the connectors tree shared mechanism in v0.11.5 runs over `src/application/` — so a use case asks a port for connector facts instead of probing for methods, reads a projection instead of a whole cache, and lets Postgres serialise two writers instead of retrying after they collide.

**Context**: A four-angle quality review (reuse, simplification, efficiency, altitude) of the whole `src/application/` tree (~37k lines), with every deferred finding designed and landed in the same pass — ports over probes, projections over whole-table loads, source-level serialisation over compensating retries. Runs immediately after v0.11.5's connector sweep and completes the pair.

**What this unlocks**: `src/domain/transforms/` is import-pure, so the v0.12.0 entity spike can reason about transforms without an application dependency; the capability gate means v0.13.x parity work declares what a connector can do rather than probing for it.

**Persona**: The **Weekly Curator** stops losing a workflow run to a lock timeout when an import is running at the same time. The **Tinkerer** gets an application layer whose dependencies are declared rather than discovered.

**Key Design Decisions**:
- **Serialise at the source**: a per-user `pg_advisory_xact_lock` taken before any row lock, replacing the application-layer contention retry. The loser queues at the lock instead of inside the index, so there is no cycle for a deadlock to form on.
- **Declared capabilities over probing**: `resolve_capability()` checks a connector's declared `capabilities` then narrows via `@runtime_checkable` protocols. `hasattr`, `cast`, and hardcoded service names are gone.
- **Transforms are domain**: `metadata_transforms/` moves to `domain/transforms/` and gives up its logging — a pure transform cannot warn, so the missing-metric warning moved out to the node wrapper that already knows the config.

#### Sweep Epics

- [x] **Application-layer quality sweep (/simplify, full scope)**

    **Context**: A four-angle quality review (reuse, simplification, efficiency, altitude) of the whole `src/application/` tree (~37k lines), with every deferred finding designed and landed in the same pass — ports over probes, projections over whole-table loads, source-level serialisation over compensating retries.

    **Spec** (28 items, three implementation waves + review):
    - **New domain ports**: `ConnectorDescriptor` + `ServiceConnectorProvider.describe()`; `PlaylistRepository.is_owned_by`; `ConnectorPlaylistSummary` read model + `list_summaries_by_connector`/`find_by_identifiers`/`find_by_ids` (whole-cache `list_by_connector` deleted); `Checkpoint.list_for_user`; `count_liked_tracks_by_service`; `MetricSpec` labels in `ConnectorConfig["metrics"]`.
    - **Capability-gated resolvers**: `resolve_capability()` checks declared `capabilities` then narrows via `@runtime_checkable` protocols — `hasattr`/`cast`/hardcoded service names gone; all resolvers take `service`.
    - **Connector-agnostic likes**: `ImportLikesUseCase`/`ExportLovesUseCase` (`connector` on the command); batch `LoveTrackConnector.love_tracks` (Last.fm fans out under its limiter); one `save_track_likes_batch` per batch; persisted `operation_type`/result-name strings byte-identical.
    - **Writers serialised at the source**: per-user `pg_advisory_xact_lock` in `save_tracks` + `ingest_external_tracks_bulk`; the application-layer contention retry/backoff deleted.
    - **Transforms into domain**: `metadata_transforms/` moved to `domain/transforms/` (logging stripped; missing-metric warning moved to the node wrapper); three legacy domain modules purified of structlog.
    - **Collapses**: six play-import handlers → one `_run_two_phase` + `match`; 30 chat confirmed-envelope literals + 33 raw UUID parses → `_common` helpers; `_with_uow` → `runner.execute_use_case`; single resolution-metrics table; one `external_as_playlist` per push; `NodeType` unified; registry-derived `CONNECTOR_OPTIONS`/`SERVICE_OPTIONS`/enricher metric defs; Spotify playlist-id parsing moved onto `PlaylistConnector.parse_playlist_identifier`.
    - **Wasted work**: keyset-cursor parsing via `cursor_sort_value_to_query`; Tidal snapshot + chat read-only tool rounds fan out via TaskGroup+Semaphore; batched run-issue writes; checkpoint dashboard 8 queries → 2.

    **Tests**: every changed surface covered (unit + integration); ingest-lock serialisation proven with a two-writer integration test; chat parallelism pinned with a concurrency-tracking fake executor; persisted-string parity pinned against the live registry.

    Effort: L | Dependencies: — | Status: Completed (2026-08-31)

- [x] **Review remediation — three high-severity defects**

    **Story**: Three defects the sweep's own review found, each of which fails silently rather than loudly. A Last.fm loves export that fails every track reported success and advanced its checkpoint past the failures; sorting a playlist by "Last Played Date" crashed the whole workflow run; and every sort-direction dropdown in the workflow editor was ignored.

    **Spec**:
    - **Loves export tells the truth**: `love_tracks` returning `False` is a failure, not a skip, so it counts as an error and the run reports failure. The checkpoint became a low watermark over outstanding work — `min(updated_at)` across everything that did not export, minus a lookback — and is free to move backwards. Because `get_unsynced_likes` is an anti-join on persisted state, a watermark that is too far back costs only a larger scan; one that is too far forward loses the likes permanently.
    - **Date metrics sort**: `sort_by_external_metrics` partitions present from missing values instead of substituting `float("±inf")`, so a `datetime` metric is never compared against a float. Missing values still land at the end in both directions.
    - **Sort direction applies**: the selects persist the strings `"true"`/`"false"`, which `bool()` reads as `True` either way. `cfg_bool` now coerces them, and `route_metric_sorting` takes a parsed `reverse` instead of parsing config inside the domain layer.

    **Tests**: the watermark test asserts the checkpoint moved *backwards* past two successes to the failure's timestamp — a test asserting only "unchanged on failure" would pass against the broken code for the wrong reason.

    Effort: S | Dependencies: the sweep epic | Status: Completed (2026-09-03)

- [x] **Review remediation — connector lifecycle and boundary leniency**

    **Story**: The remaining twelve review findings, each a small silent failure. A saved workflow filtering on a tag like "drum & bass" died at run time instead of matching nothing; every Spotify play import stranded an httpx pool in the API process; a connector that failed to import at boot left a required workflow dropdown permanently empty; a tampered numeric cursor replayed page one forever; and the Last.fm status probe, the one with a real credential fallback, had no tests at all.

    **Spec**:
    - **Lenient at the read boundary**: tag filters drop values that can never be legal tags and match nothing, keeping `normalize_tag` strict on the write path. Namespaces compare case-insensitively.
    - **Every factory-built resolver owns its pool**: Spotify's play resolver gained `aclose()` with an ownership flag; Last.fm's gained `owns_cross_discovery` so a shared provider survives the first resolver's teardown.
    - **Fail loudly at boot**: connector discovery propagates `ImportError` (all connectors are first-party), and the node config registry refuses to build a required select with zero options.
    - **Cursors either bound the page or fail**: a non-string value on a datetime column raises `ValueError`, which the API maps to a client error.
    - **Diagnostics survive non-conforming bodies**: integer JSON:API `status` is coerced; MusicBrainz attributes a failure to the one code that failed and survives a whole-call error with the artist/title fallback intact; ListenBrainz pairs rows positionally per the endpoint's contract; the presented refresh token backstops a missing cached token.
    - **The capability gate is testable**: the mock connector provider declares what the real registry declares, so a gate a production connector would reject fails under test too.

    **Tests**: each fix carries a happy path and an edge case at its own layer; the Last.fm status probe has its first test file; the findings record `v0.11.6-review-findings.md` is drained and deleted at ship.

    Effort: S | Dependencies: the sweep epic | Status: Completed (2026-09-03)

#### Post-Deploy Revisions

- [x] **v0.11.6.1** (2026-09-05) — Workflow node config declared once: the executor applies declared field defaults, the validator and runtime share one "unset" rule (null / empty multi-select), every present field is type-checked, enricher dependency rules are registry metadata, and workflow nodes take the same capability gate as use cases. New `multi_select` / `task_ref` field types make `enricher.play_history` metrics and `period_days` editable and let combiners read their inputs from `upstream`. _Notes:_ Triggered by running the sweep's four-angle review over `src/application/workflows/`, then a `/code-review` of the result, which found five correctness gaps the declaration move opened (empty metrics and JSON null crashing at run time while validating clean, wrong-typed optional values silently ignored, legacy `"false"` strings rendering as ON, a hidden stale `primary_input` blocking save) and three duplicate implementations (bound formatting, tuple-to-list, the capability gate). All landed here.

### v0.11.7: Interface-Layer Quality Sweep

**Goal**: The web app stops re-deriving what the API already knows. The same four-angle review that made the connector and application trees shared mechanism in v0.11.5 and v0.11.6 runs over `web/src/` — and where a frontend special case existed only because the contract was missing a field, the field is added and the special case deleted.

**Context**: A four-angle quality review (reuse, simplification, efficiency, altitude) of the whole `web/src/` tree (231 files, ~30k lines), widened on the user's call to the backend contract changes that let bespoke frontend logic go: typed SSE payloads, per-user sync-target availability, mapping deep links, an idle queue that is a value rather than a 404, a connector-generic token route, and node I/O types. Completes the three-sweep series.

**What this unlocks**: SSE payloads are generated types, so v0.12.x surfaces (artist/album runs) get typed progress without new casts; the connect-flow and sync-target gating are declarative, so a seventh connector is registry entries, not `if (name === …)` branches across three pages.

**Persona**: The **Weekly Curator** sees the Sync page refuse a run the server would refuse, with the reason and the fix, and a playlist import that keeps every pick made under an earlier search. The **Tinkerer** gets a frontend whose per-connector knowledge lives in one table and whose contract drift is a build error.

**Key Design Decisions**:
- **Contract over bandaid**: where the frontend guessed (connector deep links, sync-target readiness, node handle visibility, idle-queue-as-404), the API now says. `/sync/targets` carries `service`/`available`/`blocked_reason` computed by the same predicate the trigger routes 409 on, so the two cannot disagree.
- **Typed at the source**: SSE events are Pydantic models built at the emitters and merged into `openapi.json` by the export script; the wire keys are byte-identical. Terminal frames go through one best-effort push so validation can never strand a `finally`.
- **One branch, one primitive**: `ResponsiveTable` measures its container and mounts one layout; selection, run-completed toasts, import operations, library filters, and container queries are hooks with a single implementation each.
- **Zero inline suppressions**: every `noqa`/`ruff:ignore` in `src/` resolved at the source; the ratchet baseline is 0.

#### Sweep Epics

- [x] **Web quality sweep (/simplify web/, full scope)**

    **Context**: Four read-only reviewers (reuse, simplification, efficiency, altitude) over `web/src/`; 58 findings, all applied except the ones the plan records as skipped (settle-in-`invalidateTags`, the cache-tag regex widening, the Playlists connector dropdown, folding Apple's token POST, and virtualising the sortable track editor — dnd-kit needs every row mounted).

    **Spec**:
    - **Shared primitives**: `useSelectionSet` (with a `visibleIds` scope), `useRunCompletedToast`, `useContainerQuery`, `useLibraryFilters`, `useImportOperation`, `useTokenConnect`, `BulkSelectionBar`, `lib/operation-types.ts`; `useIsMobile` on `useSyncExternalStore`; `useNow` pauses when hidden; `ResponsiveTable` mounts one branch.
    - **Typed SSE**: `lib/sse-types.ts` is a discriminated union over the generated payloads; `useOperationSSE` delivers typed events; 73 casts removed across the reducers; `mergeSubOpRecord` replaces three record literals; chat streams through the shared `connectToSSE` transport (POST body, no handshake deadline).
    - **Declarative connectors**: `ConnectorCard` dispatches connect flows by `auth_method` through per-kind components; the token dialog is connector-generic with provider copy in `lib/connectors.ts`; the Sync page renders off `/sync/targets` plus the live connector probe; `MappingList` reads `external_url`.
    - **Simplification**: the playlist picker split into row components (675 → 376 lines); five Sync import components onto one hook; Library filter props 18 → 7; four effect-synced states derived; the filter panel follows "auto-open when filters become active, never auto-close" via adjust-state-on-prop-change.
    - **Efficiency**: memoised node-status maps, play-feed derivations and playlist scans; `useOperationRunRow` on the shared list key with `select`; one `useNodeSchemas` per canvas; `Promise.all` for the MusicKit setup.
    - **Reuse**: `TableCard`, `QueryErrorState` (404 special case), `getConnectorLabel`, `ListRowsSkeleton`, `pluralize`, `formatCount`, `formatDateTime`, `formatRate`/`formatEta`, `getStatusConfig`, `setFilter`, `connectErrorMessage` at every bypass site.

    **Tests**: every new hook and component has happy-path plus edge tests; consumer suites updated to the generated contract; the Vitest setup stubs `ResizeObserver` (desktop width) and `DOMMatrixReadOnly` so single-branch tables and React Flow render.

    Effort: L | Dependencies: — | Status: Completed (2026-09-05)

- [x] **Contract fixes — the API says what the frontend used to guess**

    **Story**: Six places where the frontend carried knowledge the backend owned. Each got a field or a route instead of a branch.

    **Spec**:
    - **SSE payload schemas**: nine Pydantic models in `schemas/sse_events.py` (`SseFinalStatus` aliases `RunStatus`; `SseSubOperationOutcome` aliases the `SubOperationOutcome` constant), constructed at the emitters, exported via `scripts/export_openapi.py` in serialization mode; `push_terminal_best_effort` at all seven terminal push sites.
    - **`/sync/targets` per user**: `SyncTargetSpec.service`/`required_scopes`; `connector_access.py` holds the one verdict predicate behind `require_sync_target_access` (the five import trigger routes) and the list's `available`/`blocked_reason`; tokens loaded in one batched `load_tokens`; `private, no-cache`.
    - **Mapping and playlist URLs**: `track_url`/`playlist_url` registry hooks read through `_shared/external_urls.py`; `ConnectorMappingSchema.external_url`; `ConnectorCatalog.playlist_url` replaces a fabricated `https://{connector}.com/playlist/…`.
    - **Idle queue is a value**: `GET /imports/spotify/history/queue` returns `{queue: … | null}`, `private, no-cache`; POST/DELETE return the bare `ImportQueueSchema`.
    - **Generic token route**: `PUT /connectors/{service}/token` dispatches through the registry's `validate_token` hook (Discogs declares it); the Discogs-specific route is gone.
    - **Node I/O**: `NodeTypeInfoSchema.category` is the `NodeType` union; `input_type`/`output_type` exposed; editor handles derive from them with the prefix rule as the pre-load fallback.
    - **Playlist-link scoping**: the four `/playlists/{playlist_id}/links/{link_id}` routes 404 when the link belongs to a different playlist; CLI/chat callers pass no scope.

    **Tests**: emitter payloads validate against their models; export merge is idempotent and collision-checked; sync-target verdict equals the route gate for every target; token-storage batch read is tenant-scoped; every contract change has an integration test and a pinning test for its vocabulary.

    Effort: M | Dependencies: the sweep epic | Status: Completed (2026-09-05)

- [x] **Review remediation — two review passes and a `/code-review`**

    **Story**: Bugs the sweep introduced or surfaced, all landed before ship: a stale-cache window on `/sync/targets`, terminal-event validation raising inside a `finally`, the picker dropping selections hidden by the filter, `formatEta` printing "1m 60s", cancelled sub-operations counted as succeeded, a pool fan-out on the targets endpoint, the Sync gate losing token liveness, the Library panel re-opening on search keystrokes, chat inheriting a 30 s handshake timeout, and an order-dependent structlog capture leak (module loggers bind at import; `setup_logging()` in one test blinded `capture_logs()` on the same worker — the test harness now restores the config per test and never caches loggers).

    **Spec**: each fix at the mechanism (one best-effort push helper, one verdict predicate, one selection hook option, one adjust-state idiom) with a regression test that fails on the pre-fix code; `src/` inline ruff suppressions 13 → 0 with real fixes (loopback default host, seedable shuffle `rng`, named credential kinds, abstract reuse hook) and one per-file `unused-async` exemption matching the existing httpx precedent.

    Effort: S | Dependencies: the sweep epic | Status: Completed (2026-09-06)

#### Post-Deploy Revisions

_None yet._
