# v0.6.x: Multi-User Data Isolation

For strategic overview, see the [planning overview](README.md).

**Supersedes**: The former v1.0.x "User Authentication System" epic (deleted). Neon Auth already handles authentication (JWT validation, session management, email allowlist). This milestone is purely about **data isolation** and **per-user service connections** — the missing piece that prevents sharing the deployed instance.

**Multi-Layer Isolation Strategy**: Application-level filtering (WHERE clauses in repositories) is the primary isolation mechanism. PostgreSQL Row-Level Security (RLS) is the defense-in-depth safety net — even if a repo method misses a WHERE clause, the database itself won't return other users' rows. The implementation is phased: v0.6.0 creates RLS policies (inert), v0.6.2 sets `app.user_id` session variable per-transaction via `SET LOCAL` + contextvar (RLS active but table owner bypasses), v0.6.4 applies `FORCE ROW LEVEL SECURITY` (RLS enforced on table owner, after isolation tests pass). Uses portable PostgreSQL features (`current_setting()`, `SET LOCAL`) — no extensions required.

---

### v0.6.0: Schema + User Identity Foundation
**Goal**: Add `user_id` columns to all user-owned tables and establish the `CurrentUser` dependency for extracting identity from Neon Auth JWTs.

**Context**: Currently all 18 database tables are single-user. `BusinessLimits.DEFAULT_USER_ID = "default"` is hardcoded throughout. The Neon Auth middleware already validates JWTs and attaches `scope["auth_user"]` with claims including `sub` (user ID) and `email`, but no route handler reads these claims. This phase adds the schema foundation and identity extraction without changing application behavior.

**What this unlocks**:
- Data written from this point forward is tagged with the real user ID
- Foundation for repository filtering (v0.6.2) and per-user OAuth (v0.6.3)
- Existing `'default'` data can be claimed by a real user via backfill (v0.6.4)

**Persona**: Weekly Curator — invisible change (behavior unchanged), but data is now tagged correctly for future multi-user enforcement.

**Key Design Decisions**:
- **No local `users` table** — Neon Auth manages user identities externally. We only need the JWT `sub` claim as a string foreign key. No user CRUD, no password management, no session table.
- **`user_id` is a string** — matches Neon Auth JWT `sub` claim format (UUID string). Not an integer FK to a local users table.
- **Auth disabled (local dev) = `DEFAULT_USER_ID`** — when `NEON_AUTH_URL` is empty, `get_current_user_id()` returns `"default"`. Local dev operates as single-user seamlessly.

**Data Ownership Model**:

| Category | Tables | user_id column? |
|----------|--------|-----------------|
| **Shared** (canonical metadata cache) | `connector_tracks`, `track_metrics`, `connector_playlists` | No |
| **User-scoped** (owned data) | `tracks`, `track_mappings`, `match_reviews`, `track_likes`, `track_plays`, `playlists`, `workflows`, `oauth_tokens`, `user_settings`, `connector_plays` | Yes (added) |
| **Transitively scoped** (FK to user-scoped parent) | `playlist_tracks`, `playlist_mappings`, `workflow_runs`, `workflow_run_nodes`, `workflow_versions` | No (scoped via FK) |
| **Already has user_id** | `sync_checkpoints` | Exists |

#### Schema Foundation Epics

- [x] **Alembic Migration: Add user_id Columns**
    - Effort: S | Status: Completed
    - Migration 007: `user_id VARCHAR NOT NULL DEFAULT 'default'` on 10 tables, user-scoped unique constraints, RLS policies (inert until v0.6.2)

- [x] **DB Model Updates**
    - Effort: S | Status: Completed
    - `user_id: Mapped[str]` on 10 SQLAlchemy model classes, `__table_args__` updated

- [x] **Domain Entity user_id Fields**
    - Effort: S | Status: Completed
    - `user_id: str = "default"` on `Track`, `TrackLike`, `TrackMapping`, `MatchReview`, `TrackPlay`, `ConnectorTrackPlay`, `Playlist`, `Workflow`

- [x] **CurrentUser API Dependency**
    - Effort: XS | Status: Completed
    - `get_current_user_id()` in `src/interface/api/deps.py` — reads JWT `sub` claim, falls back to `DEFAULT_USER_ID`

- [x] **UUIDv7 Primary Keys**
    - Effort: L | Status: Completed
    - Replaced all 19 tables' sequential integer PKs with application-generated UUIDv7 (RFC 9562)
    - Migration 008: 5-phase conversion (add UUID cols, populate FKs via JOIN, drop old constraints, rename cols, re-create constraints)
    - Domain entities: `id: UUID = field(factory=uuid7)` — entities born with identity, `with_id()` removed
    - `version: int = 0` field on Track for persistence guard (`require_database_tracks()` checks `version > 0`)
    - All layers updated: domain, DB models, repos, API schemas, routes, CLI, frontend types, 2231 tests passing

---

### v0.6.0.post1: Neon Auth Integration Fix
**Goal**: Fix the complete auth flow — sign-in, JWT validation, post-login navigation — so users can actually log in and use the deployed app.

**Context**: The v0.6.0 auth gate was designed for a server-rendered app (protecting all routes, redirecting browsers to /login). With an SPA architecture, the server should only protect API routes — page-level auth is the React client's job. Additionally, the JWT validation was misconfigured (wrong algorithm, missing audience/issuer validation), and the AuthView component lacked client-side navigation integration.

**Key reference**: [Neon Auth + React + Hono guide](https://neon.com/guides/react-neon-auth-hono) — the canonical pattern for React SPA with custom backend. Token retrieved via `authClient.getSession()` → `data.session.token`.

#### Auth Integration Epics

- [x] **JWT Bearer Token Attachment**
    - Effort: M | Status: Completed (2026-03-29)
    - What: Use `authClient.getSession()` to get the session JWT and attach as Bearer token to all API requests via `customFetch`
    - Notes: Session cookies live on Neon Auth's domain, never reach our backend. `getSession()` is the primary cached/deduplicated path — the SDK stores the JWT from the `set-auth-jwt` response header in `session.token`. Initial approach used `authClient.token()` (JWT plugin endpoint) which was unreliable for concurrent requests.

- [x] **Auth Gate: API-Only Protection**
    - Effort: S | Status: Completed (2026-03-29)
    - What: Flip auth gate from "exempt list" to "protected prefix" — only `/api/` routes require Bearer tokens. SPA shell, assets, and auth callbacks pass through.
    - Notes: Removed `_wants_html`, `_send_redirect`, 302 browser redirect logic. Page-level auth handled by React `AuthGuard` component.

- [x] **EdDSA Algorithm + Audience/Issuer Validation**
    - Effort: S | Status: Completed (2026-03-29)
    - What: Accept EdDSA (Ed25519) — Neon Auth's current default signing algorithm. Validate `aud` and `iss` claims against auth service origin per Neon JWT verification docs.
    - Notes: RS256 kept for backward compatibility. Previously hardcoded `algorithms=["RS256"]` which rejected all Neon Auth tokens. `Invalid audience` error was the second blocker.

- [x] **React Router Integration for Auth UI**
    - Effort: S | Status: Completed (2026-03-29)
    - What: Pass `navigate` and `Link` from React Router to `NeonAuthUIProvider` per Neon Auth UI Components docs. Moved provider inside `BrowserRouter`. Added `useAuthenticate()` check in Login page.
    - Notes: Without router integration, `AuthView` redirects via `window.location.href` which triggers a server round-trip → auth gate 302 → redirect loop. Client-side navigation stays within the SPA.

- [x] **Confirm Password + Autofill CSS**
    - Effort: XS | Status: Completed (2026-03-29)
    - What: Enable `credentials.confirmPassword` on `NeonAuthUIProvider`. Add `:-webkit-autofill` CSS overrides for 1Password/browser compatibility.

- [x] **Deploy Infrastructure**
    - Effort: S | Status: Completed (2026-03-29)
    - What: Build hash (`__BUILD_HASH__`) + version (`__APP_VERSION__`) injected at build time via Vite `define`. CI passes `BUILD_HASH=$GITHUB_SHA` as Docker build arg. `deploy` shell function pushes + watches CI. Pinned `superfly/flyctl-actions@1.5`.

---

### v0.6.1: Account Management
**Goal**: Give authenticated users a way to view their profile, sign out, and delete their account — completing the user-facing auth lifecycle.

**Context**: Neon Auth handles sign-up and sign-in, but the app had no way to view your profile, log out, or self-service delete your account. This milestone adds the Account settings page — the user-facing counterpart to the auth infrastructure. Account deletion uses a GitHub-style "type DELETE to confirm" safety gate, following 2026 UX best practices and GDPR right-to-erasure principles. Independent of the data isolation work (v0.6.2–v0.6.4) — only requires the auth foundation from v0.6.0.

**What this unlocks**:
- Users can see who they're logged in as
- Clean sign-out flow (no manual cookie clearing)
- Self-service account deletion (GDPR Article 17 compliance path)
- Settings nav completes the auth lifecycle: sign up → sign in → manage → sign out / delete

**Persona**: All personas — account management is table-stakes for any authenticated app.

**Key Design Decisions**:
- **Custom UI over Neon Auth pre-built components** — Neon Auth exports `DeleteAccountCard` and `AccountSettingsCards`, but the built-in delete dialog only supports password confirmation (not type-to-confirm). Custom UI also ensures consistency with the dark editorial aesthetic and existing settings page patterns.
- **Dialog composes `ConfirmationDialog`** — the type-to-confirm input and user identity card are passed as `children` to the existing shared dialog, with `disabled` prop gating the confirm button. Parent owns side effects (`authClient.deleteUser()`, navigation, toasts).
- **Auth-gated navigation** — the Account nav item and route are conditionally rendered based on `authEnabled`. Local dev (no `VITE_NEON_AUTH_URL`) sees no Account page.
- **No backend changes** — profile data comes from Neon Auth's JWT claims (`useAuthenticate()` hook). Sign-out and account deletion use `authClient.signOut()` and `authClient.deleteUser()` directly. Backend data cleanup on deletion is a follow-up.

#### Account Management Epics

- [x] **Account Settings Page**
    - Effort: S | Status: Completed (2026-03-28)
    - What: `/settings/account` page with Profile section (avatar, name, email, sign out button) and Danger Zone section (delete account card with red left-border accent)
    - Why: Users need to see their identity and access account actions
    - Notes:
        - `web/src/pages/settings/Account.tsx` — uses `useAuthenticate()` for session data, `authClient` for sign-out/delete
        - `UserAvatar` inline component: image with fallback to initials (e.g., "AL" for Ada Lovelace)
        - Skeleton loader while session is pending, consistent with Integrations/Sync pages
        - Card styling matches existing settings pages: `rounded-xl border border-border bg-surface-elevated shadow-elevated p-5`

- [x] **Type-to-Confirm Account Deletion Dialog**
    - Effort: S | Status: Completed (2026-03-28)
    - What: `DeleteAccountDialog` composing `ConfirmationDialog` — user must type "DELETE" to enable the confirm button
    - Why: Account deletion is the most destructive action in the app; requires deliberate friction beyond a simple click
    - Notes:
        - `web/src/components/shared/DeleteAccountDialog.tsx` — composes `ConfirmationDialog` with `children` (user identity card + text input) and `disabled` prop
        - User identity card shows name + email in `bg-surface-sunken` panel — confirms which account
        - Confirm button disabled until input exactly matches "DELETE" (case-sensitive)
        - Side effects owned by parent (`Account.tsx`): `authClient.deleteUser()` → toast → navigate to `/auth/sign-in`
        - Error handling: toast error message, dialog stays open for retry

- [x] **Settings Navigation + Routing**
    - Effort: XS | Status: Completed (2026-03-28)
    - What: "Account" nav item in Sidebar (gated by `authEnabled`) + lazy-loaded route in App.tsx
    - Why: Account page must be discoverable and properly routed
    - Notes:
        - `Sidebar.tsx`: `CircleUser` icon, conditional spread in `navItems` children array
        - `App.tsx`: `{authEnabled && <Route path="settings/account" ... />}` — catch-all redirect when auth disabled
        - Lazy import matches existing pattern for all settings pages

- [x] **Account Page Tests**
    - Effort: XS | Status: Completed (2026-03-28)
    - What: 9 Vitest tests covering skeleton, profile display, avatar variants, sign-out, dialog gating, deletion flow, error handling
    - Why: Auth-adjacent features need comprehensive test coverage — especially the deletion safety gate
    - Notes:
        - Mock pattern: `vi.hoisted()` + `vi.mock()` for `useAuthenticate` and `authClient` (same as `AuthGuard.test.tsx`)
        - Tests confirm: lowercase "delete" keeps button disabled, only exact "DELETE" enables it
        - All 409 frontend tests pass, Biome lint + TypeScript typecheck clean

---

### v0.6.2: Repository + Use Case Scoping
**Goal**: Thread `user_id` through the full request lifecycle — route handlers extract it, use cases pass it to repos, repos filter queries by it. After this phase, users see only their own data.

**Context**: The schema has `user_id` columns (v0.6.0) but nothing enforces isolation yet. This phase adds `user_id` parameters to repository protocols, implements filtering in all repo queries, threads `user_id` through use case Commands, and injects it from route handlers. This is the largest phase — it touches all layers of the architecture — but each individual change is mechanical.

**What this unlocks**:
- User A cannot see User B's playlists, likes, plays, or workflows
- Dashboard stats are per-user
- Workflow execution operates on the current user's data only
- Sharing the deployed instance with friends becomes safe

**Persona**: All personas — data isolation is the prerequisite for any multi-user scenario. The Weekly Curator's data is protected when friends sign up.

**Key Design Decisions**:
- **Reads filter by user_id, writes carry it on the entity** — read methods (`list_playlists`, `get_all_liked_tracks`) take an explicit `user_id` parameter. Write methods (`save_playlist`) get user_id from the entity's field (set by the use case).
- **ID-based lookups verify ownership** — `get_playlist_by_id(id, user_id)` returns 404 if the playlist belongs to a different user. Prevents IDOR via guessed IDs.
- **Template workflows use NULL user_id** — seeded templates (`is_template=True`) have `user_id=None` and are visible to all users. `list_workflows(user_id)` returns `WHERE user_id = ? OR user_id IS NULL`.
- **CLI always uses DEFAULT_USER_ID** — the CLI is the instance owner's tool. No JWT context available.
- **list_tracks liked_track_ids** — the `TrackRepository.list_tracks()` method joins to `track_likes` for the `liked_track_ids` set. This subquery must be scoped to the current user.
- **Defense-in-depth with RLS** — Application-level WHERE clauses are the primary isolation mechanism. PostgreSQL RLS policies (created in v0.6.0) act as a safety net via `SET LOCAL app.user_id` at transaction start. If a repo query forgets the filter, RLS catches it. Standard practice in multi-tenant PostgreSQL applications.

#### Data Isolation Epics

- [x] **User Context Infrastructure (contextvar + RLS session variable)**
    - Effort: S
    - What: `ContextVar[str]` + `user_context()` context manager + SQLAlchemy `after_begin` event that calls `SET LOCAL app.user_id` per transaction
    - Why: Defense-in-depth — even if a repository method forgets a WHERE clause, PostgreSQL RLS catches it
    - Dependencies: Alembic Migration (v0.6.0 — RLS policies exist), CurrentUser API Dependency (v0.6.0)
    - Status: Completed (2026-03-29)
    - Notes:
        - New file: `src/infrastructure/persistence/database/user_context.py`
        - Event listener on `sync_session_class` in `create_session_factory()`, uses `connection.execute()` (SQLAlchemy 2.0.17+)
        - `set_config('app.user_id', :uid, true)` — transaction-scoped, safe with Neon PgBouncer

- [x] **Repository Protocol Changes**
    - Effort: M
    - What: Add `user_id: str` keyword parameter to all protocol methods that touch user-scoped tables
    - Why: Protocols define the contract — creates compile-time cascade via basedpyright
    - Dependencies: Domain Entity user_id Fields (v0.6.0)
    - Status: Completed (2026-03-29)
    - Notes:
        - 8 protocols updated in `src/domain/repositories/interfaces.py`
        - Read methods gain explicit `*, user_id: str` keyword; write methods rely on entity's `user_id`
        - ID-based lookups verify ownership (404 for wrong user — IDOR prevention)

- [x] **Repository Implementation Scoping**
    - Effort: L
    - What: Add `.where(Model.user_id == user_id)` filters to all repository query methods
    - Why: Core data isolation enforcement — primary mechanism
    - Dependencies: Repository Protocol Changes, DB Model Updates (v0.6.0)
    - Status: Completed (2026-03-29)
    - Notes:
        - 10+ repository files: likes, plays, tracks, playlists, workflows, stats, settings, match reviews, connectors
        - Stats repo: every sub-select scoped (5 queries, each with 1-4 sub-selects)
        - Playlist mappings scoped via JOIN through user-scoped parent
        - Template workflows: `WHERE user_id = :uid OR user_id IS NULL`

- [x] **Use Case Threading**
    - Effort: L
    - What: Add `user_id: str` as first field to all Command attrs classes, thread through to repository calls
    - Why: Use cases orchestrate between route handlers and repos — user context must flow through
    - Dependencies: Repository Protocol Changes
    - Status: Completed (2026-03-29)
    - Notes:
        - ~25 use case files + application services + workflow nodes + connector resolvers
        - Workflow execution: `user_id` flows via `create_workflow_context(user_id=...)` → `_with_uow()` wraps in `user_context()` for RLS
        - `sync_likes.py` updated from `DEFAULT_USER_ID` to accept real user_id
        - `import_play_history.py`: renamed LastFM `user_id` to `username` to avoid collision
        - Dual mechanism: Commands carry explicit user_id (primary). ContextVar feeds `after_begin` for RLS (secondary).

- [x] **Route Handler Injection**
    - Effort: M
    - What: Inject `user_id: str = Depends(get_current_user_id)` in route handlers, pass to Commands + `execute_use_case()`
    - Why: Route handlers are the entry point where user identity enters the system
    - Dependencies: CurrentUser API Dependency (v0.6.0), Use Case Threading
    - Status: Completed (2026-03-29)
    - Notes:
        - 7 route files: tracks, playlists, workflows, stats, imports, settings, reviews
        - `execute_use_case()` gains `user_id: str | None` — wraps in `user_context()` when provided
        - Background tasks capture `user_id` in closures (no ContextVar propagation across `create_task()`)
        - CLI commands keep `BusinessLimits.DEFAULT_USER_ID`
        - Not in scope: `routes/connectors.py` (v0.6.3), `routes/operations.py` (operation-scoped)

- [x] **Optimistic Concurrency on Tracks**
    - Effort: S
    - What: Add `version` column + optimistic locking — `UPDATE ... WHERE version = :expected`
    - Why: Concurrent edits to the same track could silently overwrite each other
    - Dependencies: Repository Implementation Scoping
    - Status: Completed (2026-03-30)
    - Notes:
        - Version column already existed (migration 009, `DBTrack.version` default=1, `Track.version` default=0)
        - Restructured `save_track()`: values dict built before version branch, update path uses `WHERE id = :id AND version = :expected` with `SET version = version + 1`
        - `OptimisticLockError(DomainError)` with `entity_id: UUID` and `expected_version: int` — mapped to 409 `OPTIMISTIC_LOCK_CONFLICT` in middleware
        - Insert/upsert path (version=0) unchanged — connector imports are merge operations, not concurrent edits
        - 3 integration tests: version increment, stale version rejection, insert path unaffected

---

### v0.6.3: Per-User OAuth
**Goal**: Each user connects their own Spotify and Last.fm accounts. OAuth tokens are stored per-user, and connector operations use the current user's credentials.

**Context**: Currently `oauth_tokens` has `UNIQUE(service)` — one Spotify token, one Last.fm token, per database. A second user literally cannot authenticate with Spotify because the INSERT would conflict. This phase makes token storage user-scoped and threads user context through the connector auth flows.

**What this unlocks**:
- Each user connects their own Spotify/Last.fm accounts
- Each user sees their own connection status on the Integrations page
- Import operations use the current user's credentials
- Friends can sync their own liked tracks and play history

**Persona**: All personas — connecting your own service accounts is the minimum for a usable shared instance.

**Key Design Decisions**:
- **User ID in CSRF state** — the OAuth callback needs to know which user initiated the flow. Encode user_id in the CSRF state token (the `state` parameter), or recover it from the session cookie present on the redirect.
- **Session cookie preferred for callback** — Neon Auth's session cookie is present on the callback redirect. The middleware can extract user identity from it. This avoids encoding user_id in the state parameter (which could be tampered with).
- **Token storage uses standalone sessions** — `DatabaseTokenStorage` creates its own short-lived sessions (not UoW) because token refresh happens inside httpx auth flows with no UoW context. This pattern stays the same, just with an added `user_id` parameter.

#### Per-User OAuth Epics

- [ ] **User-Scoped Token Storage**
    - Effort: M
    - What: Add `user_id` parameter to `DatabaseTokenStorage` methods, update upsert conflict key
    - Why: Core change that enables per-user service credentials
    - Dependencies: Alembic Migration (v0.6.0) — `oauth_tokens.user_id` column and `UNIQUE(user_id, service)` constraint
    - Status: Not Started
    - Notes:
        - `load_token(service, user_id)` — add `.where(DBOAuthToken.user_id == user_id)`
        - `save_token(service, user_id, token_data)` — add `user_id` to values dict
        - `delete_token(service, user_id)` — add `.where(DBOAuthToken.user_id == user_id)`
        - Upsert conflict key: `index_elements=["user_id", "service"]` replaces `["service"]`
        - File: `src/infrastructure/persistence/repositories/token_storage.py`
        - Update `StoredToken` TypedDict and `TokenStorageProtocol` to include user_id
        - File: `src/infrastructure/connectors/_shared/token_storage.py`
        - **RLS compatibility**: `DatabaseTokenStorage` creates standalone sessions via `get_session()` (not UoW). These sessions don't go through the `after_begin` event that sets `app.user_id`. Wrap each `get_session()` call in `user_context(user_id)` and register the `after_begin` event on all session factories (not just UoW sessions) to ensure RLS works when FORCE is enabled (v0.6.4).

- [ ] **OAuth Callback User Association**
    - Effort: M
    - What: Extract user ID from session cookie in OAuth callback handlers and pass to token storage
    - Why: Callback must know which user to store the token for
    - Dependencies: User-Scoped Token Storage
    - Status: Not Started
    - Notes:
        - File: `src/interface/api/routes/auth.py`
        - `spotify_callback` and `lastfm_callback` need user context
        - Option A: Encode user_id in CSRF state parameter (decode in callback)
        - Option B: Extract from Neon Auth session cookie (present on redirect)
        - Option B preferred — session cookie is tamper-proof (validated by Neon Auth)
        - Auth URL generation (`get_spotify_auth_url`, `get_lastfm_auth_url`) should also be user-aware for logging/audit
        - CSRF state persistence: currently in-memory dict — consider moving to DB for multi-worker safety

- [ ] **Connector Auth Flow User Context**
    - Effort: M
    - What: Thread user_id through `SpotifyTokenManager`, `get_token_storage()`, connector initialization
    - Why: Connectors need to load/refresh the correct user's token
    - Dependencies: User-Scoped Token Storage
    - Status: Not Started
    - Notes:
        - `SpotifyTokenManager` — accepts `user_id` for token lookup and refresh
        - `SpotifyBearerAuth.async_auth_flow` — uses user_id to load token
        - `get_token_storage()` factory — may need user_id parameter or use a user-scoped factory
        - `_shared/connector_status.py` — `get_connector_statuses()` needs user_id to check per-user tokens
        - Connector initialization in `DatabaseUnitOfWork` — connectors need user context for token loading
        - `LastFMAPIClient` — `_get_session_key()` needs user_id to load correct session key
        - File: `src/infrastructure/connectors/spotify/auth.py`
        - File: `src/infrastructure/connectors/_shared/connector_status.py`
        - File: `src/infrastructure/persistence/unit_of_work.py`

- [ ] **Service Token Encryption at Rest**
    - Effort: S
    - What: Encrypt sensitive fields in `oauth_tokens` (access_token, refresh_token, session_key) before writing to the database; decrypt on read
    - Why: These fields are stored as plaintext. A database compromise (leaked backup, SQL injection, Neon breach) gives direct access to users' Spotify and Last.fm accounts. The tokens are long-lived (Spotify refresh tokens never expire; Last.fm session keys are permanent).
    - Dependencies: User-Scoped Token Storage
    - Status: Not Started
    - Notes:
        - Sensitive fields: `access_token`, `refresh_token`, `session_key`
        - Non-sensitive fields (leave plaintext): `service`, `token_type`, `expires_at`, `scope`, `account_name`, `extra_data`
        - Encryption key from environment variable — key rotation and algorithm are implementation details
        - `DatabaseTokenStorage.load_token()` and `save_token()` are the only read/write paths
        - Consider: migration path for existing plaintext tokens (detect and re-encrypt on read)

- [ ] **OAuth State Durability**
    - Effort: S
    - What: Make the CSRF state store (`_csrf_states` in `routes/auth.py`) survive server restarts and work across multiple workers
    - Why: Currently an in-memory Python dict with 5-minute TTL. If the server restarts mid-OAuth flow, the callback fails with "invalid state." Multi-worker uvicorn (if ever needed) would route the callback to a different worker that doesn't have the state.
    - Dependencies: OAuth Callback User Association
    - Status: Not Started
    - Notes:
        - Current implementation: `_csrf_states: dict[str, float] = {}` — lost on restart, per-worker only
        - The state only needs to live ~5 minutes (OAuth round-trip time)
        - Single-worker Fly.io deployment means cross-worker isn't urgent, but restart durability is (Fly.io restarts VMs for scaling/maintenance)
        - Natural to address alongside callback rework since both touch the same code

---

### v0.6.4: Testing + Data Claim
**Goal**: Comprehensive multi-user isolation tests, update all existing test fixtures, and provide a mechanism for the instance owner to claim existing `'default'` data.

**Context**: After v0.6.0–v0.6.3, the application is multi-user capable. This phase ensures correctness through testing and handles the migration of existing data from `user_id='default'` to the real user.

**What this unlocks**:
- Confidence that data isolation actually works (not just "user_id is in the WHERE clause")
- Existing 15k tracks of imported data claimed by the instance owner's real Neon Auth user ID
- Clean test fixtures that work with multi-user semantics

**Persona**: Weekly Curator — their existing data seamlessly transfers to their real account on first login.

**Key Design Decisions**:
- **Two-user isolation tests** — the gold standard for data isolation testing. Insert as user_a, query as user_b, verify empty. For every scoped repository method.
- **Data claim on first login** — a one-time operation that updates all `user_id='default'` rows to the first real user's ID. Could be an admin API endpoint, a CLI command, or automatic on first authenticated request.
- **Template workflows stay NULL** — seeded templates are not claimed. Their `user_id` remains NULL (or a system constant).

#### Testing & Migration Epics

- [ ] **Multi-User Isolation Tests**
    - Effort: L
    - What: Integration tests verifying data isolation for every scoped repository method
    - Why: Data leaks between users are the worst possible bug — tests must prove isolation works
    - Dependencies: v0.6.2 (Repository Scoping)
    - Status: Not Started
    - Notes:
        - Pattern: create data as `user_a`, query as `user_b`, assert empty/404
        - Test every scoped repo: likes, plays, playlists, workflows, settings, stats
        - Test shared tables: both users see same canonical tracks, connector_tracks, mappings
        - Test template workflows: visible to all users
        - Test ownership verification: `get_playlist_by_id(other_users_id, my_user_id)` returns 404
        - Test dashboard aggregates: user_a's stats reflect only user_a's data
        - Test OAuth token isolation: user_a's Spotify token not returned for user_b

- [ ] **Enable FORCE Row-Level Security**
    - Effort: XS
    - What: Alembic migration adding `ALTER TABLE {table} FORCE ROW LEVEL SECURITY` to all 8 user-scoped tables
    - Why: Without FORCE, the table owner role (which the application uses on Neon) bypasses RLS policies. FORCE makes RLS apply even to table owners, completing the defense-in-depth safety net. Only safe to enable after isolation tests confirm application-level filtering works correctly.
    - Dependencies: Multi-User Isolation Tests (must pass first), User Context Infrastructure (v0.6.2)
    - Status: Not Started
    - Notes:
        - Tables: `track_likes`, `track_plays`, `playlists`, `workflows`, `oauth_tokens`, `user_settings`, `connector_plays`, `sync_checkpoints`
        - Verify Alembic migrations still work (DDL like `ALTER TABLE` is not subject to RLS USING policies)
        - Test that `SET LOCAL app.user_id = 'default'` in CLI path correctly passes RLS
        - Test that `DatabaseTokenStorage` standalone sessions correctly set the session variable
        - Downgrade: `ALTER TABLE {table} NO FORCE ROW LEVEL SECURITY`

- [ ] **Test Fixture Updates**
    - Effort: M
    - What: Update all test factories and fixtures with `user_id` parameter
    - Why: Existing tests need to work with the new user_id fields
    - Dependencies: Domain Entity user_id Fields (v0.6.0)
    - Status: Not Started
    - Notes:
        - Factories in `tests/fixtures/`: `make_track_like()`, `make_playlist()`, `make_workflow()`, etc. — add `user_id` parameter defaulting to `"test-user"`
        - `make_mock_uow()` — protocol methods that take user_id must be handled
        - Existing tests continue to pass with default user_id
        - New tests use explicit user_ids (`"user-a"`, `"user-b"`) for isolation testing

- [ ] **Existing Data Claim**
    - Effort: S
    - What: Mechanism to reassign all `user_id='default'` data to a real Neon Auth user ID
    - Why: Instance owner's existing imported data must transfer to their real account
    - Dependencies: v0.6.2 (Repository Scoping)
    - Status: Not Started
    - Notes:
        - Implementation options (pick one):
            - **Admin CLI command**: `mixd admin claim-data --user-id <neon-auth-sub>` — `UPDATE track_likes SET user_id = ? WHERE user_id = 'default'` for each table
            - **Auto-claim on first login**: if all data has `user_id='default'` and no other users exist, automatically assign to the first authenticated user
            - **Admin API endpoint**: `POST /api/v1/admin/claim-data` (protected, instance owner only)
        - Must update all 7 user-scoped tables + `sync_checkpoints`
        - Template workflows (`user_id IS NULL`) are not affected
        - Idempotent — running twice is safe (second run updates 0 rows)
