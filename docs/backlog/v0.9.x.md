# v0.9.x: Agentic Workspace

For strategic overview, see the [planning overview](README.md).

**User Workflow Context**: Every mixd action today is manual — a web click-path or a CLI invocation the user drives step by step. Curators repeat the same Sunday maintenance ritual by hand; Casual Enthusiasts bounce off the node catalog before their first playlist materializes; Tinkerers who already run LLM agents (Claude Desktop, Cursor, local toolchains) can't point them at mixd at all. v0.9.x makes the same surface drivable by agents under one product contract — **bidirectional parity**: anything a human can do through the app, an agent can do (minus an explicit human-only blacklist: connector OAuth, account management, `mixd admin reset`), and nothing an agent does is invisible to the human (agent work lands in ordinary mixd entities and shows up in the same UI, run logs, and audit surfaces). The in-app assistant lands first (v0.9.0–v0.9.2), external agents via MCP second (v0.9.3) — both consuming one shared tool registry so the two surfaces never drift.

---

### Pre-Work: Best-Practices Passes & Sequencing Review

> **Status: ✅ Completed — two passes.** 2026-07-02: MCP spec 2025-11-25 (transports, authorization, tools, `schema.ts`), the MCP 2026-07-28 release-candidate announcement, Anthropic docs (prompt caching, structured outputs, MCP connector), couplefins at `da407c5`. 2026-07-10 (agentic-workspace re-scope, addendum below): couplefins at HEAD `105496f` (the v1.8.0–v1.8.3 agentic-chat series), MCP 2026-07-28 RC detail (stateless core, Tasks, Apps), current Anthropic tool-use surface (tool search, programmatic tool calling, context editing, compaction). The milestone structure below is the committed plan.

#### State of the art, July 2026

**MCP servers.** Current spec revision is **2025-11-25**; the **2026-07-28 revision publishes final on that date** (RC locked 2026-05-21; Tier-1 SDKs ship support within the ten-week validation window). What changes: the core goes **stateless** — the `initialize` handshake and protocol sessions are removed; any request can hit any server instance; state crosses calls via the **explicit-handle pattern** (tools return identifiers the model passes back). New opt-in **extensions**: **Tasks** (a server may answer `tools/call` with a task handle; the client drives `tasks/get`/`tasks/update`/`tasks/cancel`; task-vs-blocking is the *server's* decision) and **MCP Apps** (servers ship pre-declared HTML UI templates that hosts render in a sandboxed iframe, with UI actions flowing through the same JSON-RPC consent path as tool calls). A formal deprecation policy (≥12 months deprecated before removal) makes building on the spec safer. Two things did not change: **stdio** remains the recommended local transport ("clients SHOULD support stdio whenever possible"; identity from the environment, not OAuth), and tool behavior annotations (`readOnlyHint`, `destructiveHint`, …) remain **untrusted client-side hints** — mutation safety must live server-side. Practical consequence: build on the official `mcp` Python SDK *after* the 2026-07-28 release settles, treat protocol mechanics as SDK-owned, and design tools stateless-first.

**In-app LLM agents.** POST + SSE over `fetch` + `ReadableStream` remains the browser streaming standard. On the Anthropic side, the mid-2026 agentic surface is substantially richer than what the 2026-07-02 pass described: **tool search** (`tool_search_tool_bm25_*`; tools marked `defer_loading: true` are discovered on demand, keeping dozens of tools out of the upfront context and preserving the prompt cache) — Anthropic's guidance puts the accuracy cliff at roughly ten-plus tools; **programmatic tool calling** (the model writes code in the `code_execution` sandbox that calls tools marked with `allowed_callers`; intermediate results stay in the sandbox, only the final output enters model context — built for exactly the many-sequential-calls / large-intermediate-results shape); **context editing** (`clear_tool_uses_*` clears stale tool results server-side as a conversation grows); server-side **compaction** (beta) for conversations that outgrow the window; **task budgets** (beta) as a token-ceiling lever the model paces itself against. Two corrections to the 2026-07-02 brief's pinned constants: **`strict: true` is the wrong tool for a broad registry** — couplefins tried it and abandoned it (20-strict-tools-per-request cap, compiled-grammar-too-large at 16 write tools, numeric/array constraints rejected); handler-side validation is the boundary, schemas keep `additionalProperties: false` plus prose ranges. And **cache-activation minimums are model-specific and have shifted** (Opus 4.8 is 4096 tokens) — don't pin a number; verify via `usage.cache_read_input_tokens` at implementation.

**The relationship (MCP ↔ in-app chat).** Unchanged, and now double-confirmed by couplefins in production: expose capabilities once via an in-process tool registry; the first-party chat executor and the MCP server are two consumers of it. No credible pattern routes first-party chat through one's own MCP server, and annotations-as-untrusted means the confirmation gate lives inside mixd's process regardless.

#### Addendum (2026-07-10): couplefins went agentic — the port target moved

The 2026-07-02 brief verified couplefins at `da407c5`, when its chat was a 9-tool Q&A bot. Since then couplefins shipped its **v1.8.0–v1.8.3 "Agentic Chat" series** (74 commits, completed 2026-07-10, HEAD `105496f`) — the same chatbot-to-agentic-workspace leap this milestone plans, under the product principle *"anything a human can do through the app, the chatbot can do — and nothing more,"* enforced as a CI test. What exists there now, live-verified, with documented pitfalls:

- **Capability-parity registry** — `ToolSpec(name, schema, handler, use_cases, kind: read|write|agentic, defer_loading, …)`; 30 tools (15 read / 12 write / 3 agentic — the three agentic being `code_execution`, `delegate_analysis`, `tool_search_tool_bm25`); classification sets (`BLACKLISTED_USE_CASES`, `MECHANICALLY_EXCLUDED_USE_CASES`, `INTERNAL_USE_CASES`) with a CI parity test that fails when any app use case is unclassified.
- **Agentic loop** — flat tool loop with injected executor (so the subagent reuses it), `pause_turn`/`max_tokens` stop-reason handling, dual turn budgets (model turns + a ×5 sandbox-round backstop).
- **Agentic depth** — `code_execution` sandbox + programmatic tool calling (read tools carry `allowed_callers`; write tools never), a `delegate_analysis` research subagent (fresh context, read-only tools, low effort, bounded summary), tool search with only ~6 hot tools non-deferred, context editing with tuned thresholds. Server-side compaction was **evaluated and deliberately skipped** (conversations stay bounded by the 50-message cap + tool clearing).
- **Security layers** — two-phase confirmation (unchanged) *plus* systematic untrusted-content labeling: user-originated strings are wrapped in `<user_data>` tags for the model and stripped at the event boundary — a deterministic prompt-injection layer on top of mutations-as-proposals.
- **Config reality** — model `claude-opus-4-8` with explicit `thinking={"type": "adaptive"}` (Opus runs thinking-off when the field is omitted, and under-reaches for tools unless descriptions lead with a when-to-call trigger sentence); effort became a **per-request user control** (Quick/Standard/Thorough → low/high/xhigh); the SSE protocol grew to **7 events** (`code_start`/`code_result` added; `tool_start` carries `kind`); three cache breakpoints (last non-deferred tool, system prompt, last stampable message block).
- **Deliberately out of scope there** (mixd treats these as candidates, not commitments): memory tool (flagged as a prompt-injection *reinfection* vector), MCP tools inside chat, parallel subagent orchestration, conversation persistence.

Implementation sources to mine when each epic starts: couplefins `docs/chat-assistant.md` (architecture + rationale), `docs/backlog/v1.8.x-agentic-chat.md` (product principle, design-grounding citations), and `v1.8.0`–`v1.8.3` (especially v1.8.3's "Implementation learnings" — four programmatic-tool-calling bugs unit tests couldn't catch).

#### Decisions

- **D1 — The couplefins port stands; the port target is now the v1.8.x series.** Architecture layer (POST+SSE, sentinel queue + error-code map, Zustand store, two-phase confirmation, per-request system-prompt composition, in-memory rate limiter, ephemeral conversations) verified clean across both passes — port the patterns. Port the *v1.8.x* versions of everything (7-event SSE, `ToolContext` injection, untrusted-content wrapping, kind-aware registry), not the `da407c5` snapshots. As before: port patterns, not constants.
- **D2 — Chat-first retained; MCP is the second consumer, not the foundation.** Unchanged from 2026-07-02, reconfirmed 2026-07-10.
- **D3 — The shared tool registry is a v0.9.0 deliverable.** Unchanged — and strengthened: the registry's classification machinery (D4) must exist from the first tool.
- **D4 — Bidirectional parity is the product contract, enforced in CI.** Agent→human: every application use case is classified in the registry — covered by a tool, blacklisted (human-only: connector OAuth/token flows, account management, `mixd admin reset`), mechanically excluded (interactive-only UI affordances), or internal — and a CI test fails on unclassified use cases, so the contract can't silently rot as features ship. Human→agent: agents get no private capabilities — tools are thin adapters over the same use cases the UI and CLI call, agent mutations flow through the same two-phase-confirmation and operation-run surfaces, and everything an agent does is inspectable in the ordinary UI.
- **D5 — Agentic depth is committed scope, not research.** Programmatic tool calling, the research subagent, tool search, context editing, and per-request effort are all live-verified in couplefins v1.8.2/v1.8.3 with documented constants and failure modes. Mixd commits them as v0.9.2 — ports with mixd-shaped stories (batch-first library operations are the payoff), not experiments.
- **D6 — Four milestones, MCP last.** v0.9.0 foundation → v0.9.1 full parity → v0.9.2 agentic depth → v0.9.3 MCP, mirroring couplefins' shipped sequence (foundations → read parity → write parity → agentic). Each is a complete, manually testable slice; MCP inherits a mature, parity-complete registry so it reduces to transport + audit exposure. *(This re-purposes the v0.9.2 number the 2026-07-02 plan reserved for remote MCP; remote/Streamable-HTTP MCP is now scheduled as [v0.9.5](#v095-remote-mcp-server-your-production-library-from-any-agent), after the v0.9.4 follow-ups.)*

#### Couplefins port verification (choice → status at v1.8.x HEAD → action)

| Ported choice | Status | Action |
|---|---|---|
| POST + SSE streaming; `fetch`+`ReadableStream` client | Still the standard | Port as-is |
| SSE protocol, `_run_with_sentinel`, error-code map | Grew to **7 events**: + `code_start`/`code_result`; `tool_start` carries `kind` | Port the 7-event protocol from day one (code events unused until v0.9.2) |
| Two-phase confirmation (pending-action store, 5-min TTL, owner check) | Reinforced; confirmation cards must show before/after detail (approval-fatigue finding) | Port as-is; drive from registry `kind` |
| Manual tool loop | Now a full agentic loop: injected executor, `pause_turn`/`max_tokens` handling, dual turn budgets | Port the v1.8.x loop |
| Zustand store, 50-msg cap, Cmd+K, edge tab, `w-96` panel | Sound; store gained `toolCalls[].kind`, `codeExecutions[]`, `effort`, `confirmationStates` | Port v1.8.x store shape; retarget layout to `PageLayout.tsx` |
| Streamdown partial-markdown rendering | Current | Port; bump version at implementation |
| Model `claude-sonnet-4-6` → (07-02) `claude-sonnet-5` | Couplefins runs **`claude-opus-4-8`**, chosen for multi-step tool routing; Sonnet 5 remains the cheaper near-Opus-agentic tier (intro pricing ends 2026-08-31) | Decide at implementation between `claude-sonnet-5` and `claude-opus-4-8`; either way set `thinking={"type": "adaptive"}` explicitly and lead tool descriptions with when-to-call triggers |
| `output_config.effort` via `extra_body` → (07-02) native param | Native, and now a **per-request user control** (Quick/Standard/Thorough → low/high/xhigh), default `high` | Port the per-request pattern (UI control lands v0.9.2; backend accepts `effort` from v0.9.0) |
| `strict: true` on small tools (07-02 plan) | **Abandoned in couplefins** after live API limits (strict-tool cap, grammar-size failure, constraint rejection) | Drop strict entirely; handler + server-side validation is the boundary; keep `additionalProperties: false` + prose ranges |
| "≥2048 tokens to activate caching" → (07-02) "1,024 on Sonnet 5" | Minimums are model-specific and moved again (Opus 4.8: 4096) | Un-pin; size the primer generously and verify via `usage.cache_read_input_tokens` |
| `max_tokens=8192`, `thinking` unconfigured | Couplefins: `max_tokens=16384`, adaptive thinking explicit | Port v1.8.x config as starting points |
| In-memory rate limiter (20 req/60s) | Right-sized, unchanged | Port as-is |
| Ephemeral conversations, no DB persistence | Sound, unchanged; couplefins re-deferred persistence | Port as-is |

**Flagged for follow-up, not this milestone**: `.claude/rules/web-frontend-patterns.md` still says "No Zustand" while `editor-store.ts` (and now the chat store) use it — reconcile the rule; `docs/web-ui/01-user-flows.md` §6.5 (v0.9.0 sketch) needs a refresh when implementation starts.

---

### v0.9.0: Workflow Assistant (Agentic Foundation)

**Goal**: Today the Casual Enthusiast has no front door — building a workflow means learning the node catalog. v0.9.0 gives them a persistent right-panel assistant: describe a playlist in plain English ("upbeat tracks I haven't played in 6 months") and get a real, editable `WorkflowDef` — previewed in the existing graph renderer, saved on approval. Underneath the flagship story, this milestone lays the agentic-workspace foundation: the chat shell, the streaming loop, and the parity-classified tool registry that every later milestone (and the MCP server) consumes.

**Context**: Per the README's persona-alignment table this is "THE adoption enabler — changes who can use mixd." The architecture is a port of couplefins' production-tested v1.8.x agentic chat on the same stack (Vite + React + TS + FastAPI + SQLAlchemy + Tanstack Query), built deliberately so mixd could port it. The mixd-specific surface is the domain primer, the node-catalog → JSON Schema bridge, and the workflow-preview card. Implement against the Pre-Work verification table's "Action" column, not pinned constants.

**What this unlocks**:
- One-shot natural-language workflow generation — the machine-derived WorkflowDef JSON Schema is the prior, no few-shot templates needed
- Persistent right-panel chat (Cmd+K toggle, edge-tab when collapsed) across all routes
- Workflow preview rendered as the same read-only DAG users see elsewhere in the app, before save
- Two-phase confirmation: LLM proposes → user inspects → user approves → save (mutations never silent)
- The parity-classified tool registry (D3/D4) that v0.9.1 fills, v0.9.2 deepens, and v0.9.3 exposes over MCP

**Persona**: Casual Enthusiast — primary. The natural-language entry point is the front door for users who'd never build a workflow from scratch. Tinkerer — secondary. Generates a starting point in seconds, then tweaks in the editor they already know. Weekly Curator — tertiary. Faster ad-hoc creation when a specific need arises.

**Key Design Decisions**:
- **Port couplefins v1.8.x, don't rebuild** — the infrastructure (7-event SSE streaming, adapter, agentic tool loop, two-phase confirmation, prompt caching, error mapping, Zustand store, edge-tab affordance, untrusted-content wrapping) is already-solved; apply the Pre-Work verification table's delta while porting.
- **Tools are parity-classified registry entries from day one (D3/D4)** — every capability is a `ToolSpec` (thin adapter over an application-layer use case) in `src/application/tools/`; the chat executor loops over the registry; the CI parity test ships with the registry, initially with most of mixd's 68 use cases classified "not yet covered" (coverage is v0.9.1's job — classification discipline starts here).
- **Schema-guided generation, server-validated** — the WorkflowDef JSON Schema is derived mechanically from `config_fields.py` and shipped as the generation tool's `input_schema`; conformance is enforced where it already lives — `validate_workflow_def_detailed()` — with failures returned as error tool results so the model self-corrects in-turn. No `strict: true` anywhere (Pre-Work table).
- **Untrusted content is labeled, deterministically** — track titles, playlist names, and other user-originated strings are wrapped in `<user_data>` tags in model-facing JSON and stripped at the event boundary; combined with mutations-as-proposals this is the layered prompt-injection defense (music metadata is attacker-controllable text — a track title can contain instructions).
- **Workflow Templates (v0.8.9) are user-facing, not LLM-facing** — no coupling in either direction.
- **Reuse `WorkflowGraph.tsx` for previews** — the read-only DAG renderer already exists (`web/src/components/shared/WorkflowGraph.tsx`: non-interactive React Flow, ELK layout, status/diff overlays). The preview card embeds it.
- **Refinement is first-class in both chat and editor** — each refinement turn re-emits a fresh `generate_workflow_def` call with the complete updated DAG; the preview card replaces its contents in place; chat history is the diff context. Fine-grained graph surgery stays in the visual editor.
- **Server-side context injection** — the FastAPI route resolves user, library stats, and current-workflow context from the authenticated session and bakes it into the system prompt; the panel passes no context props.
- **Two-phase confirmation for saves** — `save_workflow` returns a pending action; frontend renders a ConfirmationCard showing what will be created/changed; approval triggers a second request that executes. Driven by the registry's `kind` flag.
- **Ephemeral conversations, no DB persistence** — chat state lives in the Zustand store, capped at 50 messages; add persistence only as a post-deploy revision if real demand appears.
- **Anthropic Claude via `AsyncAnthropic`** — model decided at implementation between `claude-sonnet-5` and `claude-opus-4-8` (Pre-Work table); explicit `thinking={"type": "adaptive"}`, `output_config={"effort": ...}` default `high`, `max_tokens=16384` as starting points. System prompt + tools cached via `cache_control: {"type": "ephemeral"}`; verify activation via `usage.cache_read_input_tokens`, don't assume a token minimum.
- **In-memory rate limiter** — 20 req/60s per user, matching couplefins; right-sized for mixd's deployment shape.
- **New surfaces start from the Wave-3 primitives (v0.8.15)** — every data view in the panel renders through `QueryStates` (+ `skeletons.tsx` primitives) and any picker composes `CommandSearchList`; hand-rolling a loading/error/empty ladder in ported couplefins code is a porting bug, not a style choice.

#### Implementation Progress & Learnings (updated 2026-07-11)

**All build phases (0–4) implemented as of 2026-07-11** — pending live smoke + version-bump gate before ship. Phases 0–1 shipped the agentic foundation + first live vertical slice: A user opens the panel (Cmd+K), types a message, watches tokens stream, and sees `describe_node` dispatch through the registry. Verified by 4 route integration tests (real SSE + registry dispatch, rate-limit→429, unconfigured→503, expired-confirm→409), 5 loop unit tests, 3 frontend streaming-wiring tests; backend + frontend lint/type-check clean. On branch `feat/v0.9.0-workflow-assistant`.

| Epic | Status | Landed at |
|---|---|---|
| Build Shared Tool Registry + Parity Contract | ✅ Done | `src/application/tools/registry.py`; parity test over 81 `*UseCase` classes |
| Port Right-Panel Chat Shell + Zustand Store | ✅ Done | `web/src/components/chat/`, `web/src/stores/chat-store.ts` |
| Port SSE Streaming Infrastructure | ✅ Done | `src/interface/api/chat_sse.py`, `web/src/api/chat-sse.ts` (7-event) |
| Port FastAPI Chat Route + Anthropic Adapter | ✅ Done | `routes/chat.py`, `infrastructure/chat/anthropic_adapter.py`, `application/chat/{use_case,protocols,events,pending_actions,user_data}.py` |
| Compose Mixd Domain Primer + System Prompt | ✅ Done | `system_prompt.py` 3-block layout (cached primer + uncached user_context/current_workflow); voices seam; stats via `GetDashboardStatsUseCase` in `_build_command` |
| Workflow Generation Tools | ✅ Done | `workflow_schema.py` bridge + 6 tools; `save_workflow` two-phase via `confirmed_actions.py` |
| Workflow Preview ToolResultCard | ✅ Done | `cards/WorkflowPreviewCard.tsx` (lazy-loaded); sibling save proposal is the approval gate |
| LLM Feedback Collection | ✅ Done | migration 036 + repo + `RecordChatFeedbackUseCase` (first `BLACKLISTED` member) + `POST /chat/feedback` + thumbs |

**Learnings for whoever finishes Phases 2–4** — each cost real debugging; don't rediscover:

- **SDK parity retired the biggest risk.** couplefins targets `anthropic>=0.97`; mixd installed **0.116**. Every symbol and `beta.messages.stream` param couplefins uses (`BetaAsyncMessageStream`, `output_config`, `context_management`, `thinking`, `betas`, `container`) exists unchanged — the adapter ported near-verbatim. Re-verify only on an SDK bump.
- **Parity is classified by `*UseCase` class (81), not module (58)** — a module like `sync_likes.py` holds several use cases of different read/write character. `BLACKLISTED_USE_CASES` is legitimately **empty**: mixd's human-only capabilities (OAuth, account mgmt, admin reset) aren't use-case classes; they live in the connector/interface layer. **Phase 3 must move `GetWorkflowUseCase`/`ListWorkflowsUseCase`/`CreateWorkflowUseCase`/`UpdateWorkflowUseCase` out of `NOT_YET_COVERED` into their `ToolSpec.use_cases`** as those tools land.
- **ruff B008 fires on the *second* `Depends()` in a route signature, never the first** (every existing mixd route has exactly one). Resolve extra dependencies (the LLM client) **in the route body**, not as a second `Depends`. Consequence: **tests inject the fake LLM via `monkeypatch.setattr(chat_route, "get_llm_client", …)`, not `dependency_overrides`** — it isn't a Depends.
- **`ASYNC119` is inherent to the streaming loop** (you must yield inside the LLM stream context). It's a justified per-file ignore *because* the SSE bridge's `try/finally` cancels the background task on disconnect, closing the context via GeneratorExit. Don't "fix" it by buffering.
- **`wrap_for_model` + the `UserData` str-subclass marker are deferred to v0.9.1.** No v0.9.0 tool returns user-library free text (`describe_node` is static catalog; workflow-gen returns model-generated defs), so results serialize directly while the event side still strips tags. When the first library-read tool lands, the `UserData(str)` subclass trips ruff **FURB189** ("use `collections.UserString`") — but `UserString` breaks JSON-serializability and str semantics, so it needs a `# noqa` **with user sign-off** (per `.claude/rules/python-conventions.md`) or a deliberate restructure. Decide before writing it.
- **Model is env-configurable**: `CHAT__MODEL_ID` (default `claude-opus-4-8`; `claude-sonnet-5` ≈ half cost). Adaptive thinking is always set **explicitly** — both models run thinking-off if the field is omitted.
- **The node→JSON-Schema bridge (Phase 3) must use `anyOf` per node type (not `oneOf`) and encode min/max as prose in `description` (not `minimum`/`maximum`)** — Anthropic structured outputs reject both `oneOf` and numeric constraints; no `strict:true`. Source: `config_fields.py` (`ConfigFieldDef`) + `list_nodes()`; round-trip validation via `validate_workflow_def_detailed()` returning failures as an error tool result.
- **Frontend seams**: the panel mounts as the 3rd flex child of `PageLayout.tsx` (outside `RouteOutlet`, so it persists across routes); SSE auth uses `getAuthToken()` from `#/api/auth`; **`CommandSearchList` is track-specific** (hardcodes the tracks query) — a Phase 3 entity picker over playlists/workflows needs generalization or a parallel component. The `codeExecutions[]` / `CodeExecutionCard` / `currentWorkflowDraft` store slots ship **dormant** (Phase 3 / v0.9.2 activate them).
- **`.claude/rules/web-frontend-patterns.md` already sanctions Zustand** — the backlog's earlier "No Zustand" flag was stale; the chat store is compliant. Scratch that follow-up.

**Phases 2–4 learnings (2026-07-11):**

- **`ToolResultCard` must lazy-load `WorkflowPreviewCard`** — the preview pulls React Flow + ELK, and the chat panel mounts on every page, so a static import lands the whole graph stack in the initial bundle. It also breaks vitest outright (`@jalez/react-flow-smart-edge` is CJS-in-ESM under jsdom). Type guard + `SaveProposal` live in `cards/workflow-preview-types.ts` so the dispatcher never evaluates the heavy module.
- **Save choreography**: the primer's `<mutation_rules>` has the model propose `save_workflow` in the same turn as a successful generate; the preview card confirms that sibling proposal (Save/"Save changes" label from `details.mode`) and suppresses the sibling `ConfirmationCard`; with no live proposal (expired TTL / model skipped it) Save degrades to a synthetic "Save this workflow." message. `currentWorkflowDraft` absorbs proposals from `details.definition` and covers the cross-turn case.
- **The date moved OUT of the cached primer block** — Phase 1 had it inside, invalidating the cache daily; volatile values (date, stats, current workflow) now trail as uncached blocks. A unit test guards the layout; primer sized ~17k chars to clear the 4096-token floor with margin.
- **Primer text mentioning `<current_workflow>` broke naive `"<current_workflow>" in system` test probes** — probe block *structure* (count, `startswith`) or a sentinel sentence, not tag substrings.
- **Feedback records explicit thumbs only** — save is its own durable acceptance signal. `RecordChatFeedbackUseCase` is `BLACKLISTED_USE_CASES`' first member (the agent must never file feedback on itself); the parity test caught it as unclassified within minutes of the class landing, exactly as designed.
- **Mobile route was nearly free**: `ChatPanel` already had `fullScreen`; `ChatPage` is the couplefins `AskPage` pattern (desktop → `setPanelOpen` + redirect) + a lazy route + a bottom-nav entry. Eyeball the `h-[calc(100svh-11rem)]` offset and 5-item nav density on a real phone viewport before ship.

#### Port Epics

- [x] **Port Right-Panel Chat Shell + Zustand Store**

    **Story**: A user wants the chat panel persistently available on every page (Cmd+K to open, edge-tab when collapsed) so they never navigate away from what they're doing to ask the assistant for help — the affordance they know from Cursor, Linear, and Notion AI.

    **Decisions**:
    - Port `ChatPanel`, `ChatEdgeTab`, `ChatMessageList`, `ChatMessage`, `ChatInput`, `ToolCallIndicator`, `ToolResultCard`, `ConfirmationCard`, `SuggestedQuestions` and the chat store from couplefins v1.8.x — including the store's `toolCalls[].kind` and `confirmationStates` shape (the `codeExecutions[]` slot ships dormant until v0.9.2).
    - **Layout host is `PageLayout.tsx`** (mixd has no `AppLayout`): persistent right sidebar on md+ breakpoints, edge tab when collapsed; mobile (`MobileShell`) gets a full-screen chat route, mirroring couplefins's `AskPage` pattern.
    - Cmd+K binding is net-new global-hotkey work in mixd (no hotkey infra exists; `cmdk` is only used for comboboxes) — port couplefins's `useKeyboardShortcut` hook.
    - Width `w-96`, 50-message cap, "New Conversation" clears — couplefins values as starting points.

    **Spec**:
    - New directory `web/src/components/chat/`; new store `web/src/stores/chat-store.ts` (naming consistent with `editor-store.ts`)
    - `web/src/components/layout/PageLayout.tsx` renders `<ChatPanel />` + `<ChatEdgeTab />`, lazy-loaded with Suspense
    - Suggested-questions seed list customized for mixd ("Build me a Friday-night dinner playlist", "Find tracks I loved last year but haven't played recently")

    **Tests**:
    - (component) Cmd+K toggles panel; Escape closes
    - (component) Edge tab visible when collapsed; click opens
    - (component) "New Conversation" clears messages
    - (component) 50-message cap enforced; soft warning at 45

    Effort: M | Dependencies: v0.4.3 (graph renderer used by preview card) | Status: Completed (2026-07-11)

- [x] **Port SSE Streaming Infrastructure**

    **Story**: When the assistant is generating or running a tool, the user wants incremental output (streaming tokens, tool progress badges) rather than a frozen panel — sub-300ms first paint is the "feels responsive" baseline.

    **Decisions**:
    - Port `web/src/api/chat-sse.ts` (fetch + ReadableStream — POST body, so no EventSource) and the backend SSE serializer (sentinel queue, error-code map, 15s keepalive comments) from couplefins v1.8.x.
    - **Seven SSE event types from day one**: `token`, `tool_start` (carries `kind`), `tool_result`, `code_start`, `code_result`, `done`, `error` — the code events stay unused until v0.9.2 but the protocol doesn't churn.
    - Abort via `AbortSignal` — user cancel returns silently; mid-stream failures emit typed error codes.
    - Streamdown for buffered incomplete-markdown rendering (port dep + wrapper; take current version).

    **Spec**:
    - `sendChatMessage(messages, callbacks, signal, confirmation?, effort?)` matching couplefins's v1.8.x signature
    - `text/event-stream` + `Cache-Control: no-cache` + `X-Accel-Buffering: no` headers
    - Adapter-side note: adaptive thinking emits thinking stream events — the adapter forwards text deltas only and must tolerate/skip thinking events without error

    **Tests**:
    - (frontend integration) tokens render incrementally; abort cancels silently; mid-stream error emits typed code
    - (backend integration) route streams SSE with correct content-type and event framing

    Effort: S | Dependencies: Port Right-Panel Chat Shell | Status: Completed (2026-07-11)

- [x] **Build Shared Tool Registry + Parity Contract**

    **Story**: A maintainer wants every assistant capability defined once — name, description, input schema, side-effect class, dispatcher — so the chat assistant, the v0.9.2 agentic features, and the v0.9.3 MCP server expose identical tools; and they want the bidirectional-parity contract (D4) enforced by CI from the first tool, so "the agent can do anything I can do" stays true as features ship instead of decaying into a hand-maintained wish.

    **Decisions**:
    - Each tool is a frozen attrs `ToolSpec`: `name`, `description`, `input_schema` (JSON Schema, `additionalProperties: false`, prose ranges — no `strict`), `kind: Literal["read", "write", "agentic"]`, `defer_loading: bool`, `use_cases: tuple[str, ...]` (the application use cases it covers — the parity-accounting hook), `dispatch` (async callable taking validated args + UoW, returning a JSON-serializable result).
    - **Tools are thin adapters over `src/application/` use cases** — no business logic in dispatchers; where a use case is missing or awkwardly shaped, fix it at the source. Dispatch runs through `execute_use_case()` with the acting user's id so RLS scoping is identical to the web UI.
    - **`kind` is load-bearing**: `write` routes through the pending-action confirmation flow in chat and maps to MCP annotations in v0.9.3; `agentic` marks capabilities (sandbox, subagent) that are never callable from code execution.
    - **Parity classification sets ship with the registry**: `BLACKLISTED_USE_CASES` (human-only: connector OAuth/token flows, account management, `mixd admin reset`), `MECHANICALLY_EXCLUDED_USE_CASES` (interactive-only affordances), `INTERNAL_USE_CASES` (plumbing not user-facing). A CI test asserts every module in `src/application/use_cases/` appears in exactly one bucket: covered by some `ToolSpec.use_cases`, or one of the three exclusion sets. In v0.9.0 most entries sit in an explicit `NOT_YET_COVERED` set with a tracking pointer to v0.9.1 — the test enforces *classification*, not yet coverage.
    - Registry entries carry LLM-facing descriptions (3–4 sentences, concrete examples, **leading with a when-to-call trigger sentence** — current models under-reach for tools without one) — the writing standard both consumers inherit.

    **Spec**:
    - New module `src/application/tools/registry.py` — `ToolSpec` + `TOOLS: tuple[ToolSpec, ...]` + classification sets
    - `src/application/chat/tool_executor.py` dispatches by looping the registry — no per-tool branching
    - MCP-ready metadata derivable per entry: `readOnlyHint = (kind == "read")`, `destructiveHint = (kind == "write")`, `openWorldHint = False` (mixd tools act on the user's own library, a closed world)
    - New test `tests/unit/application/tools/test_registry_parity.py` — the D4 contract

    **Tests**:
    - (unit) names unique; every description ≥50 chars and leads with a when-to-call sentence pattern; every `input_schema` is valid JSON Schema with `additionalProperties: false`
    - (unit) every `write` entry refuses direct execution without a claimed confirmation
    - (unit) parity test: every use-case module classified in exactly one bucket; unclassified modules fail CI
    - (unit) dispatcher signature consistent across entries

    Effort: M | Dependencies: — | Status: Completed (2026-07-11)

- [x] **Port FastAPI Chat Route + Anthropic Adapter**

    **Story**: The backend wants a single `POST /api/v1/chat` endpoint that takes message history (plus optional confirmation and effort), opens an Anthropic streaming connection, runs the agentic tool loop over the registry, and emits SSE events back — couplefins's v1.8.x shape adapted to mixd auth and UoW patterns.

    **Decisions**:
    - Port `routes/chat.py`, `schemas/chat.py`, `anthropic_adapter.py`, `use_case.py` (the agentic loop) from couplefins v1.8.x; adapt to mixd auth (`get_current_user_id`) and `execute_use_case()`.
    - **The loop is agentic from day one**: parallel tool calls per turn, `pause_turn` (echo assistant turn, continue) and `max_tokens` (typed truncation error) stop-reason handling, injected executor (`ToolExecutorFn`) so v0.9.2's subagent reuses it, `max_turns` budget (couplefins: 24) with the sandbox-round backstop seam present but inert until v0.9.2.
    - **Handlers receive a `ToolContext`** (current user, LLM client handle) — the injection point v0.9.2's agentic tools need; cheap to thread now, painful to retrofit.
    - Model + thinking + effort per the Pre-Work verification table (implementation-time model choice; adaptive thinking explicit; effort default `high`, accepted per-request in the API schema now even though the UI control lands in v0.9.2).
    - **Untrusted-content wrapping**: port `user_data.py` (`wrap_for_model()` / `strip_user_data()`) and apply it to every user-originated string a tool result carries.
    - Two-phase mutation pattern: pending-action store (in-memory, `action_id`-keyed, 5-min TTL, owner check) ported as-is; confirmation processed before the LLM turn.
    - Prompt caching: breakpoints on the last non-deferred tool + system prompt + last stampable message block (couplefins v1.8.x placement); verify via `usage.cache_read_input_tokens`.
    - Error mapping: typed exceptions → SSE error codes, extended with mixd-specific codes as needed. Rate limiter: 20 req/60s per user, in-memory.

    **Spec**:
    - New modules `src/application/chat/` (use case/loop, tool executor, pending actions, user_data), `src/infrastructure/chat/anthropic_adapter.py`, `POST /api/v1/chat`
    - `ANTHROPIC_API_KEY` via existing settings pattern; chat disabled cleanly when unset (`CHAT_UNAVAILABLE` error event)
    - Backend caps mirrored to frontend: 50 messages, per-message and total byte limits (couplefins values as starting points)

    **Tests**:
    - (integration) `/api/v1/chat` streams SSE; tool calls dispatch through the registry and stream results
    - (integration) rate limit → `RATE_LIMIT_EXCEEDED`; expired pending action → `ACTION_EXPIRED`
    - (integration) confirmation request executes the pending action exactly once
    - (unit) `pause_turn` continues the loop; `max_tokens` surfaces a typed truncation error
    - (unit) user-originated strings in tool results arrive model-side wrapped in `<user_data>` and reach the frontend stripped

    Effort: M | Dependencies: Port SSE Streaming Infrastructure, Build Shared Tool Registry + Parity Contract | Status: Completed (2026-07-11)

- [x] **Compose Mixd Domain Primer + System Prompt**

    **Story**: The LLM needs mixd's domain — what a track is, what workflows do, which nodes exist, what the user's library looks like — so it generates sensible WorkflowDefs without hallucinating node names or asking for information already available server-side.

    **Decisions**:
    - Port couplefins's v1.8.x structure: XML-tagged sections (identity + voice, scope, tool_habits, untrusted_content, domain_model, response_format, mutation_rules), composed per request, cached via the ephemeral breakpoint.
    - Primer content is mixd-specific: node taxonomy (sources/enrichers/filters/sorters/selectors/combiners/destinations), connector identity model, preference/tag system, per-user library statistics, current workflow context when editing.
    - **Cache-activation minimum is model-specific — don't pin it** (Pre-Work table): size the primer generously, keep volatile content (stats, date) after the cache breakpoint or accept per-user cache entries deliberately, and assert cache hits empirically.
    - Voices system: port the pluggable pattern with a single "Default" voice (straightforward, no persona); keep the registry seam for later.

    **Spec**:
    - `src/application/chat/system_prompt.py` — `build_system_prompt(user, library_stats, current_workflow, today)` → list of system blocks
    - Token-threshold test uses a chars/3.5 heuristic asserting comfortable margin over the largest current per-model activation minimum; the authoritative check is `usage.cache_read_input_tokens` in the integration smoke

    **Tests**:
    - (unit) assembled prompt contains all required XML sections
    - (unit) primer clears the cache-activation threshold with margin
    - (integration) prompt reflects current user's library stats and active workflow context

    Effort: M | Dependencies: Port FastAPI Chat Route + Anthropic Adapter | Status: Completed (2026-07-11)

- [x] **Workflow Generation Tools — Node Catalog → JSON Schema**

    **Story**: The LLM wants to call `generate_workflow_def` with a structured argument matching mixd's actual `WorkflowDef` shape — producing valid DAGs that reference real nodes with correct parameters, without the user teaching it what's available.

    **Decisions**:
    - **Schema source is `src/application/workflows/nodes/config_fields.py`, not the node registry** — `NodeRegistry` metadata carries id/description/category only; `ConfigFieldDef` holds the typed per-node parameter definitions (the same source the web editor's config forms already render from via `GET /api/v1/workflows/nodes`).
    - **`anyOf` per node type, not `oneOf`** — Anthropic structured outputs don't support `oneOf`.
    - **Validation is a feedback loop, not a gate**: the dispatcher runs `validate_workflow_def_detailed()` and returns failures as an error tool result (task id, field, message) so the model self-corrects within the same turn — the same actionable-error pattern the MCP spec prescribes for tool execution errors. No `strict: true` (Pre-Work table); the derived schema ships as the tool's `input_schema` and the model follows it as guidance.
    - All tools are registry entries; `save_workflow` is `kind: "write"` (two-phase confirmation), the rest are `read`.

    **Spec**:
    - New helper `src/application/chat/workflow_schema.py` — `build_workflow_def_schema()` walks `get_node_config_fields()` + `list_nodes()` and produces JSON Schema with `anyOf` per node type
    - Initial registry entries: `generate_workflow_def`, `validate_workflow_def`, `save_workflow`, `get_workflow`, `list_user_workflows`, `describe_node`
    - Schema stays in lockstep with the catalog by construction (derived at startup, not checked in)

    **Tests**:
    - (unit) `build_workflow_def_schema` covers all current node types; rejects unknown types; encodes required params per node
    - (unit) validation failures round-trip as actionable error tool results
    - (integration) "Build me a chill weekend playlist" → `generate_workflow_def` call → valid WorkflowDef
    - (integration) `save_workflow` returns pending action, never a silent persist

    Effort: M | Dependencies: Build Shared Tool Registry + Parity Contract, Compose Mixd Domain Primer + System Prompt | Status: Completed (2026-07-11)

- [x] **Workflow Preview ToolResultCard (Reuses WorkflowGraph)**

    **Story**: When the assistant proposes a `WorkflowDef`, the user wants to see the actual graph they'd see anywhere else in the app — same node shapes, colors, connections — so they can read it at a glance and decide whether to save without learning a new visualization.

    **Decisions**:
    - **Embed `web/src/components/shared/WorkflowGraph.tsx`** — the existing read-only DAG renderer (non-interactive React Flow, ELK auto-layout, already consumes `tasks`). Not the editable editor; no `readOnly` prop exists or is needed.
    - Card adapts to the 384px panel: compressed view + "Open in editor" link for full-size inspection.
    - **Refinement-aware**: a subsequent `generate_workflow_def` result replaces the active preview in place (store tracks `currentWorkflowDraft` keyed by action id) — no scroll-back through stale cards.
    - **Save vs Update**: sessions that began from an existing workflow map Save to `update_workflow(id, def)`; button label flips accordingly ("Save workflow" / "Save changes").
    - "Discard" appends a synthetic user message with feedback and re-streams.

    **Spec**:
    - New component `web/src/components/chat/cards/WorkflowPreviewCard.tsx`; `ToolResultCard` dispatches on `tool_name === "generate_workflow_def"`
    - Store gains `currentWorkflowDraft: { action_id, def, source: "new" | { workflow_id } } | null`
    - Save triggers the confirmation request (`confirmation: {action_id, approved: true}`)

    **Tests**:
    - (component) renders proposed WorkflowDef as a graph; regeneration replaces in place
    - (component) Save label flips between new/editing; Discard triggers regeneration
    - (E2E) generate → refine → preview updates → save → saved workflow matches latest preview
    - (E2E) load existing → refine → save changes → updated, not duplicated

    Effort: M | Dependencies: Workflow Generation Tools, Port Right-Panel Chat Shell | Status: Completed (2026-07-11)

- [x] **LLM Feedback Collection (Thumbs Up/Down)**

    **Story**: The product wants to learn which prompt patterns produce accepted vs rejected workflows so the primer and tool descriptions can be refined — without building an A/B framework or analytics dashboard.

    **Decisions**:
    - Thumbs-up/down per generation, optional free-text note; save = implicit positive, thumbs-down with note = most actionable.
    - **Not building**: A/B harness, insights dashboard, automated prompt optimization. Manual iteration is the right scale.

    **Spec**:
    - DB table `chat_feedback`: `id, user_id, prompt, generated_workflow_def (jsonb), signal (Literal["positive", "negative"]), note (nullable), created_at`
    - `POST /api/v1/chat/feedback`; buttons on the preview card

    **Tests**:
    - (integration) feedback persists with full context
    - (component) thumbs buttons submit cleanly

    Effort: S | Dependencies: Workflow Preview ToolResultCard | Status: Completed (2026-07-11)

#### Post-Deploy Revisions

- [x] **v0.9.0.1** (2026-07-11) — per-user, opt-in AI assistant (BYO Anthropic key)

    **Shipped:** delivered as specced — per-user key stored encrypted in the
    `oauth_tokens` store (`credentials.py`, RLS-scoped, write-only), a single
    `resolve_chat_credential()` precedence rule (user key → server fallback →
    none) behind `get_llm_client` / `/assistant/status`, and a `useChatAvailable()`
    gate that hides the whole surface (Cmd+K, edge tab, panel, mobile `/chat` +
    `MobileBottomNav`) until a key is connected. CLI: `mixd assistant
    connect|test|disconnect`. Keys are validated with a minimal live completion
    so a no-billing key is rejected at connect, not on first message. Post-review
    hardening: undecryptable stored key fails closed (no silent server-key
    billing); cached Anthropic clients close on shutdown; desktop `/chat`
    deep-link waits for the availability gate before redirecting.

    **Why:** v0.9.0 shipped the assistant behind a single deployment-wide
    `ANTHROPIC_API_KEY` and mounts the chat surface (Cmd+K, edge tab, panel,
    mobile `/chat`) for everyone unconditionally. That breaks mixd's multi-user
    principle two ways: one person's key (and spend) would serve every user, and
    a user who hasn't set anything up sees a chat affordance that only 503s
    (`CHAT_UNAVAILABLE`) when they try it. The assistant must be **per-user and
    opt-in**: a user brings their own credential, and until they do, the AI
    assistant simply isn't visible — no broken affordance, no shared key.

    **What:**
    - **Per-user credential**, stored encrypted at rest keyed by `user_id`,
      managed from settings (add / test / remove). The client never gets the key
      back after saving.
    - **Availability is per-user**: `get_llm_client` / the adapter resolves the
      acting user's key via `ctx.user_id` (the server-wide key stays only as an
      optional single-tenant fallback, if kept at all — decide at implementation).
      The health `chat_available` flag becomes a per-user capability signal.
    - **Frontend gates the whole surface** on that signal: the Cmd+K binding,
      `ChatEdgeTab`, the `ChatPanel` mount in `PageLayout.tsx`, and the mobile
      `/chat` route + `MobileBottomNav` "Ask" entry all disappear when the
      current user has no key. No key = no assistant, not an error card.

    **Decisions:**
    - **Opt-in, hidden by default** — surfacing an assistant a user can't use
      trains them to distrust it; absence is cleaner than a disabled-state card.
    - **Encrypted at rest, write-only from the client** — the same discipline as
      connector tokens; a leaked settings response must never expose the key.
    - **Resolved (2026-07-11) — OAuth is off the table; BYO key paste is the only
      compliant path.** Web research settled the open question: as of Feb 2026
      (server-side enforced since Jan 2026, still in force July 2026) Anthropic
      restricts OAuth tokens (`sk-ant-oat01-`) to Claude Code and Claude.ai only,
      offers **no third-party client-ID registration**, and treats consumer-plan
      OAuth in any other product as a ToS violation. The single supported route
      for a multi-tenant app is a user-supplied API key (`sk-ant-api03-…`) from
      console.anthropic.com. So the plan **drops OAuth entirely** (not "treat as
      an upgrade") and standardizes on encrypted key paste in both UI and CLI,
      with a Console deep-link + billing hint to cut setup friction. Sources:
      WinBuzzer (2026-02-19), claude-code issue #28091, Claude Code auth docs.

    **Spec (starting points, revisit at implementation):**
    - Credential storage: encrypted per-user secret (new column/table keyed by
      `user_id`), reusing the connector-token encryption pattern.
    - Resolution: adapter/`get_llm_client` takes `user_id`; per-user capability
      endpoint (or extend `/health`'s `chat_available` to a per-user check).
    - Frontend: a `useChatAvailable()` gate consumed at every assistant mount
      point above; settings panel to manage the key with a live "test key" call.

    **Tests:**
    - (integration) no per-user key → capability reports unavailable; every
      assistant affordance is absent from the rendered tree.
    - (integration) a user's key resolves only for that user (RLS); user A's key
      never serves user B's chat turn.
    - (integration) key is never echoed back in any settings/GET response.
    - (component) settings add/test/remove flow; invalid key surfaces cleanly.

    **Why a revision, not v0.9.1:** it hardens the *shipped* v0.9.0 assistant for
    multi-tenant reality rather than adding new agent capability (that's v0.9.1).
    Ships as `feat:`. Effort: M.

---

### v0.9.1: Full Capability Parity (In-App)

**Goal**: After v0.9.0 the assistant can build workflows but little else — ask it to tag the tracks it just found and it has no hands. v0.9.1 makes the D4 contract real: every read a user can perform in the web UI or CLI becomes a registry tool, every mutation becomes a two-phase-confirmed tool, long-running operations stream their progress into the chat, and everything the agent does is visible in the same UI a human uses. When this ships, "ask the assistant" is a legitimate alternative to every screen in the app.

**Context**: Mixd's surface is already crisply enumerated — 68 use cases in `src/application/use_cases/` mirrored by ~60 CLI subcommands and 15 web routes, all funneling through `execute_use_case()` with the `user_id` RLS seam. This milestone is coverage work over that enumeration: consolidated tools (couplefins learning: one well-described tool wrapping several related use cases beats 1:1 wrapping — their 68-use-case equivalent surface collapsed to ~31 tools), plus the plumbing to bridge mixd's existing long-operation machinery into chat.

**What this unlocks**:
- "Which starred tracks haven't I played in 6 months? Tag them `context:revival` and add them to my Revival playlist" — a three-surface task, end-to-end in one conversation
- Library maintenance by conversation: merge duplicates, fix mappings, resolve match reviews, re-sync playlists
- Imports and syncs launched from chat with live progress, not fire-and-forget
- The parity matrix flips from "classified" to "covered or justified" — the audit v0.9.3's MCP server inherits for free

**Persona**: Weekly Curator — primary. The Sunday ritual (review imports, fix mismatches, refresh playlists) becomes delegable sentence by sentence. Casual Enthusiast — secondary. Everyday actions ("add this to my dinner playlist") without learning where they live in the UI. Tinkerer — tertiary. The registry breadth is what makes v0.9.3's MCP surface worth pointing external agents at.

**Key Design Decisions**:
- **Consolidate, don't 1:1-wrap** — tools are shaped around user intent ("manage tags on tracks") rather than use-case granularity; each `ToolSpec.use_cases` tuple records which use cases it covers so the parity test stays mechanical.
- **Every mutation is a proposal** — `kind: "write"` tools return a pending action with a human-readable before/after preview; the ConfirmationCard must show enough detail to decide without leaving chat (couplefins approval-fatigue finding: vague confirmations train users to click yes).
- **Long-running mutations reuse the operations machinery** — tools that start syncs/imports/runs return the existing `{"operation_id"}` contract; the chat panel subscribes to the existing per-operation SSE progress endpoint (`routes/operations.py`, Last-Event-ID reconnect included) rather than inventing a chat-specific progress channel.
- **Agent actions are auditable in the human UI** — assistant-initiated mutations record through the same operation-run/audit surfaces humans see (`/settings/imports` log; entity timestamps), attributed to the assistant, so the human side of D4 holds without a new activity-feed subsystem.
- **Blacklist is explicit and tested** — connector OAuth/token flows, account management, and `mixd admin reset` live in `BLACKLISTED_USE_CASES` with rationale strings; the parity test fails if a blacklisted capability grows a tool.

#### Implementation Progress & Learnings (updated 2026-07-11)

**All five epics implemented as of 2026-07-11** — pending live smoke + version-bump gate. The registry grew from 6 tools to **31** (14 read / 17 write, 5 of which launch operations); the parity contract is **closed** (`NOT_YET_COVERED` empty, tripwire test inverted to assert emptiness). 82 use cases: 74 covered, 8 excluded. Verified by the full fast test suite + `basedpyright src/` + `ruff` clean; capability matrix generated and freshness-tested.

| Epic | Status | Landed at |
|---|---|---|
| Read-Tool Coverage | ✅ Done | 9 tools in `src/application/chat/dispatchers/{library,tags,playlists,links,stats,operations,workflows_read,schedules}.py` |
| Mutation-Tool Coverage | ✅ Done | 11 tools in `dispatchers/*_write.py`; `ConfirmationCard.tsx` before/after + destructive banner |
| Long-Running Ops in Chat | ✅ Done | `dispatchers/long_ops.py` (5 tools) + `interface/api/services/chat_operations.py` launcher + `cards/OperationProgressCard.tsx` |
| Parity Closure + Matrix | ✅ Done | `NOT_YET_COVERED` emptied; `scripts/generate_capability_matrix.py` → `docs/web-ui/capability-matrix.md` + freshness test |
| Agent Activity Visibility | ✅ Done | migration `037` `operation_runs.initiated_by`; Assistant badge in `ImportHistoryPage.tsx` |

**Key learnings — don't rediscover:**

- **Dispatchers are a package, not one file** — `tool_executor.py` was already 388 lines for 6 tools; the 20 new tools live in `src/application/chat/dispatchers/` by domain, each exposing a `SPECS: list[dict]` the registry collects (dicts, not `ToolSpec`, to avoid a `registry → dispatchers → registry` import cycle). `registry._spec_from_mapping` is the one typed conversion point.
- **The `<user_data>` marker became eager-wrap, not a `UserText` object.** A `str`-subclass marker trips ruff FURB189, and a wrapper object cascaded a custom recursive type through `ToolDispatch`/`execute_tool`/`use_case.py`. Instead dispatchers call `user_text(v)` → `wrap(v)` eagerly (plain `str`), reusing the *shipped* v0.9.0 boundaries: `json.dumps(summary)` carries the tags to the model, `strip_user_data(summary)` strips them for the frontend. No new type, no `noqa`. `resolve_user_data`/`UserText` were removed.
- **Parity is `what has a human surface`, not `wrap every use case`.** Six use cases with no direct route/CLI are `INTERNAL`, reachable by the agent the same way a human reaches them — via a workflow: `EnrichTracksUseCase`, `MatchAndIdentifyTracksUseCase` (needs a live connector instance), `Create/UpdateConnectorPlaylistUseCase` (destination nodes), plus `ExecuteWorkflowRunUseCase` and the SSE-watchdog `GetOperationSnapshotUseCase`. `MatchAndIdentify` also can't be driven standalone (infra connector). Blacklist stayed just `RecordChatFeedbackUseCase`; mechanically-excluded just `ExportLastFmLikesUseCase` (spotify-history import joined it in spirit — no chat file channel).
- **Consolidate by intent** — 74 use cases collapsed to 31 tools via `operation`/`scope`/`view` enum discriminators (e.g. `manage_tags` spans tag/untag/batch/rename/merge/delete). No `oneOf`/numeric bounds (Anthropic rejects both); per-op required-field gating lives in prose + dispatcher validation raising actionable `ToolExecutionError`.
- **Long ops can only launch from the interface layer.** `launch_sse_operation` is FastAPI-bound, so the 5 long tools are `kind:"write"` with `launches_operation=True` and no application `executor`; the registry runs them through an injected `OperationLauncher` (`chat_operations.py`) at confirm time. The `operation_id` reaches the frontend as a synthetic `tool_result` SSE frame (`{status:"operation_started", operation_id, run_id, description}`) the `ToolResultCard` dispatches to `OperationProgressCard` (which reuses the existing `useOperationProgress` hook — no chat-specific progress channel).
- **`ConfirmedExecutor` stayed `(action, user_id)`** — the launcher threads through `execute_confirmed_action(..., operation_launcher=)` and the `launches_operation` flag, so the 11 synchronous write executors were untouched. The write-tool invariant is now executor-XOR-launches_operation, CI-checked.
- **Two shipped-code deltas for attribution**: `launch_sse_operation` + `launch_playlist_link_sync` gained `initiated_by="manual"` (default keeps every existing caller unchanged); the chat launcher passes `"assistant"`. Workflow runs use the separate `workflow_runs` log, so their attribution is out of the operation-run badge's scope.

#### Epics

- [ ] **Read-Tool Coverage (Library, Playlists, Tags, Preferences, Workflows, Runs, Stats)**

    **Story**: A user wants any question answerable in the web UI or CLI — "what's in my Revival playlist?", "which imports failed last week?", "which match reviews are pending?" — answerable in chat, so the assistant is never blind to state the user can see.

    **Decisions**:
    - Read tools dispatch to existing use-case query paths; no new persistence work expected.
    - Cross-tool consistency: every list tool takes the same `limit`/`cursor` shape; every get tool takes the canonical ID type; results serialize via existing Pydantic API schemas where possible (DRY with the REST layer).
    - Tool grain follows intent clusters, not modules — e.g. one `query_library` tool spanning list/search/detail beats three near-identical tools competing for the model's attention.

    **Spec**:
    - Registry entries covering (grouping indicative, grain decided at implementation): library search/detail/track-playlists; playlist list/detail/links; sync preview; tags + vocabulary; preferences + stats; workflows/runs/versions/schedules; operation runs; match reviews + method health; connector playlists/identities; dashboard stats; data-integrity check
    - Each: registry entry + dispatcher delegating to the existing query path, `use_cases` tuple filled

    **Tests**:
    - (integration) each read tool returns the expected shape against a seeded fixture user
    - (integration) pagination cursors round-trip; user scoping holds (no cross-user leakage)
    - (unit) parity test: all read-side use cases move from `NOT_YET_COVERED` to covered

    Effort: M | Dependencies: v0.9.0 (Build Shared Tool Registry + Parity Contract) | Status: Completed (2026-07-11)

- [ ] **Mutation-Tool Coverage with Two-Phase Confirmation**

    **Story**: A user wants to *do things* by asking — tag tracks, set preferences, edit playlists, merge duplicates, link and sync connector playlists — but never silently: each mutation shows exactly what will change, and nothing commits until they approve.

    **Decisions**:
    - All mutation tools are `kind: "write"`: first call returns a pending action + human-readable preview (before/after detail on the ConfirmationCard); approval executes through the same use case the UI calls.
    - Reuse the shipped confirm-token semantics where they exist (playlist sync already returns 409 `CONFIRMATION_REQUIRED` with a preview) — the chat pending-action store fronts them, it doesn't duplicate them.
    - Batch-first shapes: `tag_tracks`/`set_preferences` accept track-id lists (mixd's batch-first principle; also what makes v0.9.2's programmatic tool calling pay off).
    - Destructive-tier actions (delete workflow, delete playlist, unlink) get sharper preview copy; the blacklisted tier (account deletion, admin reset) has no tool at all.

    **Spec**:
    - Registry entries covering: playlist CRUD + entries (add/remove/reorder/repair); playlist links (create/update/delete) + sync; playlist assignments; tag add/remove/batch; preference set/clear/sync-from-likes; track merge/relink/unlink/set-primary-mapping; match-review resolve; workflow save/update/delete/revert + schedule; connector-playlist import/refresh
    - All execution lands in existing use cases; no new persistence logic

    **Tests**:
    - (integration) first call previews without mutating; approval commits and returns post-mutation state
    - (integration) expired action → typed error + fresh proposal path; owner mismatch rejected
    - (integration) every mutation scoped to the resolved user (RLS)
    - (unit) parity test: all write-side use cases covered or explicitly excluded with rationale

    Effort: L | Dependencies: Read-Tool Coverage | Status: Completed (2026-07-11)

- [ ] **Long-Running Operations in Chat**

    **Story**: When the assistant kicks off a Last.fm history import or a workflow run, the user wants the same live progress they'd get on the Sync page — a progress indicator in the conversation, a clear completion summary, and no frozen panel or silent background mystery.

    **Decisions**:
    - Tools that start long operations return the existing `{"operation_id"}` contract as their tool result; the model reports the launch and the frontend attaches a progress affordance to that tool-result card.
    - The chat panel subscribes to the existing `GET /operations/{id}/progress` SSE endpoint (keepalives + Last-Event-ID reconnect already shipped in v0.8.5) — no parallel progress channel.
    - Completion emits a compact summary the model can narrate on the user's next turn; the operation also lands in the run log like any human-initiated one.
    - Workflow runs reuse the run-detail deep link for full inspection ("Open run").

    **Spec**:
    - Registry entries: `run_workflow`, `import_play_history`, `import_likes`, `sync_playlist_link` (and other operation-returning mutations) — `kind: "write"`, confirmation precedes launch
    - New frontend piece: `OperationProgressCard` in `web/src/components/chat/cards/`, driven by the existing operations SSE hook
    - Chat tool results carry `operation_id` in a typed envelope the card dispatcher recognizes

    **Tests**:
    - (integration) operation-returning tool → pending action → approval → `operation_id` result; operation recorded in the run log
    - (component) progress card renders live updates and terminal states (complete/error/cancelled)
    - (frontend integration) SSE reconnect on the progress card resumes via Last-Event-ID

    Effort: M | Dependencies: Mutation-Tool Coverage with Two-Phase Confirmation | Status: Completed (2026-07-11)

- [ ] **Parity Contract Closure + Capability Matrix**

    **Story**: A user wants confidence that "anything I can do in the web UI or CLI, the assistant can do" is a checked fact, not a slogan — and a maintainer wants the check to run in CI so the next shipped feature can't silently break it.

    **Decisions**:
    - The parity test flips from "every use case classified" (v0.9.0) to "every use case covered or justified": `NOT_YET_COVERED` must be empty.
    - The human-readable capability matrix is **generated from the registry**, not hand-maintained (hand-synced mirrors of generatable truth always decay — the pattern that bit the api-contracts skill twice): capability | covering tool | or exclusion rationale.
    - Blacklist rationale strings are part of the code, rendered into the doc.

    **Spec**:
    - Parity test tightened in `tests/unit/application/tools/test_registry_parity.py`
    - Generator script emits `docs/web-ui/capability-matrix.md` (wired into the docs build or checked-in with a freshness test)
    - README "What Mixd Does" gains the agent-surface sentence

    **Tests**:
    - (unit) `NOT_YET_COVERED` empty; every exclusion carries a non-empty rationale
    - (unit) generated matrix parses as a well-formed table with no blank cells

    Effort: S | Dependencies: Mutation-Tool Coverage with Two-Phase Confirmation | Status: Completed (2026-07-11)

- [ ] **Agent Activity Visibility**

    **Story**: A user returning to mixd wants to see what the assistant did while they conversed — which playlists changed, which tags were added, which imports ran — in the same places they'd look for their own actions, so trusting the agent never requires trusting their memory of the conversation.

    **Decisions**:
    - Assistant-initiated mutations are attributed: operation runs and audit-visible changes record an `initiated_by: assistant` marker (shape decided at implementation — likely a source column on the existing operation-run record), rendered as a small badge in the run log.
    - **Not building** a separate activity-feed subsystem — the existing operation-run log (v0.7.7) plus entity-level recency is the surface; a dedicated feed waits for the v1.1.x social-layer work where feeds are first-class anyway.
    - Ephemeral conversations stay ephemeral — the *effects* are durable and attributed; the transcript is not.

    **Spec**:
    - Attribution field threaded through the chat tool executor → operation-run persistence; badge in `settings/ImportHistoryPage.tsx`
    - Non-operation mutations (tag/preference edits) surface via their normal entity timestamps; the confirmation card remains the point-in-time record

    **Tests**:
    - (integration) assistant-launched operation carries the attribution marker; human-launched does not
    - (component) run-log row renders the assistant badge

    Effort: S | Dependencies: Long-Running Operations in Chat | Status: Completed (2026-07-11)

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.9.2: Agentic Depth

**Goal**: With full parity the assistant can do everything — one tool call at a time, with every intermediate result flowing through the model's context. v0.9.2 makes it *efficient and long-horizon capable*: programmatic tool calling composes batch library operations in a sandbox (only the answer reaches the model), a research subagent absorbs deep analysis without polluting the conversation, tool search keeps the now-large registry out of the upfront context, and context management + a user-facing effort control keep long sessions fast and affordable. This is a port of couplefins v1.8.3 with mixd-shaped stories.

**Context**: All four capabilities are live-verified in couplefins with tuned constants and documented failure modes (their v1.8.3 "Implementation learnings" pin four programmatic-tool-calling bugs unit tests couldn't catch — port those as spec notes, not rediscoveries). Mixd's batch-first principle makes this milestone disproportionately valuable here: libraries have tens of thousands of tracks, and "re-tag everything matching X" is the canonical mixd ask.

**What this unlocks**:
- "Retag every track that's `mood:melancholy` but has >8 plays this month as `mood:bittersweet`" — one sandbox script over batch tools instead of hundreds of model round trips
- "Which of my starred tracks drifted out of rotation this year, and why might that be?" — delegated to a subagent that reads widely and returns one dense summary
- A registry of 30–40 tools that costs ~500 upfront tokens instead of the full schema set, with the prompt cache preserved
- Long curation conversations that don't degrade or blow the context window

**Persona**: Weekly Curator — primary. Batch maintenance and analysis are their core loop. Tinkerer — secondary. The sandbox + subagent behavior is the "this is a real agent" moment. Casual Enthusiast — tertiary. Faster, cheaper responses via effort control they never have to think about.

**Key Design Decisions**:
- **Read tools become sandbox-callable; write tools never** — `allowed_callers` includes the code-execution sandbox on `kind: "read"` entries only. Mutations always surface as individual proposals to the user; a sandbox script can *compute* a batch mutation but the commit still flows through one visible, confirmable tool call with the full change list in the preview.
- **The subagent is read-only by construction** — it gets `build_subagent_tools()` (read slice of the registry), a fresh context, low effort, and a bounded turn budget; its entire output is one summary string in the parent conversation.
- **Tool search is the default posture at this registry size** — a handful of hot tools (workflow generation, library search, tag/preference basics) stay non-deferred; everything else is `defer_loading: true` with a system-prompt "search before you claim you can't" nudge (couplefins found the nudge necessary).
- **Context editing over compaction** — `clear_tool_uses` with couplefins' thresholds as starting points (trigger 30k input tokens, keep 3 tool uses, clear ≥5k); server-side compaction stays evaluated-and-skipped while conversations are bounded by the 50-message cap. Revisit only if persistence ships.
- **Effort is the user's knob** — Quick/Standard/Thorough in the panel (low/high/xhigh), persisted locally, default Standard; the subagent is always low.
- **Tool loading follows the user's screen** — the web client sends the coarse UI section and its tools promote into the loaded set (rule-based routing; ≤3/page to hold the ~10-tool ceiling). Promoted tools ride the *uncached tail* so the cached core prefix stays page-invariant — navigating between pages never re-pays for the tool schemas. Strictly additive: an unrouted page degrades to the static core + tool search.

#### Implementation Progress & Learnings (updated 2026-07-11)

**All four core epics implemented as of 2026-07-11** (a fifth, **page-contextual tool routing**, followed 2026-07-12) — pending live smoke + version-bump gate. The registry grew from 31 to **34 tools** (3 new `agentic`: `code_execution`, `delegate_analysis`, `tool_search_tool_bm25`); at ~40 tool specs the loading posture flipped **deferred-first** (7 upfront: 4 hot reads + 3 agentic, the rest discovered via BM25 search), then gained rule-based per-section promotion on top. On branch `feat/v0.9.2-agentic-depth`. Verified by the fast chat+tools suites + `basedpyright src/` + `ruff` clean; capability matrix regenerated.

| Epic | Status | Landed at |
|---|---|---|
| Code Execution Sandbox + PTC | ✅ Done | `code_execution` server tool in `registry._AGENTIC_TOOLS`; `build_tools(enable_code_execution=)` stamps `allowed_callers=["direct","code_execution_20260120"]` on read tools + emits server-tool schemas verbatim; `CHAT__ENABLE_CODE_EXECUTION` default on |
| Research Subagent | ✅ Done | `src/application/chat/subagent.py` (`run_subagent`); `ToolContext.llm` handle; `delegate_analysis` agentic tool + `build_subagent_tools()`; `ToolCallIndicator` agentic branch |
| Tool Search + Deferred Loading | ✅ Done | `ToolSpec.defer_loading` default→True; hot set = `describe_node`/`generate_workflow_def`/`query_library`/`list_tags`; `tool_search_tool_bm25` tool; `<tool_habits>` search-first nudge |
| Context Management + Effort | ✅ Done | Context editing + effort selector already shipped v0.9.0 (verified); net-new is the subagent-always-low-effort guard test |
| Page-Contextual Tool Routing | ✅ Done | `build_tools(page=)` prefix/tail split (page-invariant cached prefix); `_PAGE_TOOL_HINTS` + `ChatRequest.page` → `pageSection()` in `ChatPanel`; curated `build_subagent_tools()` hot set + `tool_search` |

**Key learnings — don't rediscover:**

- **`allowed_callers` is stamped in `build_tools`, not stored per-spec.** couplefins computes it uniformly for every read tool at build time (`if enable_code_execution and kind == "read"`), so mixd has **no** `ToolSpec.allowed_callers` field — the "reads only" rule is a property tested on `build_tools()` output. Simpler than a per-spec field and matches the reference.
- **Server tools (`dispatch is None` agentic) need a distinct `build_tools` branch.** Their `input_schema` holds the raw `{type, name}` block the API wants; the `{name, description, input_schema}` wrapper is emitted only for dispatched tools. Three registry quality tests (`test_descriptions…`, `test_input_schemas…`, the cache-breakpoint test) had to skip `dispatch is None` entries.
- **SDK 0.116 ships both `code_execution_20260120` and `_20260521`.** couplefins (SDK 0.97) said `20260521` didn't exist; on 0.116 both do. Use **`20260120`** — the REPL-persistence + programmatic-tool-calling variant that `allowed_callers` references. `BetaToolUseBlock.caller`, `BetaMessage.container`, and `BetaToolSearchToolBm25_20251119Param` all resolve in 0.116, so the ported adapter needed **no changes** — the live sandbox smoke is the authoritative block-shape check.
- **Flipping `defer_loading` default to True forces `defer_loading=False` on every agentic tool** — the construction invariant (`agentic must not be deferred`) fires at import otherwise. Hot dispatcher reads opt in via `"defer_loading": False` in their `SPECS` dict; `_spec_from_mapping` now threads the flag (default True).
- **The subagent needs no per-user injection.** couplefins injects `persons` into the subagent prompt; mixd's music-domain prompt is static + today's date (the subagent discovers library shape via read tools), so `ToolContext` only gained the optional `llm` handle — dispatcher tests still construct `ToolContext(user_id=…)` without one.
- **The frontend was already wired, not dormant** — the v0.9.0 "DORMANT" comments in `CodeExecutionCard.tsx` and `lib/effort.ts` were stale; the effort selector, code-execution card, and `code_start`/`code_result` SSE parsing all shipped and were committed in v0.9.1. The only net-new UI is the `kind === "agentic"` branch in `ToolCallIndicator` (`delegate_analysis` now reads "Running deep analysis…" / "Ran deep analysis" instead of a generic lookup).
- **Page routing is free on the cache axis *only* if the cached prefix is page-invariant.** Toggling `defer_loading` in place would put a promoted tool before the cache breakpoint and bust the tools cache on every navigation. `build_tools` instead segregates a page-invariant cached **prefix** (curated core + dispatched agentic) from an uncached **tail** (deferred/promoted pool + raw server-tool blocks); per-page variation only mutates bytes *after* the breakpoint. The same restructure fixed a latent bug — the breakpoint had been landing on the trailing `tool_search` server block (a `{type,name}` block that rejects `cache_control`); it now sits on `delegate_analysis`, asserted by `test_build_tools_stamps_one_cache_breakpoint`.

#### Epics

- [x] **Code Execution Sandbox + Programmatic Tool Calling**

    **Story**: A curator wants batch answers and batch changes over a large library — "how many of my 2024 discoveries did I star, by month?" or "queue a retag of everything matching this filter" — without watching the assistant grind through hundreds of visible tool calls, and without those intermediate results degrading the conversation.

    **Decisions**:
    - Server tool `code_execution` added as an `agentic` registry capability; read tools gain `allowed_callers` covering both direct and sandbox invocation (couplefins deviation from docs' pick-one guidance, deliberately — avoids container spin-up for one-shot reads).
    - Dual turn budgets ported: model-turn budget plus a separate sandbox-round backstop, so sandbox iteration can't starve the conversation budget.
    - Port couplefins' four live-caught pitfalls as implementation notes: cache_control rejection on sandbox tool blocks, container-id echo handling, SDK content-block union gaps, sandbox rounds vs turn accounting.
    - Sandbox results stream as `code_start`/`code_result` events into the (already-shipped) `CodeExecutionCard`; stdout/stderr truncated (couplefins: 2KB) with full results available in the tool result payload.
    - Batch mutations composed in the sandbox still commit through one confirmable `write` tool call carrying the computed change list.

    **Spec**:
    - Registry: `code_execution` entry (`kind: "agentic"`); `allowed_callers` stamped on read entries in `build_tools()`
    - Adapter: server-tool block round-tripping, container round-trip, exact tool-type id verified against the installed SDK at implementation (couplefins hit a docs-vs-SDK mismatch here)
    - Settings: `enable_code_execution` flag; disabled cleanly degrades to direct tool calls
    - Frontend: activate `CodeExecutionCard` + `codeExecutions[]` store slot shipped dormant in v0.9.0

    **Tests**:
    - (integration) a multi-read aggregation question triggers sandbox execution and returns only the aggregate to the conversation
    - (integration) sandbox-composed batch mutation surfaces as a single confirmable proposal with the full change list
    - (unit) write/agentic tools never carry `allowed_callers`; sandbox backstop halts runaway rounds
    - (component) code card renders start/result, collapsed by default

    Effort: L | Dependencies: v0.9.1 (Mutation-Tool Coverage) | Status: Completed (2026-07-11)

- [x] **Research Subagent (Delegated Analysis)**

    **Story**: A curator wants deep, multi-source analysis — "compare my listening this spring vs last spring and tell me what actually changed" — without the main conversation absorbing every intermediate query, so the thread they're working in stays fast and on-topic.

    **Decisions**:
    - Port `delegate_analysis`: a registry `agentic` tool that spins a fresh chat loop (the injected-executor seam from v0.9.0) with a subagent system prompt, the read-only registry slice, low effort, and a bounded turn budget (couplefins: 12).
    - Output contract: one dense summary (≈1,500-token target); truncation returns partial text with an explicit truncation prefix rather than failing.
    - The subagent may use the sandbox for aggregation but has no write path at all.

    **Spec**:
    - `src/application/chat/subagent.py` — `run_subagent()` reusing `ChatUseCase` with `build_subagent_tools()`
    - Registry entry `delegate_analysis` (`kind: "agentic"`, not sandbox-callable)
    - Subagent activity surfaces in the panel as a single tool-call indicator with elapsed state, not a nested transcript

    **Tests**:
    - (integration) delegation returns a bounded summary; parent context grows by the summary only
    - (unit) subagent toolset contains no `write` entries; turn budget enforced with truncation prefix
    - (component) indicator shows running/complete states

    Effort: M | Dependencies: Code Execution Sandbox + Programmatic Tool Calling | Status: Completed (2026-07-11)

- [x] **Tool Search + Deferred Loading**

    **Story**: With 30–40 parity tools, the user wants the assistant to stay accurate and cheap — picking the right tool reliably instead of wading through a schema wall on every request (accuracy degrades past roughly ten upfront tools, per Anthropic's guidance and couplefins' experience).

    **Decisions**:
    - BM25 tool-search variant; `defer_loading: true` on everything except a curated hot set (workflow generation, library search, tag/preference basics — tuned at implementation).
    - System prompt gains the search-first nudge in `<tool_habits>` — without it the model claims capabilities are missing (couplefins finding).
    - Cache interaction is the point: discovered schemas append rather than swap, preserving the tools/system prefix cache.

    **Spec**:
    - Registry: `tool_search` entry; `build_tools()` stamps `defer_loading` and keeps the cache breakpoint on the last non-deferred tool
    - Hot-set membership is data (a flag on `ToolSpec`), not executor logic

    **Tests**:
    - (integration) a deferred tool gets discovered and invoked for a request that needs it
    - (unit) hot set stays under the accuracy threshold; search tool itself never deferred
    - (integration) cache-read tokens stay nonzero across a discovery turn (no cache invalidation)

    Effort: S | Dependencies: v0.9.1 (Read-Tool Coverage) | Status: Completed (2026-07-11)

- [x] **Context Management + Effort Control**

    **Story**: A curator deep in a long maintenance session wants turn 40 to feel like turn 4 — no degradation, no context-window failure, and a way to say "this one's simple, answer fast" or "take your time and be thorough" without knowing what a token is.

    **Decisions**:
    - Context editing (`clear_tool_uses`) with couplefins' tuned thresholds as starting points: trigger 30k input tokens, keep last 3 tool uses, clear ≥5k at a time.
    - **Compaction stays skipped** — conversations are bounded (50-message cap, ephemeral); record the rationale so the decision is revisited deliberately if persistence ever ships, not re-litigated.
    - Effort selector in the panel: Quick/Standard/Thorough → low/high/xhigh, persisted to localStorage, sent per request (the API has accepted it since v0.9.0); default Standard.

    **Spec**:
    - Adapter: `context_management` config on the beta stream call; thresholds in `ChatConfig`
    - Frontend: effort selector in `ChatInput`, store field + persistence, label copy avoiding jargon
    - Config: `effort` default + subagent override already present from v0.9.0/subagent epics

    **Tests**:
    - (integration) a long synthetic session crosses the trigger and continues correctly with cleared tool results
    - (component) effort selection persists across reloads and rides the next request
    - (unit) subagent ignores user effort selection (always low)

    Effort: S | Dependencies: Research Subagent (shares config surface) | Status: Completed (2026-07-11)

- [x] **Page-Contextual Tool Routing**

    **Story**: A curator working on the Playlists page asks "which of these have gaps this month?" and wants the assistant to reach for playlist tools straight away — no "let me search for the right tool" detour — while the same question on the Library page leans on library tools. The user wants the assistant to feel like it already knows what screen they're looking at, without paying the accuracy tax of loading every tool upfront.

    **Decisions**:
    - **Route by the UI section, rule-based not semantic** — a route is the cleanest domain signal there is, so a static hint map beats a classifier (deterministic, zero added latency). Matches the 2026 progressive-disclosure consensus: classify and load the relevant tools *before* the request enters the window.
    - **Promoted tools ride the uncached tail so navigation never busts the tools cache** — the cached core prefix is page-invariant by construction; per-page variation costs a few re-sent schemas, not a cache miss on every page change (the naive in-place `defer_loading` toggle would have).
    - **Capped at ≤3 promotions per page** — keeps the loaded set under the ~10-tool ceiling past which selection accuracy degrades (OpenAI's own guidance; corroborated across the progressive-disclosure literature).
    - **Strictly additive** — an unknown or absent section promotes nothing and degrades to the static core + tool-search discovery, so a new route never breaks the assistant.
    - **Same discipline extended to the research subagent** — its flat 14-read toolset became a curated ≤10 hot set + `tool_search`; low effort is exactly where a bloated tool list most degrades selection.

    **Spec**:
    - Registry: `build_tools(*, enable_code_execution, page)` splits tools into a page-invariant cached **prefix** (curated core + dispatched agentic) and an uncached **tail** (server-tool blocks + deferred/promoted pool); the single cache breakpoint sits on the prefix's last (dispatched) tool. `_PAGE_TOOL_HINTS: Mapping[str, tuple[str, ...]]` + `_promoted_tool_names(page)`.
    - Subagent: `build_subagent_tools()` → `_SUBAGENT_HOT_TOOLS` (7 reads) loaded + `tool_search_tool_bm25`; the rest of the reads deferred behind search.
    - API: `ChatRequest.page: str | None` (≤64 chars) → `routes/chat.py` → `build_tools(page=…)`.
    - Frontend: `pageSection(pathname)` in `ChatPanel.tsx` (index→`dashboard`; `SECTION_BY_SEGMENT` kept in sync with `_PAGE_TOOL_HINTS`) threaded through `startStream` → `sendChatMessage` body `page`.
    - Hint map (starting point, revisit as sections stabilize): `playlists`, `library`, `workflows`, `dashboard`, `imports`.

    **Tests**:
    - (unit) every page's loaded set stays ≤10; the cached prefix is byte-identical across all pages (navigation never invalidates the cache); a page's hinted tools load while off-page they stay deferred.
    - (unit) subagent hot set ≤10 with `tool_search` present and one cache breakpoint on a read tool.
    - (component) the current UI section rides the request; an unrouted page (`/settings/account`) sends none.

    **Reference**: the pattern, the cache-invariance rationale, and a framework-agnostic porting recipe are written up as a teaching doc — [`docs/architecture/context-engineering-tool-routing.md`](../architecture/context-engineering-tool-routing.md).

    **Follow-ups** (not blocking ship): budget reclamation (demote workflow tools off-page) and hint-map formalization — both gated on a July-2026 best-practices research pass, tracked in [v0.9.4 Follow-Ups & Hardening](#v094-follow-ups--hardening) *(shipped 2026-07-16)*.

    Effort: S | Dependencies: Tool Search + Deferred Loading | Status: Completed (2026-07-12)

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.9.3: MCP Server (Mixd as a Tool Surface)

**Goal**: Expose mixd's capabilities as an MCP server so any MCP-aware client — Claude Desktop, Cursor, Claude Code, future agentic tools — can read from and act on a user's mixd library. By now the registry is parity-complete and battle-tested by the in-app assistant, so this milestone is **transport, long-op mapping, and exposure** — not new tool logic.

**Context**: The MCP 2026-07-28 revision is final before this milestone starts: stateless core (no handshake/sessions — the SDK owns protocol mechanics), a Tasks extension for long-running work, and MCP Apps for server-shipped UI. Build on the official `mcp` Python SDK at its post-2026-07-28 release; design tools stateless-first (explicit handles, no server-held conversation state).

**What this unlocks**:
- Local-first agentic workflows: "Claude, find all my starred tracks from last year that aren't on my Friday playlist, and add them" — from Claude Desktop or Cursor, no code.
- Long operations (workflow runs, imports) as first-class MCP Tasks with progress and cancellation, not blocked calls.
- The parity matrix doubles as the MCP coverage audit — generated, not hand-audited.

**Persona**: Tinkerer — primary. Self-hosters and power users who already pipe LLMs into their toolchains. Curator — secondary. Ad-hoc scripted maintenance ("deduplicate my 'Loved' playlist against my starred preferences"). Casual Enthusiast — tertiary, via whatever agents others build on top.

**Key Design Decisions**:
- **Stdio-only; identity from the environment.** Stdio matches the spec's recommendation and mixd's local-first deployment; the server resolves the user exactly as every CLI command does — `get_cli_user_id()` (`MIXD_USER_ID` env → `DEFAULT_USER_ID`). No new credential surface.
- **Remote/Streamable-HTTP MCP is scoped separately as [v0.9.5](#v095-remote-mcp-server-your-production-library-from-any-agent)**, not here: it requires OAuth 2.1 resource-server work under the 2026-07-28-hardened authorization spec, with mixd's Neon Auth JWT infrastructure as the authorization server. v0.9.3 ships the local stdio server; v0.9.5 makes the same registry reachable on the Fly/Neon production deployment so any agent can act on a user's live account.
- **One registry, second consumer**: `list_tools`/`call_tool` iterate `src/application/tools/registry.py`. `agentic` entries (sandbox, subagent, tool search) are chat-executor concerns and are **not exposed** over MCP — the MCP client brings its own agentic loop.
- **Annotations derived from `kind`**: `readOnlyHint = (kind == "read")`, `destructiveHint = (kind == "write")`, `idempotentHint: true` on read tools, `openWorldHint: false`. The spec marks annotations as untrusted — mutation safety never relies on them.
- **Mutations require explicit confirmation in-band**: any `write` tool called without `confirm=true` returns a structured preview + confirm token instead of executing. Well-behaved clients will also prompt on `destructiveHint`; mixd doesn't depend on it.
- **Long-running tools use the Tasks extension**: `run_workflow` and imports answer `tools/call` with a task handle backed by mixd's existing operation records; `tasks/get` maps to operation status, `tasks/cancel` to operation cancellation. Clients without the extension get the blocking fallback with a documented timeout caveat.
- **Structured output where it helps**: read tools may declare `outputSchema` and return `structuredContent` reusing existing Pydantic API schemas — decide per tool during implementation, not a blanket requirement.

#### Implementation Progress & Learnings (updated 2026-07-12)

**Now-slice (scaffold-first) implemented as of 2026-07-12** — pending live client
smoke (Claude Desktop) + version-bump gate. Per the scheduling decision,
v0.9.3 started 16 days *before* the MCP 2026-07-28 spec/SDK
finalise, against the locked RC and beta SDK: the **Scaffold**, **In-Band
Confirmation**, and the SDK-light parts of **Exposure Polish** ship now; the
**Tasks extension** (long-op tools) is deliberately gated to after 2026-07-28. On
branch `feat/v0.9.3-mcp-server`. Verified by 29 new unit tests (registry→MCP
bridge, two-phase confirmation, a real ClientSession transport round-trip, install
CLI) + the full fast suite (3725 passed) + `basedpyright src/` + `ruff` clean;
capability matrix regenerated with the MCP column.

| Epic | Status | Landed at |
|---|---|---|
| MCP Server Scaffold (stdio, stateless) | ✅ Done | `src/interface/mcp/server.py` (low-level `Server`, `list_tools`/`call_tool` over `TOOLS`), `mcp_annotations`, `mixd mcp serve` |
| In-Band Mutation Confirmation | ✅ Done | `src/interface/mcp/confirmation.py` (`handle_write_call`) reusing `pending_action_store` + `execute_confirmed_action` |
| Exposure Polish (SDK-light parts) | ◐ Partial | `mixd mcp install` + `install.py`; `docs/guides/mcp.md`; MCP column in `generate_capability_matrix.py` via shared `exposure.py`; README sentence. **Long-op exposure flip waits for the Tasks epic.** |
| Long-Running Tools via Tasks | ⏳ Gated | Deferred to post-2026-07-28 (stable SDK + published Tasks extension) |
| MCP Apps (candidate) | Not Started | Candidate, unchanged |

**Key learnings — don't rediscover:**

- **SDK is `mcp==2.0.0b1` + `mcp-types==2.0.0b1` (both pre-release).** uv won't pull
  the transitive `mcp-types` pre-release unless it's *also* directly pinned — pin
  both (their specifiers match only pre-releases, so pre-releases aren't enabled
  globally). RE-VERIFY CHECKPOINT baked into `pyproject.toml`: bump to stable v2
  on/after 2026-07-28 and re-run `tests/unit/interface/mcp/` before the Tasks epic.
- **v2 low-level API differs from v1 and from the README (which only shows the
  high-level `@mcp.tool()` decorator).** Types moved to the separate `mcp_types`
  package (no more `mcp.types`); the low-level `Server` dropped the
  `@server.list_tools()`/`@server.call_tool()` decorators for
  `add_request_handler(method, params_type, handler)` (`"tools/list"` →
  `PaginatedRequestParams`, `"tools/call"` → `CallToolRequestParams`; handler
  returns a `BaseModel`). `Tool(input_schema=…)` serialises to `inputSchema`;
  annotation fields to `readOnlyHint` etc. The high-level `MCPServer.add_tool(fn)`
  infers schemas from Python functions, so it's the *wrong* tool for a
  registry-of-hand-built-schemas — the low-level `Server` is the faithful fit.
  All facts pinned in `server.py`'s module docstring.
- **stdout cleanliness is the killer gotcha, and it's an import-time problem.**
  structlog defaults to a **stdout** `PrintLogger` until `setup_logging()` runs, so
  import-time debug logs (the Rich console singletons in `console.py`/`cli_helpers`)
  land on stdout *before* any command body — fatal for a JSON-RPC channel. Fixed in
  two places: an early argv guard at the top of `app.py` (routes logging to stderr
  before the first import-time log for any `mixd mcp …` command) and the
  `init_cli` callback (stderr + skip the console-printing startup checks for the
  `mcp` group). `setup_logging` gained a keyword-only `console_stream`. Same
  discipline keeps `mcp install --print` stdout pure JSON.
- **The exposure filter is one shared classifier.** `src/interface/mcp/exposure.py`
  `mcp_exposure(spec)` (`exposed`/`agentic`/`pending_tasks`) is imported by both
  `server.exposed_specs()` and the capability-matrix generator — SDK-free so the
  script needn't import the MCP SDK, and so the doc can't drift from what the
  server actually exposes. 26 exposed / 5 pending-Tasks / 3 agentic today.
- **RUF029 per-file-ignore for `server.py`** follows the established FastAPI/
  `ToolDispatch` precedent: the SDK's request-handler protocol requires `async`
  handlers even where one (`tools/list`) does no I/O.
- **Long-op tools can't be exposed yet by construction.** The 5 `launches_operation`
  writes need an injected `OperationLauncher`, and the only implementation
  (`launch_chat_operation`) is FastAPI-coupled — so the Tasks epic bundles the
  launcher-decoupling refactor. Cleanly excluded now via `mcp_exposure == pending_tasks`.

#### Epics

- [x] **MCP Server Scaffold (Stdio, Stateless)**

    **Story**: A Tinkerer wants to point Claude Desktop at mixd and immediately see mixd's tools in the client's palette — the same way they'd add the filesystem server. No HTTP, no tokens, just a `mixd mcp serve` subprocess.

    **Decisions**:
    - Official Python MCP SDK at its post-2026-07-28 release, registered as a `mixd mcp serve` Typer subcommand. The SDK owns protocol mechanics (now stateless — no session logic to write, and none should be written); mixd owns tool metadata + dispatch.
    - User resolved via `get_cli_user_id()`; misconfigured state returns a clear per-call error with remediation, not a crash.
    - One UoW per tool call (`execute_use_case` with the resolved user id — RLS scoping comes free). Calls are independent; state crosses calls only via explicit handles (IDs in tool results).
    - **stdout is the protocol channel**: structlog routes to stderr/file (spec allows stderr for logging; stdout MUST carry only MCP messages).

    **Spec**:
    - New module `src/interface/mcp/server.py` — builds the server from the shared registry: name, description, `input_schema`, annotations derived from `kind`; `agentic` entries filtered out
    - New CLI subcommand `mixd mcp serve` in `src/interface/cli/`
    - Log path configurable via env var

    **Tests**:
    - (integration) server boots, responds to `tools/list` over stdio; tool metadata matches registry entries; no `agentic` tools exposed
    - (integration) `tools/call` on a read tool returns a valid result scoped to the resolved user
    - (unit) annotations correctly derived from `kind` for every exposed entry

    Effort: M | Dependencies: v0.9.1 (Parity Contract Closure) | Status: Completed (2026-07-12) — built against the locked RC + beta SDK (`mcp==2.0.0b1`); re-verify at the 2026-07-28 stable bump

- [x] **In-Band Mutation Confirmation over MCP**

    **Story**: An agent acting for the user wants to run a workflow or edit a playlist from Claude Desktop — but the user wants the same never-silent guarantee the in-app assistant gives: a preview first, and nothing committed until explicitly confirmed.

    **Decisions**:
    - Two-phase in-band: first call → `{ status: "needs_confirmation", preview, confirm_token }`; second call with `confirm=true` + token commits. Expired tokens return a fresh preview, never a stale commit.
    - Confirm tokens scoped to `(user_id, tool_name, args_hash)`, 5-minute TTL — same store pattern as the chat pending actions (possibly the same store). Args drift between preview and confirm is rejected with a clear error.
    - Previews reuse the same human-readable summaries the chat ConfirmationCard shows — one preview renderer, two consumers.

    **Spec**:
    - Confirmation wrapper in `src/interface/mcp/` fronting `kind: "write"` dispatch; token store shared with `src/application/chat/pending_actions.py`
    - Preview payload includes the structured change summary + prose rendering

    **Tests**:
    - (integration) first call previews without mutating; `confirm=true` commits and returns post-mutation state
    - (integration) expired token → fresh preview; args drift → rejection
    - (integration) every mutation scoped to the resolved user

    Effort: M | Dependencies: MCP Server Scaffold | Status: Completed (2026-07-12) — synchronous writes; args-drift rejected, expired/unknown token re-previews. Implemented in `confirmation.py`

- [ ] **Long-Running Tools via the Tasks Extension**

    **Story**: An agent kicks off a 50k-scrobble import or a workflow run from Claude Desktop; the user wants it to behave like real background work — observable progress, cancellable, no client timeout roulette on a blocked call.

    **Decisions**:
    - Advertise the Tasks extension; operation-returning tools answer `tools/call` with a task handle when the client supports it (server-directed, per spec). Handles map 1:1 to mixd operation ids.
    - `tasks/get` reads operation status/progress from the existing operation records; `tasks/cancel` routes to operation cancellation.
    - Clients without the extension get a blocking call with a documented duration caveat — no bespoke polling protocol.

    **Spec**:
    - Task adapter in `src/interface/mcp/tasks.py` mapping task handles ↔ operation ids
    - Applies to the same operation-returning tools v0.9.1 wired into chat

    **Tests**:
    - (integration) task-capable flow: call → handle → progress via `tasks/get` → completion state matches the run log
    - (integration) `tasks/cancel` cancels the underlying operation
    - (integration) non-task client falls back to blocking completion

    Effort: M | Dependencies: In-Band Mutation Confirmation over MCP | Status: Not Started — **gated to post-2026-07-28** (stable SDK + published Tasks extension). Bundles the `OperationLauncher` FastAPI-decoupling refactor, since the 5 `launches_operation` tools need an interface-layer launcher the current `launch_chat_operation` doesn't provide outside FastAPI. Until then they're excluded from MCP via `mcp_exposure == "pending_tasks"`.

- [ ] **Exposure Polish: `mixd mcp install`, Docs, Coverage**

    **Story**: A user wants to install the server in their MCP client with one command, and wants the "anything I can do, my agent can do" claim documented — with the same generated matrix the in-app parity contract produces, so there's exactly one source of truth.

    **Decisions**:
    - `mixd mcp install` writes the config snippet for the detected client; `--print` emits JSON for manual paste.
    - The coverage story reuses v0.9.1's generated capability matrix — this epic adds the MCP-exposure column (exposed / chat-only with rationale), it does not create a second audit.
    - Docs: `docs/guides/mcp.md` (install, tool list, 3–4 worked walkthroughs).

    **Spec**:
    - New CLI subcommand `mixd mcp install [--client claude-desktop|cursor|claude-code] [--print]`
    - Matrix generator gains the MCP column; `docs/guides/mcp.md`; README headline updated

    **Tests**:
    - (unit) install emits valid JSON per supported client; `--print` writes nothing
    - (unit) matrix MCP column populated for every registry entry (exposed or rationale)

    Effort: S | Dependencies: Long-Running Tools via the Tasks Extension | Status: Partially Completed (2026-07-12) — `mixd mcp install`, `docs/guides/mcp.md`, README sentence, and the generated matrix **MCP column** shipped now (they don't depend on Tasks). The long-op tools show `chat-only (pending Tasks)` in the matrix; flip to `exposed` when the Tasks epic lands.

- [ ] **(Candidate) MCP Apps: Workflow Preview in the Host**

    **Story**: A Tinkerer building workflows from Claude Desktop wants to *see* the proposed DAG — the same graph the web app renders — inside the conversation, instead of reading a JSON blob.

    **Decisions**:
    - **Candidate, not committed**: MCP Apps (2026-07-28 extension) lets the server ship a pre-declared HTML template rendered in a sandboxed host iframe. A static bundle of the `WorkflowGraph` renderer over a `WorkflowDef` payload is the natural first app.
    - Promote to committed scope only after the core server ships and host support (Claude Desktop et al.) proves out — extension adoption timing is the risk, not the build.

    **Spec (sketch)**:
    - Pre-declared UI template serving a self-contained graph-render bundle; `generate/get_workflow`-family tools reference it

    **Tests**: defined at promotion.

    Effort: M | Dependencies: Exposure Polish | Status: Not Started (candidate)

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.9.4: Follow-Ups & Hardening

> **🚀 Shipped 2026-07-16.** The named hardening milestone: a high-effort full-series review of v0.9.0–v0.9.3 (`git diff v0.8.18.3..HEAD` — ~16.5k source lines across the chat/registry/MCP surface) with fixes applied, plus the ready-now follow-ups the Pre-Work passes flagged. Ten finder angles × adversarial verification surfaced ~38 confirmed findings; all verified fixes landed. *(Remote/Streamable-HTTP MCP is a committed milestone of its own — [v0.9.5](#v095-remote-mcp-server-your-production-library-from-any-agent).)*

**Goal**: the assistant users can already reach for everything (v0.9.1 parity) and reach deeply (v0.9.2) and from any client (v0.9.3) should also be *trustworthy under failure* — no hung panels, no confirmation cards that lie, no injection laundering — before the surface widens further.

**Context**: five rapid ships (2026-07-11/12) had never had a whole-series review. Prod-shaped failure modes (dropped connections, Stop clicks, expired confirmations, read-only MCP spawn dirs) and two defense-in-depth injection gaps only show up when the happy path is stressed.

**What this unlocks**: a hardened base for v0.9.5 (remote MCP) to build on, and reclaimed context budget for the tool assistant on non-workflow pages.

#### Epics (shipped)

- [x] **v0.9.x full-series review + fixes** — **Story**: the user's assistant should not strand them (hung input, false "Saved", missing progress) or leak their library text as model instructions.

    **What landed** (verified fixes, grouped):
    - *Chat reliability*: streaming lifecycle can't get stuck (network failure / Stop / message-cap all finalize cleanly; `isStreaming` derived, not tracked); confirmation cards show pending → real terminal state with rollback on error; the dropped `operation_started` frame now renders its progress card; feedback thumbs report real success/failure.
    - *Security*: subagent summary re-wrapped as `<user_data>` before re-entering the main model; `query_operations` run-detail wraps connector-originated failure text; live key validation rate-limited (5/min) + bounded timeout.
    - *Correctness*: chat request path stops 500-ing on transient failures (broad TaskGroup catch, `ToolExecutionError`→422, uniform confirmed-executor wrap); in-stream SSE errors no longer leak internals; MCP results strip `<user_data>`; MCP server survives read-only spawn dir (absolute `data_dir`) and emits an absolute `mixd` path; workflow versions >500 revertable; `manage_playlist` id validated up front.
    - *Efficiency*: per-user 60s dashboard-stats cache (was 5 queries per message); rate-limiter stale-key sweep.
    - *DRY*: shared `commit()` envelope (13 blocks), `ChatCard` shell, public `iso()` (6 copies), `CHAT_ERROR_CODES` table, removed redundant `currentWorkflowDraft`.

    **Tests**: every fix carries a unit/integration test at its layer (frontend store/component tests for the streaming + confirmation + upsert fixes; dispatcher tests for the wrapping + coercion fixes; API tests for the 500-paths, error-code table, and rate limit; MCP tests for strip + spawn robustness). Full fast suite green (backend 3759, frontend 835). Status: Completed (2026-07-16)

- [x] **Page-contextual tool routing — refinement** — **Story**: the user on a non-workflow page shouldn't pay context budget (and tool-selection accuracy) for the two largest workflow-only tool schemas.

    **Decisions**:
    - Rule-based routing stays (not an embedding router) — a July-2026 best-practices pass put the semantic-router crossover at ~50+ tools; mixd exposes ~34, where a validated rule map is more accurate and free.
    - `describe_node` + `generate_workflow_def` demoted off the invariant prefix, promoted only on the `workflows` page — reclaims ~2 hot slots on the other 4 pages.
    - Two-tier cache: the promoted segment gets its own breakpoint so a section's tools cache across its turns while the core prefix stays page-invariant (the v0.9.2 hard constraint).
    - Hint map validated at import (canonical page set + derived ≤N-per-page ceiling), so drift fails loudly.

    **Spec**: `registry.build_tools()` → prefix + promoted + rest; `_CANONICAL_PAGES`, `_MAX_PROMOTED_PER_PAGE` (derived), `_validate_page_hints()`.

    **Tests**: prefix-invariance across pages; two breakpoints on a promoting page (never on a server block); demoted tools promoted on workflows / deferred elsewhere; hint-map validator rejects unknown page / unknown tool / write tool / over-ceiling. Status: Completed (2026-07-16)

    > **Post-deploy note**: the first request after deploy re-warms the tools prompt cache once (the prefix shrank).

- [x] **Docs & conventions reconciliation** — **Story**: the planning docs and layer rules should describe what actually shipped.

    **What landed**: `.claude/rules/web-frontend-patterns.md` cites `chat-store.ts` as a Zustand exemplar (the "No Zustand" premise was already false — the rule sanctioned Zustand); `docs/web-ui/01-user-flows.md` §6.5 rewritten from the "v0.9.0 Sketch" into six shipped sub-flows (real endpoints + edge cases); four layer rules amended to match shipped-and-correct practice — streaming-loop use-case shape (`application-patterns.md` + CLAUDE.md), LLM-adapter port imports (`infrastructure-patterns.md`), assistant-key credential carve-out (`interface-patterns.md` + CLAUDE.md), metric-provider bridge (`application-patterns.md`). Status: Completed (2026-07-16)

#### Still-pending candidates (not in this ship)

- **MCP spec/SDK drift check** — gated to one release after 2026-07-28; verify no server-side assumptions broke.
- **Model/effort cost re-eval** — before Sonnet 5 introductory pricing ends 2026-08-31, once per-request effort data exists.
- **Parallel tool dispatch within a turn** (surfaced by the review, finding C7) — correctness-neutral today; needs `kind` plumbed through the executor seam + buffered SSE emission (streaming-granularity tradeoff). Demand-gated.
- **Conversation persistence** — deferred by v0.9.0's ephemeral-chat decision; only on demonstrated demand; unlocks revisiting compaction.
- **Memory tool / cross-session memory** — deferred deliberately (prompt-injection *reinfection* vector; needs a provenance/labeling design first).
- **Parallel subagent orchestration** — single delegated subagent first; fan-out only if real analysis tasks demand it.
- **Chat voices settings toggle** — the pluggable voice registry ships with a single Default voice (a documented-deliberate seam; the review confirmed it earns its keep against this open candidate).

#### Post-Deploy Revisions

(empty until first ship)

---

### v0.9.5: Remote MCP Server (Your Production Library, From Any Agent)

**Goal**: Today the MCP server is stdio-only — an agent can reach a mixd library only by launching `mixd mcp serve` as a local subprocess against a locally-configured database. But the user's data lives on the **production deployment** (Fly.io + Neon), and the whole point of MCP is *access from anywhere, any system* — point Claude Desktop, Cursor, or any MCP-aware agent at your live account and have it read and act, wherever you are. v0.9.5 makes the existing tool registry reachable over authenticated **Streamable-HTTP on the production deployment**, so "my agent can do anything I can do" stops meaning "on this one laptop" and starts meaning "on my real, hosted library." *(This reverses the v0.9.3 "demand-gated successor" framing — remote MCP is committed scope, scheduled here.)*

**Context**: The 2026-07-28 MCP authorization revision makes a remote server an **OAuth 2.1 Resource Server**: it validates audience-bound bearer tokens, advertises its authorization server via RFC 9728 Protected Resource Metadata, and challenges unauthenticated calls with `401 + WWW-Authenticate`. mixd already carries most of this — Neon Auth issues EdDSA JWTs validated against a JWKS (`src/interface/api/auth_gate.py`), `sub` drives per-request RLS (`user_context()` → `set_config('app.user_id', …)`), and the `ALLOWED_EMAILS` allowlist gates who may connect. The installed `mcp==2.0.0b1` SDK ships the transport (`StreamableHTTPSessionManager`, mountable into the existing FastAPI app) and resource-server auth (`TokenVerifier` / `BearerAuthBackend`, keying sessions on the ASGI `scope["user"]` seam the auth middleware already populates). So this milestone is **transport + auth wiring over reused infrastructure**, plus two production-correctness fixes the local server never needed.

**What this unlocks**:
- Drive your **production** library from any MCP client, anywhere — "add my starred 2024 tracks that aren't on my Friday playlist" from Claude Desktop on any machine, acting on your real hosted data.
- Per-user OAuth: a client authenticates *as you* (audience-bound token, allowlist-gated), every tool call RLS-scoped to your rows exactly like the web UI — no shared key, no DB credentials on the client.
- The same 26 read/sync-write tools the in-app assistant and the stdio server expose, now reachable without a local install — one registry, three transports (chat, stdio, remote-HTTP).

**Persona**: Tinkerer — primary. The self-hoster/power user (and the mixd owner themselves) who already pipes LLM agents into their toolchain and wants them pointed at the real, hosted library from any device. Weekly Curator — secondary. Ad-hoc maintenance from a phone-side agent without opening the app. Casual Enthusiast — tertiary, via whatever agents others build on the hosted surface.

**Key Design Decisions**:
- **The MCP server is an OAuth 2.1 Resource Server, never an authorization server** — it validates audience-bound bearer JWTs locally via Neon Auth's JWKS (reuse the `auth_gate.py` verifier), because local JWKS validation is stateless and scales cleanly on Fly with no per-call auth round-trip.
- **Reuse Neon Auth as the authorization server (spike-first)** — the first epic proves whether Neon Auth (Stack Auth / BetterAuth) can run the external-MCP-client flow (Authorization-Code + PKCE, dynamic registration or Client-ID-Metadata, RFC 8707 resource-audience tokens); a minimal self-hosted AS is the documented fallback. Chosen because it adds no new moving parts if it works, and keeps auth on the infra the user already runs.
- **Mount into the existing FastAPI app at `/mcp`, one process** — one uvicorn worker, one TLS/health/deploy config, and one in-process path so chat + MCP share state; enter `StreamableHTTPSessionManager.run()` from the existing `create_app()` lifespan. A second ASGI app would duplicate auth/health wiring and, if run as its own process, wouldn't share the confirmation store.
- **Per-request identity replaces the startup binding** — the stdio path binds one `user_id` at `build_server(user_id)`; HTTP resolves identity per request from the validated token `sub`, bridging `scope["auth_user"]` → the SDK's `AuthenticatedUser`, then runs each tool call inside `user_context(sub)` so RLS scopes it. The `ALLOWED_EMAILS` allowlist gates connections, so only the owner's identities can bind.
- **Durable, multi-instance-safe confirmation store** — move `pending_action_store` off the in-memory dict to Neon Postgres (new table + Alembic migration via the existing `release_command`), because `auto_start_machines=true` can spin a second Fly machine and a preview on machine A must be confirmable on machine B. The two-phase write guarantee is incorrect without shared state.
- **Stateless Streamable-HTTP** — no sticky sessions; every instance validates the same JWT independently. Session ids, if used, are correlation hints only.
- **Canonical resource URI = `https://mixd.me/mcp`** — the public custom domain, held in a single `MCP_RESOURCE_URI` config constant; every token MUST be audience-bound to exactly this string (RFC 8707), and the resource-metadata document advertises it. The app also answers on `mixd.fly.dev`, but that is an alias, not a second resource identity: requests arriving over either host carry a token whose `aud` is `https://mixd.me/mcp`. Set the SDK's transport host/`allowed_hosts` explicitly to both `mixd.me` and `mixd.fly.dev` (a Fly deploy must not inherit the localhost DNS-rebinding default, which would reject both).
- **No token passthrough** — the MCP access token authenticates the user *to mixd* only; downstream Spotify/Last.fm calls keep using mixd's own per-user stored connector creds (`get_token_storage()`), per the spec's confused-deputy guidance.
- **Local stdio mode stays, unchanged** — remote is purely additive; `mixd mcp serve` and the existing install flow keep working.
- **Long-running ops stay `pending_tasks`** — the 5 `launches_operation` writes remain excluded (unchanged from v0.9.3); the Tasks extension is still gated on the 2026-07-28 stable SDK and is out of scope here.
- **SDK is beta (`mcp==2.0.0b1`)** — re-verify the transport + auth API at the stable-v2 bump (the same RE-VERIFY checkpoint v0.9.3 already tracks).

#### Epics

- [ ] **Spike: Neon Auth as the OAuth 2.1 Authorization Server for external MCP clients**

    **Story**: The Tinkerer wants to connect their agent to mixd's production URL and log in with the identity they already use for the web app — not manage a second credential system. Before building the transport, we must know whether mixd's existing Neon Auth can *be* the OAuth authorization server an external MCP client talks to, or whether a thin self-hosted AS is required — because that answer shapes the auth epics.

    **Decisions**:
    - Determine whether Neon Auth (Stack Auth / BetterAuth) supports, for a third-party client: Authorization-Code + PKCE, client registration (Dynamic Client Registration RFC 7591 *or* Client-ID-Metadata-Documents), RFC 8707 resource indicators, and **resource-audience** access tokens (`aud = https://mixd.me/mcp`) rather than Neon Auth's current `aud = auth origin`.
    - Confirm it exposes RFC 8414 / OIDC discovery so a client can find the token/authorize endpoints.
    - Output is a **written decision** appended to the milestone: reuse-as-is, reuse-with-config, or fall back to a minimal self-hosted AS (documented shape if so).

    **Spec**:
    - Investigate Neon Auth capabilities against the [MCP authorization spec](https://modelcontextprotocol.io/specification/draft/basic/authorization); record findings inline in this section as an `#### Implementation Progress & Learnings` note.
    - No production code; a throwaway probe of the auth endpoints is acceptable.

    **Tests**:
    - (manual) A real MCP client (Claude Desktop / MCP Inspector) completes the OAuth handshake against a scratch config and receives a token whose `aud` targets the mixd resource URI — or the fallback path is chosen with rationale.

    Effort: S | Dependencies: v0.9.3 (MCP Server Scaffold) | Status: Not Started

- [ ] **Streamable-HTTP Transport Mounted in the FastAPI App**

    **Story**: The Tinkerer wants a stable HTTPS endpoint their agent connects to (`https://mixd.me/mcp`), serving the exact same tools the stdio server does, with identity resolved per request instead of pinned at process start.

    **Decisions**:
    - Reuse the transport-agnostic core (`build_server`, `_to_mcp_tool`, `mcp_annotations`, `exposed_specs`, `confirmation.py`) untouched; add an HTTP transport alongside `serve_stdio`, not in place of it.
    - Mount `StreamableHTTPSessionManager(app=build_server(...), stateless=True)` into `create_app()`; enter its `run()` from the existing lifespan (`src/interface/api/app.py`). Set the transport `allowed_hosts`/security explicitly to `mixd.me` + `mixd.fly.dev` (don't inherit the localhost DNS-rebinding default, which rejects both).
    - Refactor identity to per-request: derive `user_id` from the request principal (see the RS-auth epic) and execute each tool call inside `user_context(user_id)` so RLS scopes it identically to the web API.

    **Spec**:
    - New transport wiring in `src/interface/mcp/` (e.g. `http.py`) + a mount point in `create_app()`; `serve_stdio` and `build_server`'s registry logic unchanged.
    - The per-call `ToolContext(user_id=…)` is built from the request, not a closure captured at startup.

    **Tests**:
    - (integration) a Streamable-HTTP client lists tools and calls a read tool over the mounted endpoint; result matches the stdio path and is scoped to the request's user.
    - (integration) two different authenticated users hitting the same endpoint see only their own rows (RLS holds per request).
    - (unit) the exposed tool set over HTTP equals `exposed_specs()` (no `agentic`/`pending_tasks` leakage).

    Effort: M | Dependencies: Spike | Status: Not Started

- [ ] **Resource-Server Auth: Protected-Resource Metadata, Audience Validation, Challenges**

    **Story**: The user wants only *their* agent to reach *their* data — an unauthenticated or wrong-audience request must be rejected, and a well-behaved client must be able to discover how to authenticate automatically.

    **Decisions**:
    - Serve RFC 9728 Protected Resource Metadata at `/.well-known/oauth-protected-resource` advertising the Neon Auth authorization server + supported scopes.
    - Validate the bearer token as a resource server: signature via JWKS (reuse `auth_gate.py`'s verifier + 1h JWKS cache), `exp`, `iss`, and **`aud` = the canonical MCP resource URI** (RFC 8707). Bridge the validated principal onto the request (`scope["user"]` / `sub`) for the transport epic to consume; enforce the `ALLOWED_EMAILS` allowlist.
    - Emit `401 + WWW-Authenticate: Bearer resource_metadata=…, scope=…` for missing/invalid tokens and `403 + WWW-Authenticate: error="insufficient_scope"` for scope failures.

    **Spec**:
    - Extend or parallel `NeonAuthMiddleware` to accept MCP-resource-audience tokens (the web app's `aud=auth origin` path stays intact); add the `.well-known` route.
    - Reuse the SDK's `TokenVerifier` / `BearerAuthBackend` where it fits the JWKS validation, or thread the existing verifier through it.

    **Tests**:
    - (integration) no token → 401 with a `WWW-Authenticate` header naming the resource-metadata URL; wrong-audience token → 401; disallowed email → 403.
    - (integration) valid audience-bound token → tool call succeeds, scoped to `sub`.
    - (integration) `GET /.well-known/oauth-protected-resource` returns spec-valid metadata pointing at the Neon Auth AS.

    Effort: M | Dependencies: Spike, Streamable-HTTP Transport | Status: Not Started

- [ ] **Durable Pending-Action Store (Neon Postgres)**

    **Story**: A user previews a destructive change from their agent, then confirms it a moment later — and it must commit reliably even if the two calls land on different production machines. The never-silent write guarantee can't depend on both calls hitting the same process.

    **Decisions**:
    - Replace the in-memory `pending_action_store` (`src/application/chat/pending_actions.py`) with a Neon-Postgres-backed store, preserving the exact `create` / `claim` (owner-checked, single-use, 5-min TTL) / `cancel` contract so chat, stdio MCP, and remote MCP all keep working unchanged.
    - New table + Alembic migration (runs via the existing Fly `release_command`); TTL eviction on read, RLS-scoped by `user_id` like every other table.

    **Spec**:
    - Repository-backed store behind the current `PendingActionStore` interface; migration under `alembic/`.
    - No change to the two callers' code paths (`confirmation.py`, chat `_common.py`) beyond the store being durable.

    **Tests**:
    - (integration) preview on one store instance, claim on a *separate* instance (simulating two machines) → commits once; second claim of the same token → expired/rejected.
    - (integration) TTL expiry → claim re-previews; owner mismatch → forbidden.
    - (integration) existing chat two-phase tests pass unchanged against the durable store.

    Effort: M | Dependencies: none (parallelizable) | Status: Not Started

- [ ] **Remote Onboarding DX: `mixd mcp install --remote`**

    **Story**: The Tinkerer wants a one-command way to wire their client to the hosted server, the same way `mixd mcp install` wires the local one — emit the right config for the production URL and walk them through the one-time OAuth consent.

    **Decisions**:
    - `mixd mcp install --remote` emits the client config pointing at `https://mixd.me/mcp` (HTTP transport, not a `{command,args}` subprocess); local stdio install stays the default.
    - Document the one-time browser OAuth consent; no secrets written to disk beyond what the client's own token store holds.

    **Spec**:
    - Extend `src/interface/mcp/install.py` + `mcp_commands.py` with the remote variant; reuse `client_location`/snippet-builder patterns.

    **Tests**:
    - (unit) `--remote` emits a valid remote-transport config for each supported client; local install unchanged.

    Effort: S | Dependencies: Streamable-HTTP Transport, Resource-Server Auth | Status: Not Started

- [ ] **Documentation & DX**

    **Story**: A user (or anyone they share the deployment with) needs to know how to connect an external agent to their production library, what the security model is, and how to operate it — without reading the source.

    **Decisions**:
    - Documentation describes *shipped* behavior, so it is written as part of this milestone, not before it.
    - Correct the now-stale v0.9.3 docs that assert MCP is local-only.

    **Spec** (deliverables):
    - `docs/guides/mcp.md` — new **"Remote (production) connection"** section: connecting a client to `https://mixd.me/mcp`, the one-time OAuth consent flow, `mixd mcp install --remote`; **fix the stale lines** ("There's no HTTP, no OAuth …" intro and the "What's not exposed" framing).
    - `docs/deployment.md` — new secrets/config (OAuth AS settings, canonical `MCP_RESOURCE_URI`, JWKS/audience) with a `fly secrets set` block, following the existing "Optional — <feature> (vX.Y.Z)" note pattern.
    - A **security/architecture note** — the RS model, audience binding, no-token-passthrough, allowlist scoping, and rate-limiting posture for the public `/mcp` endpoint (consider `Mcp-Method`/`Mcp-Name` header routing for a future gateway).
    - `docs/web-ui/capability-matrix.md` + `README.md` — reflect that MCP is now remotely reachable.
    - Troubleshooting: 401/consent failures, audience errors, allowlist rejections.

    **Tests**:
    - (manual) a fresh client, following only the guide, connects to the hosted server and completes a read + a confirmed write.
    - `scripts/check_backlog.py` + doc-link checks pass.

    Effort: S | Dependencies: all above | Status: Not Started

#### Post-Deploy Revisions

(empty until first ship)
