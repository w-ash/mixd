# v0.8.13 – v0.8.17: The Fable Sweep — Execution Waves

> Part of the **v0.8.x** cycle — overview and User Workflow Context in [v0.8.0–v0.8.4](v0.8.0-0.8.4.md).

> Executes the work orders produced by the [v0.8.12](v0.8.12.md) audit. The **hub** ([fable-sweep/README.md](fable-sweep/README.md)) is the single source of truth for per-spoke specs, guardrails, and status — this file only schedules them into shippable waves. Every wave inherits v0.8.12's execution rules verbatim: **behavior-preserving, clean breaks / no shims, one spoke = one commit, green at every increment, no test weakened.**

**Wave → version mapping** (sequenced cheap-confident → riskiest; each version = one deploy after its spokes land):

| Version | Wave | Spokes | Effort |
|---|---|---|---|
| v0.8.13 | 1 — Dead code & free wins | 12, 13, 18, 20 + basedpyright promotion | S-M |
| v0.8.14 | 2 — Backend structure | 04, 05, 09, 15, 23, 25 → 07, 08, 10, 14 → 21, 22, 02, 03 | XL |
| v0.8.15 | 3 — Frontend structure | 16 → 17 → 19 | M-L |
| v0.8.16 | 4 — High-risk decompositions | 11, 06 (+ optional 24) | L |
| v0.8.17 | Closeout — ratchet & review | 26 | M |

**Fable-window note (through 2026-07-07):** the spokes with *Fable* as suggested executor — **02, 07** (v0.8.14), **16** (v0.8.15), **11** (v0.8.16) — should be dispatched inside the window even if their wave's deploy comes later; each is an isolated commit, so early execution doesn't break wave ordering. Past the window they fall back to Opus.

**Window re-scope (2026-07-02 sequencing review):**
- **Per-wave Fable review** — v0.8.17's sweep review runs against each wave's diff *as it lands inside the window* (v0.8.12 Story 3 permits "aggregate or per-batch"), so the review isn't silently lost to the Opus fallback if waves finish past 07-07; Opus covers only post-window remainders.
- **Two doc-only briefs share the window** — the [v0.9.x Pre-Work brief](v0.9.x.md#pre-work-2026-mcp--chatbot-best-practices-pass--sequencing-review) and the [identity-resolution research pass](identity-resolution-research-handoff.md) (scope-extended 2026-07-02: artist identity + the omni-integration provider breadth scan). Compression fallback: if the window shrinks, the briefs outrank spokes 02/07/16 (those have Fable-written work orders and a documented Opus fallback; the briefs have neither — done later, the thinking itself degrades). **Spoke 11 keeps its Fable slot longest** — the sweep's riskiest change, where executor quality matters most.

**Window execution order** — the backlog's source of truth for *what to tackle next* (2026-07-02; check items off as they land). Ordering principle: Fable-exclusive unblockers first (each unlocks a batch of farmable Opus/Haiku spokes), doc-only briefs threaded through while the farm runs, riskiest refactor and closeout last. Farmed spokes run in parallel throughout — this list sequences *Fable's* attention:

1. [x] **Spoke 07** (Fable) — use-case skeleton collapse; unblocks 08 + 10 for Opus. In parallel, farm the batch-2a remainder (09, 15, 23, 25 — no dependencies). ✅ 2026-07-02 — whole of Wave 2 (all 14 spokes) landed the same session; v0.8.14 awaits only `/ship`.
2. [x] **Spoke 02** (Fable) — play-importer pipeline; unblocks 03 for Opus. Farm 21 + 22 alongside (batch 2c). ✅ 2026-07-02.
3. [x] **Spoke 16** (Fable) — QueryStates wrapper; unblocks 17 → 19 for Opus (Wave 3 is independent of Wave 2 and may run parallel to its tail). ✅ 2026-07-02 — whole of Wave 3 landed and shipped as v0.8.15 the same session (16 on the Fable main thread; 17/19 farmed to Opus in worktrees, diffs Fable-reviewed and verified on main before commit).
4. [x] **v0.9.x Pre-Work brief** (Fable, doc-only) — ✅ landed 2026-07-02. Brief + re-sequenced epics live in [v0.9.x.md](v0.9.x.md); decisions: couplefins port stands with a modernization delta, chat-first with MCP as second consumer, shared tool registry pulled into v0.9.0.
5. [x] **Identity-resolution research pass** (Fable, doc-only) — the largest brief: phases A–D + artist identity + provider breadth scan. Phases produce artifacts incrementally, so a partial pass still lands value; interleave with the reviews below as waves finish. ✅ 2026-07-02 — [identity-resolution-design-space.md](identity-resolution-design-space.md) + [identity-governance-design-space.md](identity-governance-design-space.md) + PDR-001/002 (`docs/decisions/`); shipped with v0.8.17.
6. [x] **Per-wave Fable reviews** — review the v0.8.14 and v0.8.15 aggregate diffs as each wave completes; findings become revisions before that wave's deploy. ✅ v0.8.14 was reviewed per-spoke as each executor diff landed (see the Wave 2 status notes — three executor defects caught in spoke 15 alone); v0.8.15's aggregate diff reviewed 2026-07-02 with move fidelity mechanically verified — zero findings. Wave 4 review rides item 7.
7. [x] **Spoke 11** (Fable) — executor closure flatten, characterization tests first (requires Wave 2 landed + trusted). Farm 06 (+ optional 24) to Opus alongside; then the Wave 4 review. ✅ 2026-07-02 — 11 on the Fable main thread (tests-first, two commits), 06 farmed to Opus in a worktree and Fable-verified on main, 24 skipped (user-approved; premises falsified). Wave 4 review done (AST move-fidelity check + engineer review, zero findings); shipped as v0.8.16.
8. [x] **Closeout** — spoke 26 (Opus) + a final Fable pass over any diff not yet reviewed → ship v0.8.17. ✅ 2026-07-02 — three closeout stories landed on branch `sweep-closeout-v0.8.17` (spoke 26 · visual gate · dependency audit), closeout diff reviewed at ship (engineer + QA, zero violations), shipped as v0.8.17.

---

### v0.8.13: Sweep Wave 1 — Dead Code & Free Wins
**Goal**: Delete the audit's grep-confirmed dead surfaces and lock the free type floor — the highest-confidence, lowest-risk quarter of the sweep, shipped first so later waves are checked at the higher bar.

**Context**: All four spokes are dead-code deletions or conformance fixes with zero behavioral reach; the basedpyright census found `src/` already at 0 warnings, making the warning→error promotion a free ratchet flip (hub counter-proposal #1: flip it *first*, not last).

**What this unlocks**: Every subsequent wave's refactors are type-checked at error severity from day one; ~700 lines of dead abstraction/config gone before anything is built on top of them.

**Persona**: Maintainer (Ash) directly; all personas indirectly via reliability.

- [x] **Lock the type floor** — promote the 8 basedpyright rules (`reportUnknown*` ×5, `reportMissingTypeArgument`, `reportImplicitOverride`, `reportDeprecated`) from `"warning"` to `"error"` in `pyproject.toml`. Verified free 2026-07-01 (0 warnings); re-verify with a fresh run before flipping. (Pulled forward from [spoke 26](fable-sweep/26-ratchet-closeout.md) step 1.)

    Effort: XS | Executor: Haiku | Status: Completed (2026-07-02) — re-verified 0 warnings, flipped as the wave's first commit.

- [x] **[Spoke 12](fable-sweep/12-execution-strategies-dead-abstraction.md)** — delete the dead execution-Strategy abstraction (~75% of `execution_strategies.py`; one live path survives as plain functions).

    Effort: M | Executor: Opus | Status: Completed (2026-07-02) — collapsed to `plan_api_operations()` + surviving `simulate_position_shifts`/`_adjust_move_operations`; API-path integration tests green.

- [x] **[Spoke 13](fable-sweep/13-dead-config-purge.md)** — purge dead config: `BatchConfig` + ~14 pre-Fellegi-Sunter `MatchingConfig` fields across settings/factories/domain.

    Effort: S-M | Executor: Opus | Status: Completed (2026-07-02) — 4 surfaces per field (incl. `vulture_whitelist.py`); type-constraint tests re-pointed to live `ConfidenceScore` fields.

- [x] **[Spoke 18](fable-sweep/18-dead-frontend-export.md)** — resolve dead export `hasActiveFilters` (wire or delete).

    Effort: XS | Executor: Haiku | Status: Completed (2026-07-02) — deleted (Library uses the tested `countActiveFilters`; wiring would be redundant).

- [x] **[Spoke 20](fable-sweep/20-design-token-conformance.md)** — design-token conformance in `SyncConfirmationDialog` (+2 lighter files). Flagged net-positive UX conformance; before/after screenshots in the PR.

    Effort: XS-S | Executor: Haiku | Status: Completed (2026-07-02) — added `status-success/error/warning/info` severity tokens (Strategy B); also repaired the phantom `bg-status-error/10` in MergeTrackDialog/UnlinkMappingDialog.

**Tests**: per-spoke guardrail blocks; `uv run pytest` + `pnpm --prefix web test` green per commit.

Effort: S-M | Dependencies: v0.8.12 audit (done) | Status: Completed (2026-07-02)

---

### v0.8.14: Sweep Wave 2 — Backend Structure
**Goal**: Collapse the backend's copy-pasted envelopes and decompose its oversized methods — the bulk of the sweep — with zero behavior change.

**Context**: Sequenced in three internal batches (small-low-risk → application collapse → persistence + play pipeline) because spokes 07/08/10 share `_shared/` files and 02/03 share the play subsystem; the order below prevents mid-wave collisions.

**What this unlocks**: The five use-case skeletons stop drifting (cluster D's tamper guard exists in three hand-copies today — a multi-tenancy consistency risk); the play-import pipeline becomes type-checkable and single-sources its token-first username resolution.

**Persona**: Maintainer directly; Curator indirectly (identity-data integrity, import reliability).

**Batch 2a — small, independent** (any order):
- [x] **[Spoke 04](fable-sweep/04-matching-provider-loop-hoist.md)** — hoist the matching-provider per-track loop; drop unreachable validation. Effort: S | Opus

    Status: Completed (2026-07-02) — `_match_each` in the base owns the loop shell (id guard + exception classification); Spotify/MusicBrainz hooks are thin per-track matchers; unreachable NO_ISRC/NO_METADATA re-validation deleted (base partition is the single validation point). LastFM untouched.
- [x] **[Spoke 05](fable-sweep/05-lastfm-conversions-boundary.md)** — Last.fm conversions: validate at the boundary. Effort: S | Opus

    Status: Completed (2026-07-02) — new `LastFMTrackData` Pydantic model (faithful before-validators; deliberately not reusing `LastFMTrackInfoData`, whose `#text` aliasing would change behavior); validation at the sole caller seam `LastFMConnector.convert_track_to_connector`; 23-case characterization test proved before/after equivalence; function-local imports hoisted here + musicbrainz; `validate_non_empty` kept (live callers in spotify/operations.py).
- [x] **[Spoke 09](fable-sweep/09-timed-query-envelope.md)** — shared timed-query envelope for read use cases. Effort: S | Haiku

    Status: Completed (2026-07-02) — `_shared/timed_execution.py::timed_query` async CM; agent converted the three read modules (+8 helper tests); Fable review extended scope to the spec's canonical-CRUD timer blocks (create/delete canonical, create connector — redundant explicit rollbacks dropped, `__aexit__` already rolls back). Left as bespoke: `get_preferred_tracks` (baseline, no failure envelope — wrapping would add a new error log), `update_canonical_playlist` (spoke 10 restructures it), orchestration modules.
- [x] **[Spoke 15](fable-sweep/15-backend-minor-batch.md)** — minor batch: `dual_mode`, play-history predicate, protocol-surface shrink, playlist-entity conveniences. Effort: S | Haiku

    Status: Completed (2026-07-02) — all five items landed (~−330 lines). Fable review caught and fixed three executor defects: a stale `@db_operation` decorator left stacked on `upsert_model`; the extracted window predicate conflating out-of-window with missing-date (would have let `include_missing=True` readmit out-of-window tracks — regression test added); vacuous rewrites of the deleted `to_tracklist` tests replaced with honest deletions. Also deleted now-dead `select_by_connector_id`; `added_at_dates` orphaned-reader finding logged in hub Deferred.
- [x] **[Spoke 23](fable-sweep/23-base-repo-mapper-split.md)** — split mapper machinery out of `base_repo.py`. Effort: S | Opus

    Status: Completed (2026-07-02) — mapper machinery moved verbatim to `repositories/mappers.py`; 16 import sites rewritten, no re-export; upsert machinery untouched; 375 repo integration tests green; `BASE_PYRIGHT_IGNORE` within baseline (19 ≤ 20).
- [x] **[Spoke 25](fable-sweep/25-cli-structural-pass.md)** — CLI: `_display_table_result` split + helper param-objects; command-family package split is the optional third item (decide at execution). Effort: M | Opus

    Status: Completed (2026-07-02) — items 1–2: `_render_summary_table`/`_render_track_details_table`/`_format_metric_cell` split out of `_display_table_result`; `ImportProgressSpec` (housed in `batch_file_import_service` beside its `ImportExecutorProtocol` consumer) + `ScheduleCommandSpec` replace the 10/11-arg helpers; all call sites + tests updated. **Item 3 (playlist_commands package split) skipped** — items 1–2 stand alone per spec; revisit alongside spoke 24's optional wide split in Wave 4 if still wanted. (Executor agent stalled twice mid-verification; Fable ported the worktree diff and verified on main.)

**Batch 2b — application collapse** (07 before 08/10; both touch `_shared/`):
- [x] **[Spoke 07](fable-sweep/07-use-case-skeleton-collapse.md)** — collapse the five use-case skeletons; includes the `.claude/rules/application-patterns.md` one-line amendment (user signs off in PR). Effort: L | **Fable (window)**

    Status: Completed (2026-07-02, Fable main thread) — all five clusters: A merged into `tag_vocabulary.py` (+`_bulk_tag_mutation`; `delete_playlist_assignment` left standalone — different vocabulary, conditional commit); B → `mutate_owned_link` in `playlist_resolver.py`; C → `_shared/entry_edit.py::persist_entry_change` (dual sync/async transform); D → `_shared/mapping_guard.py::require_owned_mapping` (tamper guard single-sourced); E → `_shared/event_log.py::apply_with_event_log` (event rides the mutation's commit). `api_batch_size_validator` + its tests deleted. Use-case suites passed unmodified except tag-trio import paths. Rule amendment applied — pending user sign-off.
- [x] **[Spoke 08](fable-sweep/08-route-handler-conformance.md)** — move the six stray route handlers back behind `execute_use_case`. Effort: M | Opus

    Status: Completed (2026-07-02) — all six handlers now 5–10-line wiring; new `CreateAndApplyAssignmentUseCase`, `ReadPlaylistTracksPageUseCase`, `MergeTrackAndFetchDetailsUseCase` (composition chosen over widening `MergeTracksUseCase` — CLI path stays single-read), tag normalization moved into `ListTracksCommand` (fixes the CLI/web parity gap); SSE orchestration → `services/workflow_execution.launch_workflow_run` + new `services/playlist_sync` (event shapes byte-identical). **Flagged deviation:** the bad-tag 422 *body* is now Pydantic's list shape instead of `{"detail": "<str>"}` (status unchanged, nothing parses it) — one-line revert available if byte-identity is wanted.
- [x] **[Spoke 10](fable-sweep/10-update-canonical-playlist-split.md)** — `update_canonical_playlist`: push diff sub-algorithms down to domain. Effort: M | Opus

    Status: Completed (2026-07-02) — pure `select_appendable_entries` in new `domain/playlist/append.py` (+6-case decision-table test); `_append_entries` gone; `operation_counters.py` folded into `playlist_results.py` (spec's single-consumer premise was false — two live consumers). Deviation accepted: file is 434 lines, not the spec's ~250 target — the residue is Command/Result contract + transaction envelope, not extractable pure logic; v0.8.11 identity semantics pinned by the passing preservation suite.
- [x] **[Spoke 14](fable-sweep/14-service-method-splits.md)** — split the two phase-commented service methods. Effort: M | Opus

    Status: Completed (2026-07-02) — `get_external_track_metrics` → 5 phase helpers, `process_connector_playlist` → 3 step helpers; phase/step comments deleted (names carry the meaning); await sequence byte-identical (thundering-herd behavior preserved); both methods' PLR0915/PLR0914 cleared.

**Batch 2c — persistence + play pipeline** (02 before 03; same subsystem):
- [x] **[Spoke 21](fable-sweep/21-track-connector-mapping-spec.md)** — `ConnectorMappingSpec` replaces the 7-tuple; decompose the two batch monsters. Effort: M | Opus

    Status: Completed (2026-07-02) — frozen `ConnectorMappingSpec` lives in the domain protocol module (the protocol carried the tuple type); both batch methods decomposed along their phase seams; SQL identical; MANUAL_OVERRIDE pin green. Spec's builder list was narrower than written: the inward resolvers use the single-item path — only `track_identity_service_impl` built tuples. Two write-only locals (`track_metadata_map`, `metrics_data`) deleted.
- [x] **[Spoke 22](fable-sweep/22-list-tracks-decomposition.md)** — extract the `list_tracks` filter builder. Effort: S-M | Opus

    Status: Completed (2026-07-02) — `_build_list_filters` + `_apply_sort_and_page` extracted verbatim (SQL byte-identical incl. pg_trgm + `tag_mode="and"` trick); `list_tracks` is a linear ~86-line orchestration; substantive PLR0915/PLR0912 cleared. `TrackListFilters` param object NOT introduced (PLR0913 is globally ignored; bundling would rewrite the public contract across 5 call sites for no enforced gain — decided on the diff).
- [x] **[Spoke 02](fable-sweep/02-play-importer-pipeline.md)** — play-importer pipeline: typed params, single username resolution, dead paths out. Effort: L | **Fable (window)**

    Status: Completed (2026-07-02, Fable main thread) — frozen `LastfmImportParams`/`SpotifyImportParams` in `domain/repositories/play.py`; `BasePlayImporter[TRawData, TParams]` generic; `import_data` returns `(result, plays)` directly (instance-state handoff deleted); TypedDicts, `_extract_common_params`, `_import_plays_unified`, base `_save_data`, `plays_repository` all gone; env fallbacks in `_load_checkpoint`/`_fetch_date_range_strategy` replaced by a hard "must be resolved" guard (`_require_resolved_username`); MIGRATED comments + narration logs stripped; the lastfm importer's substantive PLR violations (0912/0914/0915) cleared. Username-precedence characterization already existed (TestUsernameResolution) and passes unmodified. Note: `commit_batch` missing from `UnitOfWorkProtocol` (getattr guard retained) — pre-existing, logged in hub Deferred.
- [x] **[Spoke 03](fable-sweep/03-play-resolver-decomposition.md)** — play-resolver decomposition; flatten the Last.fm chain. Effort: M | Opus

    Status: Completed (2026-07-02) — Spotify `resolve_connector_plays` split into `_extract_ids_and_hints`/`_should_skip`/`_build_context`/`_assemble_metrics`; `LastfmTrackResolutionService` folded into `LastfmConnectorPlayResolver` (file deleted, 15 tests carried into the resolver suite as 16); context-dict keys verified identical programmatically; `architecture_version` untouched. Executor agent stalled mid-verification and had strayed two commands into the main tree (caught and reverted); Fable ported the worktree diff, fixed 3 type errors the agent hadn't reached (JsonValue annotations + one narrowing local), and verified on main.

**Tests**: per-spoke; characterization tests *before* risky decompositions where the spoke says so (02: username precedence; 03: context-dict keys; 05: conversion fixture).

Effort: XL | Dependencies: v0.8.13 | Status: Shipped (2026-07-02) — all 14 spokes landed as one-commit-per-spoke on main; full matrix green (3322 backend / 699 frontend). Shipped as v0.8.14 alongside the dependency refresh (react-router 7→8, cryptography 49, fastapi 0.139, Starlette 1.3, + bumps) landed ahead of the v0.8.15 frontend wave.

---

### v0.8.15: Sweep Wave 3 — Frontend Structure
**Goal**: Kill the four-state ladder with shared primitives, de-fork the search dialogs, and split the four oversized screens — the frontend's whole structural-debt inventory in one deploy.

**Context**: Strictly sequenced 16 → 17 → 19: spoke 16 creates the shared components (QueryStates, skeleton primitives) that 17 and 19 build on; running them out of order double-touches the same pages.

**What this unlocks**: Consistent loading/error/empty behavior on every screen (the drift is already visible: `isLoading` vs `isPending`, two different error components); every future page starts from the wrapper instead of copying the ladder.

**Persona**: Maintainer directly; Curator/Casual indirectly (consistent states on flows 2.1, 3.1, 6.1, 7.1).

- [x] **[Spoke 16](fable-sweep/16-query-states-wrapper.md)** — `QueryStates` wrapper + skeleton primitives across ~10 screens; deletes 14 local skeletons. Effort: L | **Fable (window)**

    Status: Completed (2026-07-02, Fable main thread) — `QueryStates` takes a discrete `loading` flag passed verbatim (`isLoading` vs `isPending` diverge on `enabled:false` queries — the wrapper never picks), caller-derived `isEmpty`, plain-ReactNode children (mixed response envelopes forbid reaching into shapes). Four primitives (`ListRowsSkeleton`/`BlocksSkeleton`/`CardGridSkeleton`/`DetailHeaderSkeleton`) carry bar classes as data for pixel fidelity; `data-slot="skeleton"` preserved (loading tests key off it). 9 screens migrated, 7 local skeletons deleted, 4 detail pages kept early-return guards with skeleton bodies as thin compositions. Bespoke keepers: `IntegrationsSkeleton` (grouped sections), `MatchingHealthSkeleton`, `PlaylistRowSkeleton`; excluded: Sync (bespoke banner, static sections), Account (non-Tanstack), `PageSkeleton` (Suspense), nested sub-ladders. Tags/ImportHistory error states normalized onto `QueryErrorState` via new `description` override — copy unchanged, they gain the icon + database-unavailable branch. Both ladder greps empty; page suites unmodified; +16 focused tests.

- [x] **[Spoke 17](fable-sweep/17-command-search-list.md)** — `CommandSearchList` + `TrackResultRow`; de-fork `AddTracksDialog`/`TrackSearchCombobox`. Effort: M | Opus

    Status: Completed (2026-07-02) — Opus executor in worktree; Fable reviewed the diff and verified on main (executor's worktree was cut pre-wave — disjoint file sets made the port clean; base-sync made mandatory for spoke 19). `CommandSearchList` owns search state + query (`limit`/`enabled`/`loop`/`listClassName` map 1:1 to the two consumers' historical diffs); `TrackResultRow` exposes `leading`/`trailing` slots so checkbox/badge markup stays consumer-owned. `TrackSearchCombobox` deleted (git sees a 52%-similarity rename); Merge/Relink dialogs call the shell directly. Spec's `renderItem` slot deliberately narrowed to row adornments — after the TagAutocomplete skip every consumer renders track rows. TagAutocomplete skipped per spec allowance (no threshold, no loading branch, create-new row, different item type). Approved normalization: "Searching..." → "Searching…" (U+2026 house style). Grep gate exactly 1; `PlaylistTrackEditor` suite unmodified; +12 focused tests incl. ArrowDown+Enter keyboard selection.

- [x] **[Spoke 19](fable-sweep/19-page-decomposition.md)** — page decomposition along named seams (Dashboard, TrackDetail, WorkflowRunDetail, Tags, PlaylistDetail; Library/picker judged on inspection). Effort: M-L | Opus

    Status: Completed (2026-07-02) — Opus executor in worktree (base-sync verified first); Fable reviewed by mechanically diffing all 14 moved symbols against their pre-wave originals — byte-identical modulo `export` keywords, formatter wraps, and two unicode-escape→literal-glyph renderings (runtime strings unchanged). Dashboard 552→241 (`components/dashboard/`), TrackDetail 554→306 (`components/track/MappingList` + `lib/match-methods.ts`), WorkflowRunDetail 511→210 (`components/workflow/run-detail/` ×3), PlaylistDetail 937→186 (5 components + shared `link-queries.ts`; orphaned doc comment deleted, Main-Page marker fixed). Tags' three ConfirmationDialogs → one mode-keyed instance (+13 lines — the win is single-source copy, not line count; per-mode text byte-identical, tests assert it). Library + ConnectorPlaylistPickerDialog skipped: no clean seam (each extraction would drill 5–6 state/callback props). Page suites unmodified; 96-state playlist-detail visual audit green.

**Tests**: `pnpm --prefix web test` per commit; page suites pass unmodified except import paths; new focused suites for `QueryStates` and `CommandSearchList`.

Effort: M-L | Dependencies: v0.8.13 (independent of v0.8.14 — may run in parallel with it) | Status: Shipped (2026-07-02) — all three spokes landed one-commit-per-spoke on main; aggregate wave diff Fable-reviewed (zero findings); full matrix green (3384 backend / 727 frontend). Pre-existing visual-baseline staleness vs Chrome 149 logged in hub Deferred (failing set identical pre/post wave — not wave-caused).

---

### v0.8.16: Sweep Wave 4 — High-Risk Decompositions
**Goal**: The two refactors most likely to bite — the workflow executor's closure flatten and the base-connector reflection removal — run last, when the suite is maximally trusted, plus the optional wide split.

**Context**: Spoke 11 is the sweep's riskiest change (the engine behind every workflow run); its characterization-tests-first rule is non-negotiable. Spoke 06 touches every connector's mocks. Spoke 24 (db_models aggregate split) is approved-optional — the audit rates the status quo acceptable; execute only if the diff-hygiene value still reads as worth ~30 import-site churn when the time comes.

**Execution protocol (learned shipping v0.8.15)** — for farmed spokes 06/24: (1) **base-sync first** — the agent's worktree may be cut from a stale base (spoke 17's was cut pre-wave; only disjoint file sets made it harmless), so the dispatch's step 0 is `git merge main` + verify HEAD includes the prior spoke commits, stop-and-report on failure; (2) **verified design in the dispatch, not just the work order** — Wave 3's farming succeeded where Wave 2 stalled twice because each dispatch carried pre-verified seams/props/line ranges; give spoke 06 the same pre-verification pass before farming; (3) executor leaves the worktree uncommitted, Fable reviews the diff and verifies on main before committing.

**What this unlocks**: The executor's 7 PLR violations clear (the biggest single contributor to re-enabling `PLR0915`/`PLR1702` in the closeout); the last reflection-dispatch idiom in the codebase is gone.

**Persona**: Maintainer directly; Curator indirectly (workflow-engine reliability — the Sunday ritual).

- [x] **[Spoke 11](fable-sweep/11-executor-closure-flatten.md)** — flatten the executor's triple-nested closure stack (characterization tests FIRST: observer sequence, degrade path, timeout path). Effort: L | **Fable (window)**

    Status: Completed (2026-07-02, Fable main thread, two commits) — **commit 1** landed 6 characterization tests before the refactor: observer event sequence (happy/degrade/fatal — the SSE contract, previously unpinned; degrade emits `on_node_failed` but never `on_node_completed`), the timeout firing + rewrap path (patching `_get_node_timeout`; patching `WorkflowConstants` is a no-op — `_CATEGORY_TIMEOUTS` snapshots at import), and the first-ever real `run_workflow` executions (the nested `_build_and_execute_workflow` was a zero-coverage seam). **Commit 2**: mutable `@attrs.define _RunState` (11 fields = exactly the former closure captures; `parameters` held by reference — the `workflow_name` mutation aliases into node contexts; `_shutdown_requested` deliberately NOT in it, stays a live module-global read); `_run_task_inner`/`_run_node_lifecycle` promoted to module level `(state, task_def)`; `workflow_flow` now a gate-compliant depth-2 closure (level loop + TaskGroup + shielded finally); `_build_and_execute_workflow` promoted with plain params. AST nesting gate 4→2; move fidelity verified mechanically (AST-normalized diff: only the intended signature/derivation deltas; `_build_and_execute_workflow` byte-identical); all prior engine test files pass byte-unmodified.

- [x] **[Spoke 06](fable-sweep/06-base-connector-reflection.md)** — replace `get_playlist` reflection with an explicit contract; `get_connector_config` key-mapping check. Effort: M | Opus

    Status: Completed (2026-07-02) — pre-execution exploration found the work order stale: **the reflection path was production-dead** (no `get_<name>_playlist` methods exist anywhere; Spotify overrides `get_playlist` directly and is the only playlist-capable connector), so the `_get_playlist_impl` migration became a deletion — base `get_playlist` is now docstring + `NotImplementedError`. **`get_connector_config` deleted entirely** (user-approved scope extension: zero production callers; only tests called it, with legacy UPPER_CASE keys) — the settings-consistency integration tests re-pointed to `settings.api.<connector>.<field>` directly. Mirrored conditional-on_page fork in `connector_playlist_sync_service` collapsed to an unconditional kwarg call; mock assertions retargeted to expect `on_page=None` explicitly (stricter, not weaker); 4 tests whose entire subject was the deleted machinery removed. Removes 3 `cast()`s. Opus executor in worktree per protocol (base-sync verified, uncommitted diff Fable-reviewed line-by-line, verified on main before commit).

- [x] **[Spoke 24](fable-sweep/24-db-models-aggregate-split.md)** *(optional — skip is a valid outcome)* — split `db_models.py` by aggregate; alembic empty-autogenerate-diff is the proof. Effort: M | Opus

    Status: **Skipped (2026-07-02, user-approved)** — both work-order premises falsified by exploration: 72 importer files (not ~30), and zero string-based relationship targets — all 31 are typed `Mapped[DBX]` annotations, so per-aggregate modules would need mutual cross-module imports between mutually-referencing aggregates (circular-import surface unpriced by the work order). Full rationale in the spoke file.

**Tests**: per-spoke; spoke 11's characterization suite lands before its refactor commit.

Effort: L | Dependencies: v0.8.14 (backend suite trusted), v0.8.15 not required | Status: Shipped (2026-07-02) — spokes 11 + 06 landed one-commit-per-spoke (11 as tests+refactor pair), 24 skipped with rationale; aggregate wave diff reviewed (mechanical AST fidelity check + independent engineer review — zero findings); full matrix green.

---

### v0.8.17: Sweep Closeout — Ratchet & Review
**Goal**: Flip the remaining suppressed ruff rules now that the code clears them, run the full matrix, have Fable review the aggregate sweep diff, and ship — locking the sweep's gains as an enforced floor. Bookended by a **dependency audit** that holds the dependency surface to the same discipline the sweep held the code to.

**Context**: [Spoke 26](fable-sweep/26-ratchet-closeout.md) is the spec (census baselines, per-rule flip conditions, and the PLR0913/0917 decision — recommendation: keep those two off, flip the five body-complexity rules). Step 1 (basedpyright) already shipped in v0.8.13; this version handles the remainder.

**What this unlocks**: The debt cannot silently regrow — CI fails if it does. Closes the v0.8.12–v0.8.17 arc.

**Persona**: Maintainer.

- [x] **[Spoke 26](fable-sweep/26-ratchet-closeout.md)** — re-census PLR per rule; flip cleared rules (any flip not already done in its clearing spoke); PLR0913/0917 decision (user call, options in the spoke); prune stale vulture-whitelist entries; full matrix green (`uv run pytest -m ""` · `basedpyright` · `ruff` · `pnpm --prefix web check && build` · import-linter). Effort: M | Opus

    Status: Completed (2026-07-02, branch `sweep-closeout-v0.8.17`) — re-census found **no PLR rule at 0** (per-rule outcome table in the spoke file), so none flipped; all seven re-documented dated in `pyproject.toml`. noqa whodunit solved (v0.8.7 commit `d2d3a179`; fixed by `_emitter` rename, no suppression). Vulture cleaned: 8 dead `operation_summary` properties deleted, `retryable`/`NO_ISRC`/`added_at_dates` whitelisted (latter two as dated parked decisions), 5 stale whitelist entries pruned; ratchet baselines lowered (whitelist 79→63, pyright-ignore 20→18). Matrix: 3386 passed / 2 skipped (`-m ""`), basedpyright 0/0, ruff clean, vulture clean, ratchet green. **import-linter does not exist in the repo** (spec premise false) — deferred as a dep-audit work order. PLR0913/0917 remains the user's call.
- [x] **Fable reviews the sweep** — aggregate diff review across v0.8.13–v0.8.16: call-site completeness, behavior preservation, layer boundaries. Findings become revisions here, not silent fixes.

    Status: Completed (2026-07-02) — the wave diffs were reviewed per-wave inside the window as re-scoped (v0.8.14 per-spoke, v0.8.15 aggregate + mechanical move-fidelity, v0.8.16 AST fidelity + engineer review — zero findings each; see the window execution order notes). The remaining unreviewed diff — the closeout branch itself — was reviewed at ship by independent engineer + QA agents: **zero violations**, two doc-tidy suggestions, both applied before tagging (stale `operation_summary` line in the import-sync handoff doc; import-linter strikethrough in spoke 26's checklist).
- [x] **Revive the visual-regression gate** *(promoted from hub Deferred, found shipping v0.8.15)*

    **Story**: The maintainer wants the Playwright visual suite to be a live gate again — today it fails 12/18 snapshots on browser-antialiasing drift (baselines captured on an older Chromium vs. Chrome 149), which means it can't catch a real regression and trains everyone to ignore red. A dead gate is worse than none.

    **Spec**: (1) check what browser CI pins (CI may be green with its own pin — confirm before assuming parity); (2) regenerate baselines with the current pinned browser (`pnpm --prefix web test:e2e --update-snapshots`), reviewing each diff is drift-only before accepting; (3) decide whether `pnpm --prefix web test:e2e` joins the version-bump verification bar in CLAUDE.md (it was absent from the v0.8.13–15 ships — the browser wasn't even installed locally); (4) note the browser↔baseline coupling in the dependency audit's supply-chain section.

    **Tests**: the regenerated suite green locally + CI is the acceptance. Effort: S | Executor: Opus | Status: Completed (2026-07-02, branch `sweep-closeout-v0.8.17`) — spec step 1 falsified the premise: CI was NOT green with its own pin; the web-e2e job had been red since 2026-06-01 on image↔package skew (`v1.60.0-noble` container vs `@playwright/test` 1.61.1 → browsers never launched). Fix: CI image pinned to `v1.61.1-noble`; under the correct image 16/18 committed baselines were already valid (the local 12/18 "drift" was macOS rendering, never authoritative) and only settings-sync ×2 regenerated — reviewed as legitimate v0.8.4 schedule-card growth (+663px), not drift. Suite 18/18 green in the pinned container (CI-green confirmation lands at ship — branch not pushed). Two latent bugs fixed in the regen procedure (`CI=true` required; `pnpm exec playwright test --update-snapshots`, pnpm 11.5 drops the flag after `--`). Decision (3): yes — Docker-pinned e2e + `uv run vulture` + `check_ratchet.sh` added to CLAUDE.md's version-bump bar. Coupling noted in the dependency audit (4).
- [x] **Dependency audit — every dep earns its place** *(docs-first; sibling to the v0.8.12 code audit)*

    Status: Completed (2026-07-02, branch `sweep-closeout-v0.8.17`) — findings + 10 work orders in [dependency-audit-findings.md](dependency-audit-findings.md). Headline: tree healthy (0 unused runtime deps either side, versions current); real findings: `starlette` imported undeclared in 3 middleware modules (W1), `interrogate`+`bandit` unwired dev deps (W2), one hand-rolled retry loop in `base_repo.py` that tenacity should own (W3), pnpm's release-age cooldown is tool-default not repo policy (W5), import-linter gap from spoke 26 (W10). All three pre-logged forward-signals carried (Starlette httpx2 · @react-email transitives via @neondatabase/auth 0.4.2-beta · Playwright browser↔baseline coupling). Work orders live in the findings doc, not `unscheduled.md` (deliberate: concurrent uncommitted edits there in the main tree).

    **Story**: The maintainer (Ash) wants the dependency surface held to the same discipline the sweep just imposed on the code — every dependency **necessary** (no dead installs), on the **most modern, well-supported** option for its job, and **fully utilized** rather than shadowed by bespoke code that reimplements what the library already gives us for free. Left unaudited the tree accretes redundant or abandoned deps and hand-rolled logic a maintained library would carry.

    **Decisions**:
    - **Docs-first, like v0.8.12** — the pass produces a findings doc + work orders; any removal/swap/adoption schedules into its own revision or a later version, so the closeout ship isn't gated on refactors.
    - **Three lenses per dep** — (1) *necessity*: is it imported/used at all; (2) *modernity & support*: best-maintained option for the job, or superseded; (3) *utilization vs bespoke*: are we reimplementing what the dep already offers.
    - **Respect the supply-chain posture** — pnpm's release-age cooldown (confirmed active during the v0.8.15 dependency bump) and the transitive security-floor `overrides` in `pnpm-workspace.yaml` are documented, not fought.

    **Spec** (starting checklist — extend during the pass):
    - Scope: `pyproject.toml` (21 runtime + 12 dev) and the frontend `web/package.json` + root `package.json`.
    - *Necessity*: `knip` (frontend, already wired) + a Python unused-dep pass (e.g. `deptry`) → list zero-import deps.
    - *Modernity/support*: carry the two forward-signals the bump surfaced — **Starlette 1.3 deprecates `httpx` under its TestClient in favour of `httpx2`** (plan the migration before a future Starlette hard-removes it), and **~20 deprecated `@react-email/*` transitive subdeps arrive via `@neondatabase/auth`** (assess whether that pathway is still the right one). Third signal from the v0.8.15 ship: **Playwright browser↔baseline coupling** — visual baselines silently expire when the browser bumps (Chrome 149 drifted 12/18 snapshots at ~1%); document the pinning/refresh discipline (see the closeout's visual-gate story).
    - *Utilization vs bespoke*: hunt hand-rolled logic that `tenacity` (retry/backoff), `rapidfuzz`/`jellyfish` (fuzzy/phonetic), `croniter` (schedule math), `pydantic` (validation), or `orjson` already cover.
    - Output: a `docs/backlog/` findings doc; each actionable finding becomes a scheduled work order.

    **Tests**: docs-only, no runtime change; any swap it schedules carries its own regression tests at the layer it touches. Effort: M | Executor: Opus | Status: Completed (2026-07-02, see Status note above)
- [x] **Ship** — `pyproject.toml` → `0.8.17` → `uv sync` → `pnpm --prefix web sync-api`; flip README rows for v0.8.13–v0.8.17. (Note: v0.8.13–v0.8.16 each bump `pyproject.toml` at their own deploys per version-management; 0.8.12 itself has no code artifact — the audit's tag is its docs commit.)

    Status: Completed (2026-07-02) — v0.8.13–v0.8.16 rows were flipped at their own ships; this ship flipped the v0.8.17 row, bumped to 0.8.17, and tagged. The identity-resolution research docs (memos + PDRs) rode the same push.

**Tests**: the re-enabled rules ARE the regression gate; full matrix is the acceptance bar.

Effort: M | Dependencies: v0.8.14, v0.8.15, v0.8.16 | Status: Shipped (2026-07-02)

---

#### Post-Deploy Revisions

_Reserved — post-ship revisions for v0.8.13–v0.8.17 accumulate here (`v0.8.1N.M` tags), each entry naming its wave version._

- [x] **v0.8.17.1** (2026-07-02) — fix version-metadata mismatch from the v0.8.17 ship

    **Why:** The ship commit was composed as `git commit -m "..." | tail -2 && git tag ...` — piping `git commit` through `tail` discards its exit code, so a failing pre-commit hook (`check-added-large-files`) went unnoticed. `git tag v0.8.17` and the push ran against the prior (merge) commit, which still read `pyproject.toml` version `0.8.16`. The tag, GitHub Release, and Fly.io deploy all completed before the mistake was caught.
    **What:** No code or API-contract change — the deployed closeout code (spoke 26, visual gate, dependency audit, identity research docs) was already complete and correct in the mis-tagged commit. This revision lands the missing version bump, regenerates `web/openapi.json` + the Orval client, and re-flips the README/backlog status lines that never made it into the original commit. Process fix: never pipe `git commit` through another command when its exit code gates subsequent steps.

- [x] **v0.8.17.2** (2026-07-02) — fix CLI test flakiness under CI's narrow terminal-width detection

    **Why:** Shipping v0.8.17.1 exposed a Python-CI failure unrelated to either revision's content: `tests/unit/interface/cli/test_sync_commands.py::TestScheduleValidationRules::test_cadence_requires_at` asserts a literal substring (`"--at"`) of Typer's rendered `BadParameter` error text. Reproduced deterministically on two separate CI attempts (identical truncated output both times — `"Spotify not configured — Spo..."` followed by a bare Rich panel border, the real error message entirely absent), but 0/5+ times locally across multiple environments (stripped credentials, matched worker count, explicit narrow `COLUMNS`) — GitHub Actions' non-interactive runner resolves Rich/Click's ambient terminal-width detection differently than any local reproduction attempt, truncating rendered output before the asserted substring survives. The underlying CLI validation is correct (exit code 2, right error raised) — this is a rendering/assertion fragility, not a functional defect.
    **What:** Added `tests/unit/interface/cli/conftest.py` — an autouse fixture pinning `COLUMNS=200`/`LINES=50` for every test in that directory, removing the dependency on ambient terminal detection. Monotonically safe (widening can only reduce truncation, never introduce it); verified with the full 3386-test suite green locally. Root-caused as far as local tooling allows — the exact GH Actions width-detection mechanism wasn't independently confirmed since it isn't reproducible outside that runner; the next CI run is the empirical verification.
