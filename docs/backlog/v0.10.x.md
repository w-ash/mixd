# v0.10.x: Artists, Albums & Physical Media

For strategic overview, see the [planning overview](README.md).

## User Workflow Context

The user's music identity is bigger than their track list, but mixd models only tracks today. Artists are `{"names": ["..."]}` JSON strings — "Radiohead" on Spotify and "Radiohead" on Last.fm are unrelated text, so there is no artist page, no "all tracks by X", no favorites. Albums are a bare `VARCHAR(255)` on tracks. And the physical side of a collector's identity — the vinyl on the shelf, the CDs in the car — is invisible: owning a record and listening to it leave no trace in the canonical history. This cycle promotes artists (v0.10.0) and albums (v0.10.1) to first-class entities on the same three-layer identity pattern as tracks, then builds physical ownership (v0.10.2, Discogs) and physical listening (v0.10.3, manual scrobbling) on top.

Persona view (from the [alignment table](README.md#persona-alignment)): the **Weekly Curator** gets deeper library modeling, favorites, and the Discogs/vinyl story; the **Tinkerer** gets a rich relational data model to explore and new workflow nodes; the **Casual** listener gets the intuitive browse-by-artist/album navigation every music app is expected to have.

---

### v0.10.0: First-Class Artists

**Goal**: Promote artists from embedded JSON strings to first-class entities with identity resolution, favorites, web UI, and workflow integration.

**Context**: Artists are currently stored as `{"names": ["..."]}` JSON on Track and ConnectorTrack records — "Radiohead" on Spotify and "Radiohead" on Last.fm are unrelated text. This milestone gives artists their own identity, cross-service mapping, favorites, and dedicated pages, following the same three-layer pattern established for tracks.

**What this unlocks**: artist browsing and favorites in the web UI, "tracks by my favorite artists" workflows, and the artist-identity substrate that v0.10.1 albums and the v1.1.x social layer build on.

**Persona**: all three — the Curator favorites artists and builds artist-driven playlists; the Tinkerer explores the mapping model; the Casual listener finally gets browse-by-artist.

**Key Design Decisions**:
- **Favorites are Mixd-only** — users favorite artists in the UI, because the favorite is *their* curation signal, not a mirror of platform state. Service sync (e.g., Spotify followed artists) deferred to a future milestone.
- **`track_artists` join table** replaces JSON artist embedding on tracks — enables efficient "all tracks by artist X" queries and proper relational integrity.

#### Backend Data Model Epics

- [ ] **Artist Entity & Domain Model**

    **Story**: The user wants artists to *be something* in mixd — favoritable, mappable across services, filterable in workflows — so the domain needs an `Artist` with identity instead of a name string riding along on tracks.

    **Decisions**:
    - `ArtistFavorite` mirrors `TrackLike` but without the `service` field — favorites are Mixd-only curation (see Key Design Decisions).

    **Spec**:
    - Domain: `Artist(id, name)` in `src/domain/entities/` — attrs frozen entity like `Track`
    - Domain: `ArtistFavorite(artist_id, is_favorited, favorited_at)`
    - `ConnectorArtist(connector_name, connector_artist_identifier, name, raw_metadata)` — per-service artist record
    - Artist matching: name normalization, MusicBrainz MBID when available

    **Tests**:
    - (unit) entity immutability + field validation, following existing `Track` entity tests

    Effort: M | Dependencies: None | Status: Not Started

- [ ] **Artist Database Schema**

    **Story**: The user's "all tracks by artist X" and favorites queries need a relational model — the JSON embedding can't answer them without full scans.

    **Decisions**:
    - Migration backfills `track_artists` from existing JSON and **retains the JSON column as fallback during transition** — no data loss window.
    - `artist_favorites` has no `service` column — Mixd-only by design.

    **Spec**:
    - Replicates three-layer pattern from `db_models.py`: `DBArtist` ↔ `DBArtistMapping` ↔ `DBConnectorArtist`
    - `artists`: `id`, `name` (unique), `created_at`, `updated_at`
    - `connector_artists`: `id`, `connector_name`, `connector_artist_identifier`, `name`, `raw_metadata` (JSON)
    - `artist_mappings`: `artist_id` → `artists`, `connector_artist_id` → `connector_artists`, `match_method`, `confidence`, `is_primary`
    - `artist_favorites`: `artist_id` → `artists`, `is_favorited`, `favorited_at`
    - `track_artists`: `track_id` → `tracks`, `artist_id` → `artists`, `position` (ordering). Replaces JSON embedding.
    - Alembic migration: create tables, backfill `track_artists` from existing JSON

    **Tests**:
    - (integration) migration backfill: JSON artists → `track_artists` rows with positions preserved
    - (integration) cascade behavior on artist/track delete

    Effort: M | Dependencies: Artist Entity & Domain Model | Status: Not Started

- [ ] **Artist Repository Layer**

    **Story**: Use cases and the web UI need clean artist data access — list, search, favorites — through the same repository seam every other aggregate uses.

    **Spec**:
    - Mirrors structure in `src/infrastructure/persistence/repositories/track/`
    - `ArtistRepository`: `get_by_id`, `get_by_name`, `list_artists` (paginated), `search_by_name`
    - `ArtistConnectorRepository`: `find_artists_by_connectors`, `bulk_upsert_connector_artists`, `set_primary_mapping`
    - `ArtistFavoriteRepository`: `get_favorites`, `save_favorite`, `is_favorited`
    - Mapper classes: `ArtistMapper`, `ConnectorArtistMapper`, `ArtistMappingMapper` following `mapper.py` patterns

    **Tests**:
    - (integration) repository round-trips: upsert, paginated list, search, favorite toggle

    Effort: M | Dependencies: Artist Database Schema | Status: Not Started

- [ ] **Alias-Aware Artist Comparison** *(scheduled 2026-07-02 from the identity research — [memo §7 option C](identity-resolution-design-space.md))*

    **Story**: "TEED" scrobbles and "Totally Enormous Extinct Dinosaurs" imports score as different artists in every match, dragging down *track*-match confidence and splitting artist views. This is deliberately the minimal slice of Artist Identity Resolution: it improves track matching immediately and becomes the alias substrate the artist entity builds on.

    **Decisions**:
    - **Anchor on artist MBID, not name** — live probe showed MusicBrainz has since made "TEED" the *primary* name with the full name as the alias; direction inverts over time, so the table maps MBID → all names/aliases (typed: Artist name / Legal name / Search hint, with locale), refreshed periodically.

    **Spec**:
    - MusicBrainz artist-alias cache + alias equivalence in the matching layer's artist comparison
    - Alias cache table: (artist MBID, name, alias_type, locale, fetched_at); MB `inc=aliases` fetch via existing client — shares the 1 req/s budget, trickle-scheduled
    - Hook into `domain/matching/algorithms.py` artist comparison as a pre-check via an injected equivalence lookup (domain stays pure — data passed in, not fetched), like the existing feat./ft. equivalences

    **Tests**:
    - (unit) TEED ↔ full name scores as artist match with alias data present, mismatch without
    - (integration) alias fetch + cache round-trip

    Effort: M | Dependencies: v0.8.18 (confidence semantics repaired — don't build alias scoring on broken constants) | Status: Not Started

- [ ] **Artist Identity Resolution**

    **Story**: The user wants one artist page per artist, not one per service spelling — canonical artist identity enables deduplication, enrichment, and cross-service queries.

    **Decisions**:
    - **Confidence scoring routes through the evaluation-service pattern, not fresh literals** *(revised 2026-07-02)* — the earlier sketch ("direct ID 100, MBID 95, name 80") would clone the scattered-constants defect (memo FM1f) the v0.8.18 repairs just fixed on the track side.
    - MusicBrainz artist MBIDs are the only reliable artist identifiers in the stack — anchor canonical artists on them where available.
    - Last.fm name matches are **capped-confidence evidence, never identity** — Last.fm shares one page per name string, so same-name artists co-mingle.
    - Populate during track import — when tracks arrive with artist info, upsert canonical artists.

    **Spec**:
    - Cross-service artist matching (Spotify artist ID ↔ Last.fm artist ↔ MusicBrainz artist MBID)
    - Leverages existing `MatchProvider` protocol from `domain/matching/protocols.py`
    - Spotify: artist ID from track metadata (already fetched); note Spotify has **no alias surface** and renames happen in-place (same ID, new name) — treat display names as mutable
    - Last.fm: artist name matching (case-insensitive, normalized) — route through `artist.getCorrection` first
    - MusicBrainz: artist MBID lookup via existing `MusicBrainzAPIClient`
    - Artist alias/abbreviation handling: provided by the Alias-Aware Artist Comparison epic above (MBID-anchored, not name-anchored)

    **Tests**:
    - (unit) matching evidence combination per provider, confidence caps for name-only matches
    - (integration) import path upserts canonical artists + mappings

    Effort: L | Dependencies: Artist Repository Layer, Alias-Aware Artist Comparison | Status: Not Started

#### Application & Workflow Epics

- [ ] **Artist Use Cases**

    **Story**: The web UI and CLI need application-layer orchestration for browsing and favoriting artists, following the Command/Result pattern.

    **Spec**:
    - Follow patterns from `src/application/use_cases/get_liked_tracks.py`
    - `ListArtistsCommand(limit, offset, search, sort_by, favorites_only)`
    - `GetArtistDetailCommand(artist_id)` → returns artist + connector info + track count + favorite status
    - `FavoriteArtistCommand(artist_id, is_favorited)` → toggle favorite

    **Tests**:
    - (unit + mock UoW) each use case happy path + not-found error path

    Effort: M | Dependencies: Artist Repository Layer | Status: Not Started

- [ ] **Artist API Endpoints**

    **Story**: The artist pages need REST access to artist data.

    **Spec**:
    - `GET /api/v1/artists` — paginated list with search, sort, favorites filter
    - `GET /api/v1/artists/{id}` — detail with connector mappings, track count, favorite status
    - `POST /api/v1/artists/{id}/favorite` — favorite an artist
    - `DELETE /api/v1/artists/{id}/favorite` — unfavorite
    - OpenAPI schema → Orval codegen for frontend hooks

    **Tests**:
    - (integration) route round-trips incl. favorite/unfavorite idempotency

    Effort: S | Dependencies: Artist Use Cases | Status: Not Started

- [ ] **Artist Workflow Nodes**

    **Story**: The Curator wants playlists like "tracks by my favorite artists" and "everything except artist X" — workflow filtering/sorting by artist.

    **Spec**:
    - Register in `src/application/workflows/transform_definitions.py`
    - `filter.by_artist`: config `{ artists: ["name1", "name2"], exclude: bool, favorites_only: bool }`
    - `sort.by_artist_name`: alphabetical sort by primary artist name
    - `favorites_only: true` enables "tracks by my favorite artists" workflow pattern
    - Node catalog auto-registration — no explicit `node_catalog.py` changes needed

    **Tests**:
    - (unit) filter include/exclude/favorites-only over a fixture tracklist; sort stability

    Effort: S | Dependencies: Artist Repository Layer | Status: Not Started

#### Web UI Epics

- [ ] **Artist List Page**

    **Story**: The user wants to browse and discover artists in their library the way they browse tracks.

    **Spec**:
    - `/artists` route; follows `web/src/pages/Library.tsx` pattern
    - Columns: name, track count, favorite status, connectors
    - Sortable by name, track count; search by artist name
    - Filter: all / favorites only
    - Add to sidebar navigation

    **Tests**:
    - (component) list renders, search + favorites filter drive the query hooks (MSW)

    Effort: M | Dependencies: Artist API Endpoints | Status: Not Started

- [ ] **Artist Detail Page**

    **Story**: The user wants a dedicated view per artist — who they are, where they live on each service, and every track of theirs in the library.

    **Spec**:
    - `/artists/:id`; follows `web/src/pages/TrackDetail.tsx` pattern
    - Sections: artist info, connector links (Spotify/Last.fm profile URLs), tracks by this artist
    - Favorite toggle button (heart icon)
    - Tracks section uses existing track table component with artist pre-filtered
    - External links to Spotify/Last.fm artist pages

    **Tests**:
    - (component) detail renders sections; favorite toggle mutation (MSW)

    Effort: M | Dependencies: Artist API Endpoints | Status: Not Started

- [ ] **Artist Favorites UI**

    **Story**: The Curator wants their favorite artists one click away everywhere artists appear.

    **Spec**:
    - Heart/star toggle on artist list rows and detail page header
    - "Favorites" filter toggle on artist list page
    - Dashboard stat: "X favorite artists" alongside existing track/playlist counts

    **Tests**:
    - (component) toggle state round-trip on list row; dashboard stat renders

    Effort: S | Dependencies: Artist List Page, Artist Detail Page | Status: Not Started

---

### v0.10.1: First-Class Albums

**Goal**: Promote `album` from a string field on Track to a proper entity with identity resolution, cross-service mapping, and dedicated web UI.

**Context**: Albums are currently a bare `VARCHAR(255)` on the tracks table — "OK Computer" on Spotify and "OK Computer" on Last.fm are unrelated strings. This milestone gives albums their own identity, following the same three-layer pattern (canonical → mapping → connector) established for tracks and extended for artists in v0.10.0.

**What this unlocks**: album browsing with ordered track listings, and the album entity that physical media (v0.10.2) and manual scrobbling (v0.10.3) require.

**Persona**: all three — album browsing is core navigation for everyone; the Curator additionally needs albums as the unit of physical ownership.

**Key Design Decisions**:
- **Album ≠ Release**: An "album" is the abstract work (MusicBrainz calls this a "release group"). A "release" is a specific pressing/edition (UK vinyl 1997, Japanese CD with bonus tracks). This milestone covers albums; v0.10.2 adds the release/pressing layer.
- **MusicBrainz release groups as canonical**: Recording lookups already return release data — we promote it from ignored to first-class.
- **Album artists vs track artists**: An album has its own artist(s) (e.g., "Various Artists" for compilations). `album_artists` join table parallels `track_artists` from v0.10.0.

#### Backend Data Model Epics

- [ ] **Album Entity & Domain Model**

    **Story**: The user's albums need identity — for cross-service mapping, ordered track listing, and the physical media tracking that follows — not a string that happens to match sometimes.

    **Spec**:
    - Domain: `Album(id, title, artists: list[Artist], release_date, track_count, album_type)` — attrs frozen entity
    - `album_type`: "album", "single", "EP", "compilation" (from MusicBrainz release group type)
    - `ConnectorAlbum(connector_name, connector_album_identifier, title, raw_metadata)` — per-service album record
    - Cover art URL stored in `raw_metadata` (sourced from Spotify, MusicBrainz Cover Art Archive, or Discogs)

    **Tests**:
    - (unit) entity construction + validation, matching existing entity test patterns

    Effort: M | Dependencies: v0.10.0 (first-class artists — albums reference artists) | Status: Not Started

- [ ] **Album Database Schema**

    **Story**: Album-level queries — track listings in disc/track order, albums by artist — need the relational model.

    **Decisions**:
    - Migration backfills albums from existing track `album` strings, deduplicating by normalized title+artist — existing data becomes browsable without re-import.

    **Spec**:
    - Three-layer pattern: `DBAlbum` ↔ `DBAlbumMapping` ↔ `DBConnectorAlbum`
    - `albums`: `id`, `title`, `album_type`, `release_date`, `created_at`, `updated_at`
    - `connector_albums`: `id`, `connector_name`, `connector_album_identifier`, `title`, `raw_metadata` (JSON)
    - `album_mappings`: `album_id` → `albums`, `connector_album_id` → `connector_albums`, `match_method`, `confidence`, `is_primary`
    - `album_tracks`: `album_id` → `albums`, `track_id` → `tracks`, `disc_number`, `track_number` (ordering)
    - `album_artists`: `album_id` → `albums`, `artist_id` → `artists`, `position`
    - Alembic migration: backfill + link tracks via `album_tracks`

    **Tests**:
    - (integration) backfill dedup by normalized title+artist; ordering columns round-trip

    Effort: M | Dependencies: Album Entity & Domain Model | Status: Not Started

- [ ] **Album Repository Layer**

    **Story**: Use cases need album data access — paginated browse, search, and ordered track listings — through the standard repository seam.

    **Spec**:
    - `AlbumRepository`: `get_by_id`, `list_albums` (paginated, with search/sort/filter), `get_album_tracks` (ordered by disc+track number)
    - `AlbumConnectorRepository`: `find_albums_by_connector`, `bulk_upsert_connector_albums`
    - Mapper classes following `mapper.py` patterns

    **Tests**:
    - (integration) repository round-trips incl. ordered track listing

    Effort: M | Dependencies: Album Database Schema | Status: Not Started

- [ ] **Album Identity Resolution**

    **Story**: One album page per album — "OK Computer" from Spotify and Last.fm resolve to the same canonical entity with unified detail.

    **Decisions**:
    - Populate during track import — when tracks arrive with album info, upsert canonical albums.
    - Confidence scoring mirrors track/artist matching (evaluation-service pattern per the v0.10.0 revision).

    **Spec**:
    - Cross-service album matching (Spotify album ID ↔ Last.fm album ↔ MusicBrainz release group)
    - MusicBrainz: release group MBID from existing recording lookup responses (already fetched, currently ignored)
    - Spotify: album ID from track metadata
    - Last.fm: album name matching (case-insensitive, normalized), `album.getInfo` for detailed metadata

    **Tests**:
    - (unit) match evidence combination; (integration) import path upserts canonical albums

    Effort: M | Dependencies: Album Repository Layer | Status: Not Started

#### Application & Web UI Epics

- [ ] **Album Use Cases & API**

    **Story**: The album pages need application orchestration and REST access.

    **Spec**:
    - `ListAlbumsCommand(limit, offset, search, sort_by, artist_id)` — filter by artist
    - `GetAlbumDetailCommand(album_id)` → album + tracks (ordered) + connector info + artists
    - `GET /api/v1/albums` — paginated list with search, sort, artist filter
    - `GET /api/v1/albums/{id}` — detail with track listing, connector mappings, artists

    **Tests**:
    - (unit + mock UoW) use cases; (integration) route round-trips

    Effort: M | Dependencies: Album Repository Layer | Status: Not Started

- [ ] **Album Web UI**

    **Story**: The user wants to browse and explore the albums in their library, and hop between artist ↔ album ↔ track views.

    **Spec**:
    - `/albums` list page: album title, artist(s), track count, release date, album type. Search, sort, paginate.
    - `/albums/:id` detail page: album header (title, artist, date, type, cover art), ordered track listing (disc/track number), connector links
    - Link from artist detail page → "Albums by this artist"
    - Link from track detail page → album
    - Add to sidebar navigation

    **Tests**:
    - (component) list + detail render with MSW fixtures; cross-links navigate

    Effort: M | Dependencies: Album Use Cases & API | Status: Not Started

---

### v0.10.2: Physical Media & Discogs

**Goal**: Track which albums the user owns and on which media. Discogs as the primary source for physical release metadata — pressings, formats, labels, catalog numbers.

**Context**: Your music identity isn't just streaming — it's the vinyl on your shelf, the CDs in your car, the albums you've purchased digitally. Discogs has 15M+ releases with rich physical media metadata (format, label, catalog number, country, pressing notes). Importing a user's Discogs collection instantly populates "what I own and in what format."

**What this unlocks**: the physical half of the library — ownership records that v0.10.3's manual scrobbling and future collection features build on.

**Persona**: Curator-primary (the record collector's data reclamation); Tinkerer secondary (a new connector to explore). Casual: marginal _(persona fit — confirm)_.

**Key Design Decisions**:
- **Release ≠ Album**: An album (release group) can have many releases (pressings). "OK Computer" is one album; the 1997 UK vinyl, 2008 Japanese CD, and 2017 OKNOTOK remaster are three releases.
- **Discogs as primary physical source**: MusicBrainz has releases too, but Discogs has stronger collector community data (condition, notes, marketplace). Start with Discogs; MusicBrainz release data can supplement later.
- **Read-only initially**: Import from Discogs, don't write back. User manages their Discogs collection on Discogs.

#### Discogs Connector Epics

- [ ] **Discogs API Client & Auth**

    **Story**: Every Discogs operation — collection import, release detail — needs an authenticated client; this is the connector foundation.

    **Decisions**:
    - OAuth 1.0a flow (different from Spotify's OAuth 2.0 — similar to Last.fm's signature-based auth).

    **Spec**:
    - `DiscogsAPIClient(BaseAPIClient)` with httpx, Pydantic models
    - Rate limit: 60 requests/minute (authenticated)
    - Key endpoints: `/users/{username}/collection/folders/0/releases` (full collection), `/releases/{id}` (release detail), `/masters/{id}` (master release)
    - Pydantic models for release, master, artist, label, format responses
    - Reuse: `_shared/http_client.py`, `_shared/retry_policies.py`

    **Tests**:
    - (unit) auth signature construction; (integration, mocked transport) pagination + rate-limit retry

    Effort: M | Dependencies: None | Status: Not Started

- [ ] **Discogs Release Matching**

    **Story**: The user's Discogs collection is only useful once each release connects to the canonical album it's a pressing of.

    **Decisions**:
    - Discogs "master release" ≈ MusicBrainz "release group" ≈ mixd "album" — that's the mapping spine.
    - Prefer the MusicBrainz↔Discogs cross-reference (MusicBrainz stores Discogs URLs as external links); fall back to artist+title fuzzy matching with duration cross-check.

    **Spec**:
    - Match Discogs releases to canonical albums, and Discogs master releases to MusicBrainz release groups
    - Match by: artist+title normalization, MB↔Discogs cross-reference, duration cross-check fallback

    **Tests**:
    - (unit) match ladder: cross-reference hit, normalized-title hit, fuzzy fallback

    Effort: M | Dependencies: Discogs API Client, v0.10.1 (first-class albums) | Status: Not Started

#### Physical Media Epics

- [ ] **Release Version Model**

    **Story**: "Which pressing do I own?" needs a release entity — format, label, catalog number, country are the physical identity of the object on the shelf.

    **Spec**:
    - Domain: `AlbumRelease(id, album_id, format, label, catalog_number, country, year, discogs_release_id, musicbrainz_release_id, notes)`
    - `format`: "Vinyl", "CD", "Cassette", "Digital", "Vinyl (12\")", "Vinyl (7\")", "Box Set", etc. (Discogs format taxonomy)
    - DB: `album_releases` table with FK to `albums`
    - Multiple releases per album is the norm — a well-known album might have 50+ pressings on Discogs

    **Tests**:
    - (integration) many releases per album round-trip

    Effort: M | Dependencies: v0.10.1 (first-class albums) | Status: Not Started

- [ ] **Ownership Tracking**

    **Story**: "What do I own, and on what format?" is the core question for physical media tracking — sourced from Discogs import or entered by hand.

    **Spec**:
    - Domain: `OwnedRelease(id, album_release_id, source, acquired_date, notes)` — `source` is "discogs_import" or "manual"
    - DB: `owned_releases` table with FK to `album_releases`
    - Discogs collection import: bulk-create `OwnedRelease` records
    - Manual entry: web UI form on album detail page — pick a known release or add custom
    - Future: Discogs condition grades, purchase price (available in collection API)

    **Tests**:
    - (integration) ownership create from both sources; (unit) source enum validation

    Effort: M | Dependencies: Release Version Model | Status: Not Started

- [ ] **Collection Import from Discogs**

    **Story**: One import populates the collector's entire physical collection — the "instant gratification" moment of the milestone.

    **Decisions**:
    - Incremental after the first run: track last import timestamp, only process new additions.

    **Spec**:
    - Paginated API: `/users/{username}/collection/folders/0/releases` returns all releases
    - Per release: match to canonical album → create `AlbumRelease` if new → create `OwnedRelease`
    - Progress tracking via existing SSE infrastructure

    **Tests**:
    - (integration, mocked transport) full + incremental import; unmatched releases surface, not vanish

    Effort: M | Dependencies: Discogs Release Matching, Ownership Tracking | Status: Not Started

- [ ] **Collection Web UI**

    **Story**: The collector wants visual confirmation of what they own and in what format, browsable and filterable.

    **Spec**:
    - Album detail page: "You own this" badge with format details ("Vinyl — 2015 UK pressing, XL Recordings, XLLP790")
    - Collection overview page (`/collection`): all owned albums grouped by format, sortable by artist/title/acquired date
    - Filter: "Vinyl only", "CD only", "Digital only"
    - Dashboard stat: "X albums owned across Y formats"

    **Tests**:
    - (component) badge renders from ownership fixture; collection page filters

    Effort: M | Dependencies: Ownership Tracking, Album Web UI (v0.10.1) | Status: Not Started

---

### v0.10.3: Manual Scrobbling

**Goal**: Record physical album listens — "I put this record on at 3pm" — scrobble to Last.fm and store as canonical plays in mixd.

**Context**: When you listen to a vinyl record, that listening event is invisible to your digital history. Mixd should let you log it: pick the album, enter when you started, and the system figures out the per-track timestamps from track order and durations. The plays become part of your canonical history — indistinguishable from streaming plays in workflows and metrics.

**What this unlocks**: complete listening history — physical plays join the canonical record, so "loved but forgotten" workflows and play metrics stop under-counting vinyl listeners.

**Persona**: Curator-primary (the vinyl listener whose history has holes); Tinkerer secondary (CLI + scripting surface). Casual: unlikely to log physical listens _(persona fit — confirm)_.

**Key Design Decisions**:
- **Album-level input, track-level output**: User says "I listened to this album starting at 3pm." System calculates per-track timestamps and creates individual `TrackPlay` records. Each track gets its own play entry because that's what Last.fm expects and what mixd's play history model requires.
- **Optional Last.fm scrobble**: User chooses whether to also scrobble to Last.fm or keep it mixd-only. Some people want to keep physical and digital scrobbles separate.
- **Retroactive and real-time**: Support both "I listened yesterday at 3pm" and "I'm listening right now."

#### Scrobble Infrastructure Epics

- [ ] **Last.fm Scrobble API**

    **Story**: Sending play events to Last.fm is the foundation for physical listens reaching the user's scrobble history.

    **Spec**:
    - Implement `track.scrobble` on `LastFMAPIClient` — authenticated POST, batch up to 50 tracks per request
    - Last.fm `track.scrobble` API: `method=track.scrobble`, params: `artist`, `track`, `timestamp` (Unix epoch), optional `album`, `duration`
    - Batch: up to 50 scrobbles per request via indexed params (`artist[0]`, `track[0]`, `timestamp[0]`, etc.)
    - Requires same session key auth as `track.love` (already implemented)
    - Scrobble rules: track must be >30s duration, listen must be >50% of duration or >4min

    **Tests**:
    - (unit) batch param encoding; (integration, mocked transport) partial-failure reporting

    Effort: S | Dependencies: None (builds on existing Last.fm auth — `track.love` write already works) | Status: Not Started

- [ ] **Album Listen Calculator**

    **Story**: Converting "album listen at 3pm" into individual track plays with correct timestamps is the core logic of manual scrobbling — and it must be right, because it writes permanent history.

    **Decisions**:
    - Handle missing durations: skip track (log warning), or use average duration as estimate.

    **Spec**:
    - Pure domain function: `calculate_listen_timestamps(tracks: list[Track], start_time: datetime) -> list[tuple[Track, datetime]]`
    - Track N+1 starts at: start_time + sum(duration of tracks 1..N)
    - Handle multi-disc albums: disc order → track order (already modeled in `album_tracks` via disc_number + track_number)
    - Pure function, no side effects — testable in isolation

    **Tests**:
    - (unit) timestamp arithmetic incl. multi-disc ordering and missing-duration handling

    Effort: S | Dependencies: v0.10.1 (first-class albums with ordered track listings) | Status: Not Started

- [ ] **Record Album Listen Use Case**

    **Story**: The user logs one album listen; the system resolves tracks, calculates timestamps, persists canonical plays, and (optionally) scrobbles — one command, full flow.

    **Decisions**:
    - Plays land in the same table as streaming plays — canonical history is unified.
    - Play source tracking: `service="manual"` or `service="physical"` to distinguish from streaming plays in queries.

    **Spec**:
    - `RecordAlbumListenCommand(album_id, started_at, scrobble_to_lastfm: bool = True)`
    - Creates `TrackPlay` records in mixd for each track
    - If `scrobble_to_lastfm`: batch-scrobble to Last.fm via `track.scrobble`
    - Result includes: tracks played, timestamps, Last.fm scrobble success/failure per track

    **Tests**:
    - (unit + mock UoW) plays persisted with calculated timestamps; scrobble skipped when toggled off; per-track scrobble failure reported, not swallowed

    Effort: M | Dependencies: Album Listen Calculator, Last.fm Scrobble API | Status: Not Started

#### User Interface Epics

- [ ] **Manual Listen Web UI**

    **Story**: The vinyl listener finishes a record, opens the album page, and logs the listen in three clicks — with a preview so they trust what's about to be written to history.

    **Spec**:
    - Album detail page: "Log a listen" button → modal/drawer
    - Date/time picker: defaults to "now", supports past dates/times
    - Preview: calculated track-by-track timeline before submission ("Track 1 at 15:00, Track 2 at 15:04:30, ...")
    - Toggle: "Also scrobble to Last.fm" (default on)
    - Confirmation: success toast with track count and Last.fm status
    - History: "Recent listens" section on album detail page showing logged physical plays

    **Tests**:
    - (component) picker + preview render; submit fires mutation with toggle state (MSW)

    Effort: M | Dependencies: Record Album Listen Use Case, Album Web UI (v0.10.1) | Status: Not Started

- [ ] **Manual Listen CLI Command**

    **Story**: The Tinkerer wants to log listens from the terminal and script them.

    **Spec**:
    - `mixd listen log <album_id_or_name>` — interactive album picker if no ID given
    - `--started-at` — ISO datetime or "now" (default)
    - `--no-scrobble` — skip Last.fm, mixd-only
    - `--dry-run` — show calculated timestamps without submitting
    - Rich output: table of tracks with calculated play times

    **Tests**:
    - (unit, CliRunner) arg parsing incl. `--dry-run` producing no writes

    Effort: S | Dependencies: Record Album Listen Use Case | Status: Not Started
